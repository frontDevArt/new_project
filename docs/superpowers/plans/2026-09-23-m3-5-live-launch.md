# M3.5. Боевой запуск: план реализации

> **Для агента-исполнителя:** одна фаза — одна сессия. В начале сессии читаешь
> «Global Constraints», «Карта файлов», «Долги», «Результат фазы N−1» и свою
> фазу; чужие фазы не трогаешь. Рядом лежит спека —
> `docs/superpowers/specs/2026-09-23-m3-5-live-launch-design.md`, решения 1–18
> в фазах не пересматриваются. В конце сессии дописываешь в этот файл раздел
> «Результат фазы N» и стартовый промпт для следующей фазы, затем делаешь коммит.
> Шаги помечены `- [ ]` — отмечай по ходу. Исполнять план помогает скилл
> `superpowers:executing-plans` или `superpowers:subagent-driven-development`.
>
> План нарочно плотнее планов M2 и M3: код приведён только там, где он —
> договор (миграция, порты, конфиг, модель сообщения). Остальное — поведение,
> имя теста и файл. Тест пишется до правки, как и раньше.

**Цель:** система каждый день сама обходит ленту и шлёт брокеру уведомления;
«звони сейчас» приносит единицы вариантов, а не сотни; из сообщения за секунду
понятно, кому звонить и про какую квартиру.

**Архитектура:** ядро не переписывается. Появляются: цикл из конфига и
установщик расписания (`schedule`), вёрстка сообщения структурой (`layout`),
шаг воронки, открывающий страницы кандидатов (`pages`), кэш страниц и
исключения из отказов (миграция 012), седьмой фактор балла `wishes`,
быстрый поиск `find` и отметки `mark`.

**Стек:** Python 3.14 (`.venv`), SQLite, pytest, `requests`, Playwright
(уже в `requirements-playwright.txt`), PyYAML, openpyxl. Новая зависимость
одна — `tzdata` (фаза 1): `zoneinfo` на Windows без неё не знает часовых поясов.

**Исходное состояние (снято 23.09.2026):**

| Что | Значение |
| --- | --- |
| HEAD | `e12ef68`, дерево чистое |
| Батарея | **824 passed, 25 skipped** (44,4 с) |
| Схема базы | 11 |
| `.env` | Telegram заполнен; `GDRIVE_*`, `REQUESTS_SHEET` пусты |
| Боевая база | нет: `data/listam.sqlite` — пустой файл 4 КБ, `runs` пуст |
| Расписание | не настроено |

---

## Global Constraints (нарушать нельзя)

- Схема базы меняется **только** миграцией **012** (фаза 3). Понадобилась
  вторая — это находка в отчёт фазы, а не файл `013`.
- Новый метод порта — новый контрактный тест в `tests/contracts/`.
- Ни один аккаунт, путь, ключ, идентификатор и имя адаптера не зашит в код:
  внешнее — за портом, выбор — в конфиге, секреты — только в `.env`,
  подключение — в `listam/wiring.py`. Путь к интерпретатору и к проекту
  расписание вычисляет при установке (`sys.executable`, корень пакета),
  в репозиторий и README они не пишутся.
- Все отметки времени в базе — UTC (`to_iso`/`from_iso`). Человеческое время —
  через `locale.timezone`.
- **robots.txt:** никаких query-параметров фильтров. Лента — путевая
  пагинация, объявление — `/item/<id>`. Агент сам на list.am не ходит
  (WebFetch и подобное запрещены): страницы берёт только инструмент своим
  `Fetcher`.
- **Воронка:** страница объявления открывается только для кандидата под живую
  заявку, с паузой `funnel.delay_seconds` и потолком `funnel.max_opens_per_run`.
  Никакого массового обхода. Открытая страница кэшируется в `listing_pages`.
- **Вёрстка Telegram — строго по пункту 8** `docs/анализ-после-M3.md`: карточка
  из трёх строк, словарь значков (новых нет), HTML, резать только между
  карточками.
- Пороги не поднимаются, чтобы тест позеленел. Исключение — фаза 4, и каждое
  изменённое число приходит с замером.
- Ноль в пороге — «ноль», выключается `null`; бессмысленное значение — код 2
  на входе. Читать пороги — через `listam.config.threshold` / `positive` /
  `score_threshold` / `switch` / `hours`.
- След звонка (`matches.status`, `matches.reject_reason`) пишет только
  человек — командой `mark` (фаза 6). `MATCH_COMPARED` не расширяется:
  `origin` пишется при вставке и не сравнивается.
- Тесты — только `.venv/Scripts/python.exe -m pytest -q`. Любой прогон CLI
  из скрипта — только с `PYTHONIOENCODING=utf-8`.
- `listam/crawler.py` правится только там, где это прямо названо.
- **Живые проверки Playwright не исполняются** — они вписываются в раздел
  «Долги» (Д-2) с командой запуска. Отправлять в Telegram можно;
  читать отправленное обратно — нет.
- **Реальных заявок нет — работаем на примере** (раздел «Пример заявок»).
  Замер на примере помечается «предварительно» и попадает в долг Д-1.
- Ни одного числа в отчёте фазы без команды, которая его напечатала.
- Если для живого запуска чего-то не хватает (ключ, доступ, решение) —
  спроси пользователя, обходной путь не выдумывай.
- Числа «ожидается N passed» — арифметика от 824 плюс тесты фазы. Разошлось
  на один-два — не повод подгонять: сверь и поправь число в плане.

## Карта файлов

| Файл | Ответственность | Фаза |
| --- | --- | --- |
| `tests/test_matching.py`, `tests/test_events.py` | даты из календаря → от настоящих часов | 1 |
| `listam/notifications.py` | пустое «горячее» не шлётся; вёрстка; `origin` | 1, 2, 4 |
| `config/prod.yaml`, `config/dev.yaml` | `locale`, `schedule`, `funnel`, `feedback`, `notify.layout`, `wishes` | 1–6 |
| `listam/schedule.py` | цикл, лог, тревога, задачи `schtasks` / `crontab` | 1 |
| `listam/adapters/run_lock.py` | `busy()` — занят ли замок, не захватывая его | 1 |
| `listam/doctor.py` | строки «Расписание», «Воронка» | 1, 3 |
| `requirements.txt` | `tzdata` | 1 |
| `listam/layout.py` | `Message`, `Section`, карточка, HTML и простой текст | 2 |
| `listam/ports/notifier.py` | `send(message, to=None)` | 2 |
| `listam/adapters/notify_telegram.py` | HTML, превью выключено, резка между карточками | 2 |
| `listam/migrations/012_funnel.sql` | `listing_pages`, `request_exclusions`, `matches.origin` | 3 |
| `listam/domain/models.py` | `PageFields`, `ListingPage`, `Exclusion`, `Match.origin` | 3 |
| `listam/ports/database.py`, `listam/adapters/db_sqlite.py` | кэш страниц, исключения, `origin` | 3, 6 |
| `listam/parsers/item_page.py` | разбор страницы объявления | 3 |
| `tests/fixtures/item-*.html` | 2–3 настоящие страницы, снятые инструментом | 3 |
| `listam/domain/wishes.py` | словарь пожеланий | 3 |
| `listam/domain/scoring.py` | `must_have`, фактор `wishes`, исключения | 3, 4, 6 |
| `listam/pages.py` | шаг воронки | 3 |
| `listam/matching.py` | страницы в подборе, `origin`, «страница не открыта» не закрывает | 3, 4 |
| `listam/requests_sync.py` | неизвестное слово пожелания | 3 |
| `listam/find.py` | быстрый поиск | 5 |
| `listam/feedback.py` | `mark`, исключения | 6 |
| `listam/cli.py` | `cycle`, `schedule`, `pages`, `find`, `mark` | 1, 3, 5, 6 |
| `tmp/measure_m35.py` | замер «единицы, а не сотни» на копии боевой базы (повтор — Д-1) | 4 |
| `README.md`, `docs/устройство.md` | руководство оператора; подробности — в `docs/` | 1–7 |

---

## Пример заявок (пока нет реальных — долг Д-1)

Три синтетические заявки. Клиенты помечены словом «ПРИМЕР», чтобы брокер
не спутал их уведомления с настоящими. Кладутся в `data/requests.csv`
(папка `data/` не коммитится):

```csv
id,client_name,client_phone,status,budget_max,budget_stretch,districts,districts_priority,rooms,area_min,area_max,floor_min,floor_max,no_first_floor,no_last_floor,must_have,nice_to_have,floor_rules,notes
R-1,ПРИМЕР узкая,,active,120 000 $,,Арабкир,Арабкир,3,70,95,2,,да,нет,,,,без пожеланий: страницы не нужны
R-2,ПРИМЕР средняя,,active,175 000 $,190000,"Канакер-Зейтун, Нор Норк",Канакер-Зейтун,2-3,80,110,,,да,да,ремонт,"балкон, лифт",,пожелания со страницы
R-3,ПРИМЕР широкая,,active,250 000 $,,"Кентрон, Арабкир, Давташен",Кентрон,2-4,60,,,,нет,нет,не панель,евроремонт,,нарочно широкая: проверка первичной подборки
```

Когда пользователь пришлёт реальные заявки: они заменяют пример в
`data/requests.csv`, `python -m listam --env prod requests` закрывает R-1…R-3
(«закрыты (нет в источнике)»), а замеры фазы 4 повторяются на живых.

## Долги (ведутся всем планом)

Исполнитель **дописывает** сюда, а не исполняет. Прогоняет пользователь.

| # | Долг | Чей | Как закрыть |
| --- | --- | --- | --- |
| Д-1 | Реальные заявки клиентов | пользователь | прислать строки CSV по образцу выше; повторить замер фазы 4: `PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe tmp/measure_m35.py --label real --out <папка вне репозитория>` (копия базы, оригинал не трогается). Замер фазы 4 на примере — **предварительный**: у R-2/R-3 балл без страниц (в кэше 5), вес `wishes` 15 не мерен; темп прихода объявлений в час неизвестен (окно модели — 100 объявлений) — брать из логов часового цикла, когда встанет расписание (Д-6) |
| Д-2 | Живые проверки Telegram через Playwright | пользователь | `TELEGRAM_LIVE=1 .venv/Scripts/python.exe -m pytest -q tests/live -s`; список проверок — ниже |
| Д-3 | Компьютер не засыпает | пользователь | «Электропитание» → сон «Никогда» при питании от сети; задачи идут с `/it` — пользователь должен быть залогинен |
| Д-4 | Переезд на Linux вживую не проверен | пользователь | на сервере: `git clone`, `.env`, `pip install`, `python -m listam schedule show`, `schedule install` |
| Д-5 | «За секунду понятно» глазами брокера | пользователь | неделя фазы 7: отзыв брокера о сообщениях |
| Д-6 | Живая отправка и установка расписания фазы 1 | пользователь | классификатор прав не дал агенту: `notify --digest` и `schedule install` — команды в «Результате фазы 1», «Что осталось». На начало фазы 2 не сделано (`schtasks /query /tn listam-hourly` → «не найдено», `runs` — одна строка `full`, `data/logs` нет); пользователь велел не блокироваться. Первая отправка уйдёт уже в вёрстке фазы 2. На начало фазы 3 — то же (`schtasks` → «не найдено», `runs` — одна строка `full`, `data/logs` нет, `doctor` «часовой: ещё не было»); пользователь: «не блокер, оставим в долгах». Циклы теперь с шагом `pages` — ставить после коммита фазы 3. На начало фазы 4 — то же (`schtasks` → «не найдено», `runs` — одна строка `full`, `fresh` нет, `data/logs` нет, `notifications` 0 строк, `doctor` «часовой: ещё не было»); пользователь: «если не блокер — оставим в долгах». Журнала «горячего» поэтому нет, фаза 4 мерила пробным прогоном. На начало фазы 5 — то же (`schtasks` → «не найдено», `runs` — одна строка `full`, `data/logs` нет, `notifications` 0 строк, `doctor` «часовой: ещё не было»); пользователь: «если уверен, что не блокер — оставь в долгах»; `find` журнал не читает. На начало фазы 6 — то же (`schtasks` → «не найдено» для обеих задач, `runs` — одна строка `full`, `fresh` нет, `data/logs` нет, `notifications` 0 строк, `doctor` «часовой: ещё не было»); пользователь: «если уверен, что не блокер — оставляй в долгах»; `mark` сам зовёт подбор и журнал не читает. На начало фазы 7 сессия шла на **другой машине** (`DESKTOP-79NF3LJ`, проект в `Downloads\list`, а не `Artur.A.Gevorgyan\Downloads\new_project`): `schtasks` → «не найдено» для обеих задач, `data/logs` нет, **боевой базы нет вовсе** (`data/listam-prod.sqlite` и `data/store` отсутствуют), `tzdata` в `.venv` не стоял (поставлен, см. «Результат фазы 7»), в `data/requests.csv` не пример, а одна заявка R-101. Пользователь: «если не блокер — оставим в долгах, если блокер — делаем». Для недели приёмки это блокер, но замену `data/requests.csv` примером классификатор прав отклонил («Irreversible Local Destruction»); запуск передан пользователю — «Результат фазы 7», «Что осталось» |
| Д-7 | 💎 ленты ловит неотмеченные выбросы площади | фаза 4 или M5 | `notify --feed --dry-run` на боевой базе: после отсева `anomaly` первыми идут «$110 000 · 1100 м²», «$150 000 · 800 м²» (похоже на дома или участки; страницы не открывались). Нужна проверка площади в `domain/checks`, а не правка вёрстки |
| Д-8 | Вёрстка страницы снятого объявления не видена | пользователь / фаза 4 | в боевой базе на 23.09 снятых нет (`select count(*) from listings where status='gone'` → 0). `pages` считает снятым ответ 404/410 (`FetchError.status`); если list.am отдаёт снятое кодом 200 с плашкой — страница уйдёт в `failed` и после `max_attempts` станет «поля неизвестны». Проверить: после первого ночного обхода с `gone` — `pages --keep-html` на таком id и посмотреть HTML |
| Д-10 | «Подешевевший тонет в хвосте» не мерен | фаза 7 | на копии фазы 4 у всех 20 664 объявлений одна цена (`price_history`: объявлений с двумя ценами — 0), 📉 в пробных прогонах — 0. После недели расписания: `tmp/measure_m35.py` + счёт 📉 за потолком `notify.hot.per_request` в журнале; ноль или единицы — «не чинится», иначе 📉 первыми в пределах заявки (тест) |
| Д-11 | Очередь воронки разойдётся в «звони сейчас» | фаза 7 | решение 9: матч, родившийся после открытия страницы, идёт в «горячее» новым (`origin = market` — заявка не правлена). Кандидатов на копии после фазы 4 — 948, в очереди 943: ~32 часовых прогона по 30 страниц, и в каждом до 30 таких матчей на R-2/R-3. Смотреть в логах первых суток: если «звони сейчас» у заявок с пожеланиями стабильно у потолка — вопрос к спеке (не решение фазы) |
| Д-12 | Отказ по кластеру держится, пока жив «якорь» кластера | фаза 7 или M3.6 | исключение `cluster` хранит `matches.cluster_id` на момент отметки; кластер называется по старейшему объявлению (`domain/clustering._anchor`) — уйдёт оно с ленты, имя сменится, и отвергнутая квартира может вернуться под новым именем. Без второй миграции: сверять ещё и по `match_id` → `matches.listing_id` (член кластера). Проверить: после недели расписания `select count(*) from request_exclusions where kind='cluster' and value not in (select cluster_id from listings where cluster_id is not null)` — исключений, чьё имя кластера из базы исчезло |
| Д-13 | Откат отказа у заявки с `must_have` возвращает не всё | вопрос к спеке | на копии: `mark R-3 22985823 rejected --reason "первый этаж, дорого"` закрыл 234 матча, `mark … new` снял исключения, но 233 остались закрытыми: у R-3 `must_have: не панель`, страницы у них не открыты — подбор считает их кандидатами «страница не открыта» и не закрывает, и не возвращает (решения 9, 12). Вернутся, когда `pages` откроет страницы. То же у исключения по полю страницы: неизвестное поле не отказ, а `pages` об исключениях не знает и страниц ради них не откроет |
| Д-14 | Две машины — одна неделя | пользователь | боевая база фаз 1–6 лежит на машине `Artur.A.Gevorgyan` (`storage.kind: local` — хранилище не общее); на `DESKTOP-79NF3LJ` её нет. Задачи расписания на обеих — брокер получит каждое сообщение дважды, а базы разойдутся. Выбрать машину недели; на второй — `python -m listam --env prod schedule remove` и `schtasks /query /tn listam-hourly` → «не найдено». Заодно решить, что такое R-101 в `data/requests.csv` этой машины (с именем и телефоном): реальная заявка — закрывает Д-1, иначе — пример R-1…R-3 |
| Д-9 | Бытовая техника на странице не разобрана | фаза 4 или 5 | живой `pages --max 5` дал «⚠ Неразобранных подписей: 24 (Кондиционер ×4, Плита ×4, Посудомоечная машина ×4, Стиральная машина ×4, Сушильная машина ×4, Холодильник ×4)». В словаре пожеланий их нет — матчам не мешает. Снять фикстуру (`pages --keep-html`), добавить подписи в `parsers/item_page.py` |

Живые проверки Д-2 (дописываются фазами):

| # | Что проверить глазами в Telegram Web | Фаза |
| --- | --- | --- |
| Т-1 | карточка из трёх строк, цена жирным, «Открыть» — ссылка, превью нет; тест `tests/live::test_a_card_arrives_as_html` | 2 |
| Т-2 | длинная заявка режется между карточками, продолжение — с шапкой; тест `tests/live::test_long_message_is_cut_between_cards_with_the_head` | 2 |
| Т-3 | символы `<`, `>`, `&` в адресе объявления не ломают сообщение; тест `test_a_card_arrives_as_html` | 2 |
| Т-6 | дайджест: сводка первым сообщением, заявка только с закрытиями — строка «❌» в сводке, не сообщение; время в сводке — ереванское | 2 |
| Т-4 | тревога цикла приходит одной строкой и не чаще `alert_every_hours` | 1 |
| Т-5 | L-1, H-4, H-5 приёмки M3 (продолжение раздела, 429, журнал не записан) | из M3 |
| Т-8 | шапка заявки с отказами в Telegram: «… · до $250 000 · без 1-го этажа» в «звони сейчас» и в обоих разделах дайджеста; строка «❌ N (клиент отказал: первый этаж N)» в сводке. На копии: `mark R-3 22985823 rejected --reason "первый этаж"`, затем `notify --digest` (без `--dry-run` — только на копии с `notify.kind: telegram`) | 6 |
| Т-7 | `find --open` вживую: `PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe -m listam --env prod find --district Арабкир --rooms 3 --max-price 120000 --wish ремонт --open 3` — «Открыто страниц: 3 … разобрано 3», строка «ремонт: известно у 3 из …», в `listing_pages` +3; пауза между страницами — `funnel.delay_seconds` (не Telegram, но живой Playwright — агенту нельзя) | 5 |

---

# Фаза 1. Боевой запуск на нынешнем коде

**Смысл:** с конца этой фазы система работает сама на Windows, и история цен
начинает копиться. Всё остальное в M3.5 доводится поверх живого прогона.

**Ожидается после фазы:** ~850 passed, 25 skipped, схема 11 (вышло 888 — см. «Результат фазы 1»).

### Задача 1.1. Тесты не зависят от календаря

Приёмка QA после M3 нашла «бомбы»: `datetime(2026, 9, 23, …)` в
`tests/test_matching.py` (строки ~161, 427, 450, 482, 538, 578, 598) и
`tests/test_events.py` (~65). Сравниваются с настоящими часами — завтра упадут.

- [x] `grep -n "datetime(2026" tests/` — список мест; для каждого решить,
  сравнивается ли дата с `now()` кода. Сравнивается — заменить на
  `datetime.now(timezone.utc) + timedelta(…)`. Не сравнивается — оставить.
- [x] Оставшимся календарным датам — пометка в строке
  `# календарь: не сравнивается с часами`.
- [x] Сторож `test_no_calendar_dates_against_real_clock` в `tests/test_docs.py`:
  ищет `datetime(20` в `tests/` и падает на строке без этой пометки. Новая
  бомба не проскочит.

### Задача 1.2. Пустое «звони сейчас» не шлётся (решение 5)

- [x] Тест `test_an_empty_hot_is_not_sent_but_moves_the_window`
  (`tests/test_notifications.py`): окно без событий, `kind="hot"`, канал —
  подменный `Notifier`; `send` не зван, строка журнала `events=0` есть,
  повтор берёт окно от неё.
- [x] Тест `test_an_empty_digest_is_still_sent`: у дайджеста «событий нет» —
  законный ответ раз в сутки.
- [x] Правка `run_notify`: `kind == "hot" and report.events == 0` → не
  звать `send`, писать журнал, `report.notes = "событий нет — не отправлено"`.

### Задача 1.3. Боевой конфиг без облака

- [x] Тест `test_prod_config_loads_without_google_keys` (`tests/test_config.py`):
  `load_config(env="prod")` при пустых `GDRIVE_*` не падает.
- [x] `config/prod.yaml`:

```yaml
storage:
  kind: local                   # Drive — этап M3.6 (решение пользователя)
  directory: ./data/store       # общая копия базы и бэкапы
  work_dir: ./data
  db_filename: listam-prod.sqlite   # не делит рабочую копию с dev
requests:
  kind: csv
  path: ./data/requests.csv
locale:
  timezone: Asia/Yerevan
schedule:
  log_dir: ./data/logs
  keep_logs_days: 14
  task_prefix: listam
  alert_on_failure: true
  alert_every_hours: 6
  linux_xvfb: true
  cycles:
    hourly:  {every_minutes: 60, steps: ["scrape --fresh", "match --new", "notify --hot"]}
    nightly: {at: "04:30", steps: ["scrape", "match --all", "notify --feed"]}   # :30 — см. «Результат фазы 1», п. 3
    evening: {at: "20:30", steps: ["notify --digest"]}
notify:
  feed:
    enabled: true
```

  `dev.yaml` получает те же секции `locale` и `schedule` с `task_prefix: listam-dev`.
- [x] `requirements.txt` + `tzdata`; `.venv/Scripts/pip install tzdata`.

### Задача 1.4. `listam cycle` (решения 3, 4)

Договор модуля:

```python
# listam/schedule.py
@dataclass
class Cycle:
    name: str
    steps: list[str]                 # «scrape --fresh» — аргументы CLI без «python -m listam»
    every_minutes: int | None = None
    at: time | None = None           # местное время locale.timezone

def cycles(config: Config) -> dict[str, Cycle]: ...          # проверка на входе, ConfigError
def run_cycle(config: Config, name: str, *, dispatch=None, notifier=None,
              now=None) -> int: ...                           # код возврата цикла
```

- [x] `test_cycles_are_read_and_checked`: пустой `steps`, неизвестная команда
  шага (`"scarpe"` — разбирается `cli.build_parser()`), `at: "25:00"`, оба
  `every_minutes` и `at`, ни одного — `ConfigError` с именем ключа.
- [x] `test_a_cycle_stops_at_the_first_failing_step`: подменный `dispatch`
  (шаг → код); второй вернул 1 — третий не зван, код цикла 1.
- [x] `test_a_busy_lock_skips_the_cycle_quietly`: замок взят — ни одного
  шага, код 0, строка «пропущен: идёт другой прогон» в логе, тревоги нет.
  Для этого `RunLock.busy() -> str | None` (описание держателя; протухший
  замок — `None`) — тест `test_busy_does_not_take_the_lock` в
  `tests/test_concurrency.py`.
- [x] `test_the_cycle_writes_its_log`: файл `cycle-YYYY-MM-DD.log` в
  `schedule.log_dir`, в нём шапка цикла, вывод каждого шага и код; логи
  старше `keep_logs_days` удаляются.
- [x] `test_a_failed_cycle_alerts_once_per_window`: два падения подряд —
  одна отправка; отметка — файл `last-alert` в `log_dir` (не база: упавший
  шаг мог быть про базу). Отправка тревоги упала — строка в логе, код цикла
  прежний.
- [x] Шаг исполняется в том же процессе: `cli.build_parser().parse_args(step.split())`
  и `cli._dispatch(args, config)`; вывод шага — в лог и в консоль
  (`contextlib.redirect_stdout` в тройник). Флаги `--env`/`--config-dir` цикла
  шаги наследуют через уже загруженный `config`.
- [x] CLI: `listam cycle <имя>`; неизвестное имя — код 2 со списком известных.

### Задача 1.5. `listam schedule show|install|remove`

- [x] Чистые функции, без системы:
  `windows_tasks(config, root, python, env) -> list[Task]` и
  `cron_lines(config, root, python, env) -> list[str]`. `Task` — имя,
  `schtasks`-аргументы и текст `.cmd`-обёртки.
- [x] `test_windows_tasks`: `hourly` → `/sc minute /mo 60`; `evening` →
  `/sc daily /st HH:MM` в **местном времени хоста** (Yerevan 20:00 при
  хосте UTC → `16:00`); `/it`, `/f`; команда задачи — обёртка
  `data/schedule/<prefix>-<cycle>.cmd` (`schtasks /tr` не берёт больше
  261 символа), внутри `cd /d "<root>"` и `"<python>" -m listam --env <env> cycle <имя>`.
- [x] `test_cron_lines`: `0 * * * *`, `0 16 * * *` для хоста в UTC;
  `cd "<root>" && xvfb-run -a "<python>" -m listam --env prod cycle hourly`;
  каждая строка с меткой `# <prefix>:<cycle>`; `linux_xvfb: false` — без
  `xvfb-run`.
- [x] `test_no_path_is_hardcoded`: в `schedule.py`, `config/*.yaml` и README
  нет `C:\` и `/srv/` (корень и python приходят аргументами).
- [x] `install`/`remove` зовут систему через внедрённый `run(cmd)`;
  на Linux читают `crontab -l`, убирают строки своей метки, дописывают новые.
  Тест на подменном `run`. Платформа — `sys.platform`; флаг
  `--platform windows|linux` для `show` (посмотреть чужую); `install`
  на чужой платформе — код 2.

### Задача 1.6. `doctor`: строка «Расписание»

- [x] Тест: последний `fresh` старше 2 ч или последний `full` старше 26 ч —
  `⚠` с датой; прогонов нет — `⚠ по расписанию ещё не работало`. Мерка —
  `runs`, не системный планировщик: так строка одинакова на обеих платформах.

### Задача 1.7. README: расписание

- [x] Раздел «Как запускать по расписанию» — `schedule show` / `install`,
  циклы из конфига, лог, тревога, переезд на Linux тремя командами.
  Примеры с `C:\Users\Admin\…` удалить. `tests/test_docs.py`
  (`test_the_schedule_sends_the_notifications`, `test_readme_names_every_command_the_cli_has`)
  остаются зелёными: `cycle` и `schedule` — в таблице команд.

### Задача 1.8. Запуск (операции, не код)

- [x] `data/requests.csv` ← «Пример заявок» (или реальные, если пришли).
- [x] `PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe -m listam --env prod doctor` —
  без «СБОЙ».
- [x] `--env prod requests`, затем полный `--env prod scrape` (ориентир —
  215 страниц за ~11 минут; окно браузера откроется — это нормально),
  `match --all`, `notify --hot --dry-run`, `notify --digest --dry-run`.
- [ ] Одна живая отправка: `--env prod notify --digest`. Читать в чате
  не нужно (решение 17).
- [ ] `--env prod schedule show`, затем `schedule install`;
  `schtasks /query /tn listam-hourly /v /fo list` — задача есть.
- [ ] Через час с лишним: в `runs` строка `fresh`, в `data/logs` — лог цикла.
- [x] Сказать пользователю про Д-3 (сон) и что заявки-примеры шлют
  уведомления с пометкой «ПРИМЕР».

### Конец фазы 1

Отчёт: батарея, что установлено (`schedule show`), числа первого полного
обхода, первая строка `fresh` из `runs` (с командой), текст сухого дайджеста
(первые строки), долги, дописанные в раздел «Долги».

## Результат фазы 1

Сессия 23.09.2026. Код фазы готов и закоммичен; боевая база заведена чистым
полным обходом. **Две операции 1.8 не исполнены агентом** — их отклонил
классификатор прав Claude Code, и они переданы пользователю (см. «Что осталось»):
живая отправка `notify --digest` и `schedule install`. Поэтому первой строки
`fresh` в `runs` и лога цикла ещё нет.

### Что сделано

| Задача | Итог | Коммит |
| --- | --- | --- |
| 0 | спека и план M3.5 были в дереве, но не в истории — закоммичены | `40a4cdd` |
| 1.1 | календарные даты: одна настоящая бомба, остальные помечены; сторож `test_no_calendar_dates_against_real_clock` | `87d0f4b` |
| 1.2 | пустое «звони сейчас» не шлётся, журнал пишется, окно сдвигается; дайджест «событий нет» уходит | `a80e651` |
| 1.3 | `prod.yaml`: `storage.kind: local` (`./data/store`, `listam-prod.sqlite`), `requests.kind: csv`, `locale`, `schedule`, `notify.feed.enabled: true`; `dev.yaml` — `locale`, `schedule` с `listam-dev`; `tzdata` | `8d74bd3` |
| 1.4 | `listam/schedule.py`: `Cycle`, `cycles`, `run_cycle`; `RunLock.busy()`; CLI `cycle <имя>` | `d3bdc01` |
| 1.5 | `windows_tasks`, `cron_lines`, `install`, `remove`, `show`; CLI `schedule show\|install\|remove [--platform]` | `d3bdc01` |
| 1.6 | `doctor`: строка «Расписание» по журналу `runs` | `d3bdc01` |
| 1.7 | README: раздел «Как запускать по расписанию» переписан, `C:\Users\Admin\…` и `/srv/` удалены, команды `cycle`/`schedule` в таблице | `d3bdc01` |
| — | цикл без консоли (`sys.stdout is None`) пишет лог, а не падает | `479b5f3` |
| — | **парсер: значок агентства по слову, а не по классу** (находка боевого обхода, ниже) | `ff8a996` |

### Числа (каждое — с командой)

* Батарея до фазы: `.venv/Scripts/python.exe -m pytest -q` → **824 passed, 25 skipped**.
* Батарея после фазы: та же команда → **888 passed, 25 skipped** (46,2 с).
  План ждал ~850: тестов фазы больше, чем в плане (параметризованные проверки
  циклов, 12 случаев `test_cycles_are_read_and_checked`, установщик на подмене).
* Бомбы календаря: плагин pytest, сдвигающий `datetime.now()` кода и тестов
  на +1/+3/+30 суток (скрипт вне репозитория, `-p shiftclock`), до правки дал
  падение `tests/test_matching.py::test_new_sees_the_one_that_got_cheaper` на
  всех трёх сдвигах; остальные падения — артефакты подмены (openpyxl не узнаёт
  подкласс `datetime`, локальный `from datetime import` в `test_crawler`).
  После правки под сдвигом +30 сут `tests/test_matching.py tests/test_events.py
  tests/test_notifications.py` → 89 passed.
* `PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe -m listam --env prod doctor` →
  без «СБОЙ»; ⚠ только «Расписание: по расписанию ещё не работало».
* `… --env prod requests` → «Заявки: новых 3, обновлённых 0, без изменений 0,
  закрытых 0» (пример заявок R-1…R-3).
* Первый `… --env prod scrape` → «страниц: 216, карточек: 20684, новых: 20672,
  … ошибок: 1»; «seller_type = agency у 0% карточек прогона, порог … 5%»;
  база не залита. Причина и починка — ниже; брак отложен в scratchpad, в
  хранилище не попал.
* Второй `… --env prod scrape` (после починки) → **«Прогон (full): страниц: 216,
  карточек: 20685, новых: 20664, обновлённых: 0, сменили цену: 0, снято: 0,
  вернулось: 0, ошибок: 0»**, «конец ленты: пагинатор не дал следующей
  страницы», код 0; заняло 9 мин 25 с (`runs.started_at` 13:02:26 →
  `finished_at` 13:11:51 UTC).
* `runs` боевой базы (`python -c "…select … from runs"` по
  `data/listam-prod.sqlite`): `(1, 2026-09-23T13:02:26Z, 2026-09-23T13:11:51Z,
  full, 216, 20685, 20664, 0)`. Строки `fresh` нет — задачи не поставлены.
* Продавцы (`select seller_type, count(*) from listings group by 1`):
  agency 16 558, owner 4 106.
* `… --env prod match --all` → «Заявок: 3, объявлений в выборке: 20644, из них
  представителей кластеров: 14395; Матчи: новых 3322 …; Из них горячих: 2673,
  в дайджест: 627», база залита, код 0.
* `… --env prod notify --hot --dry-run` → «Событий: 2673, заявок: 3»; первые
  строки: «Звони сейчас: отправок ещё не было — беру последние 2 ч»,
  «Заявка R-1 (ПРИМЕР узкая) — новый: 23», верхний вариант «100 баллов
  $109,900 1,465 $/м² Арабкир … https://www.list.am/ru/item/24123988».
* `… --env prod notify --digest --dry-run` → «Событий: 3300, заявок: 3»; первые
  строки: «Что нового со вчера: отправок ещё не было — беру последние 24 ч»,
  «Заявка R-1 (ПРИМЕР узкая) — новый: 25», «⚠ заявка слишком широкая: 25
  событий за окно». Всё это — первичная подборка (решение 14, фаза 4),
  **предварительно**, на примере (Д-1).
* `… --env prod schedule show` на этой машине (часы хоста — Ереван):
  `listam-hourly` — `/sc minute /mo 60 /st 00:00 /it /f`;
  `listam-nightly` — `/sc daily /st 04:30`; `listam-evening` — `/sc daily
  /st 20:30`; команда каждой задачи — обёртка
  `data/schedule/listam-<цикл>.cmd` с `cd /d` в корень и
  `"<.venv>\python.exe" -m listam --env prod cycle <цикл>`.

### Что разошлось с планом и почему

1. **Спека и план не были закоммичены** (исходное состояние обещало HEAD со
   спекой). Закоммичены первым коммитом сессии.
2. **Бомба календаря — одна, а не восемь.** План назвал все даты
   `datetime(2026, 9, 23)` в `test_matching.py` и `test_events.py` бомбами;
   сдвиг часов показал, что с `now()` сравнивается только окно `--new` без
   прогонов (`fallback_hours` от настоящих часов). Остальные — помечены.
3. **Циклы «в ЧЧ:ММ» — 04:30 и 20:30, а не 04:00 и 20:00.** Часовой идёт
   ровно в начале часа (`0 * * * *`, `/st 00:00`), и в 04:00 и 20:00 два цикла
   стартовали бы разом: кто первым взял замок, тот прошёл, второй — тихий
   пропуск (решение 4). Пропущенный ночной — сутки без полного обхода,
   пропущенный вечерний — день без дайджеста. Сторож —
   `test_the_shipped_fixed_cycles_do_not_start_with_a_periodic_one`.
4. **`/st 00:00` у часовой задачи Windows** — в плане не было. Без него задача
   идёт с минуты установки, и Windows с Linux расходились бы по минутам.
5. **`test_the_schedule_sends_the_notifications` переписан**, а не «остался
   зелёным»: он требовал шаги прямо в строке `schtasks`/`cron`, а по решению 3
   задача зовёт `cycle <имя>`, шаги живут в конфиге. Теперь тест проверяет
   поставляемые `schedule.cycles` обоих конфигов (`notify --hot` после
   `match --new` в периодическом, `notify --digest` — в суточном) и выдержку
   конфига и команды `schedule` в README.
6. **Парсер ленты правлен в фазе 1** (`listam/parsers/category_list.py`, не
   `crawler.py`). Первый боевой обход записал всех продавцов собственниками:
   list.am 23.09 переименовал обфусцированный класс значка `ge3` → `ge4`.
   Проверка доли агентств (coverage) сработала как задумано — остановила
   заливку. Значок теперь ищется по слову «Агентство» в `span` карточки.
   Для диагноза инструмент своим `Fetcher` (`build_fetcher(prod)`) взял одну
   страницу `/category/60` в scratchpad — вне репозитория.
7. **Проверки периода.** `every_minutes` принимается только такой, который
   одинаково понимают `schtasks` и `cron` (делитель часа или целые часы,
   делящие сутки); шаг не может начинаться с флага и звать `cycle`/`schedule`.
   В плане этого не было — это «бессмысленное значение отклоняется на входе».
8. **Живая отправка и `schedule install` не исполнены агентом** — отклонены
   классификатором прав («Real-World Transactions», «Unauthorized
   Persistence»). Переданы пользователю.

### Находки для следующих фаз

* **Первый «звони сейчас» после установки будет большим.** В окне
   `fallback_hours: 2` лежат 2 673 горячих события первичной подборки трёх
   заявок-примеров: три сообщения по 5 строк и хвост «…и ещё 2268 из 2273».
   Это ровно проблема решения 14 (фаза 4); до неё — ожидаемо.
* **dev и prod делят `./data/requests.csv`.** Пример заявок теперь виден и
   dev-прогонам. Базы и замки у них разные (`listam.sqlite` / `listam-prod.sqlite`).
* **Окно `cmd` мелькает раз в час** на рабочем столе: задача с `/it` и
   консольный `python.exe`. Обход всё равно открывает окно браузера.

### Что осталось (пользователю)

```
! PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe -m listam --env prod notify --digest
! PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe -m listam --env prod schedule install
! schtasks /query /tn listam-hourly /v /fo list
```

Через час с лишним: `doctor` — строка «Расписание» без ⚠ по часовому; в
`runs` — строка `fresh`; в `data/logs` — `cycle-ГГГГ-ММ-ДД.log`. Эти три
проверки — первое, что делает фаза 2 (стартовый промпт ниже).

## Стартовый промпт фазы 2

```
Ты продолжаешь этап M3.5 инструмента мониторинга list.am
в C:\Users\Artur.A.Gevorgyan\Downloads\new_project.

Прочитай docs/superpowers/plans/2026-09-23-m3-5-live-launch.md: разделы
«Global Constraints», «Карта файлов», «Долги», «Результат фазы 1» и свою
«Фазу 2». Чужие фазы не трогай. Спека —
docs/superpowers/specs/2026-09-23-m3-5-live-launch-design.md, решения 1–18
не пересматриваются.

Исходное состояние: HEAD — коммит «Результат фазы 1», дерево чистое,
батарея 888 passed, 25 skipped, схема 11. Боевая база — data/listam-prod.sqlite
(хранилище — data/store; замеры — только на копии), заявки — пример R-1…R-3.
Первым делом проверь, поставил ли пользователь расписание (см. «Что осталось»
в «Результате фазы 1»): schtasks /query /tn listam-hourly, строка fresh
в runs, лог в data/logs, строка «Расписание» в doctor. Не поставил —
спроси, не ставь сам. Расписание установлено — не останавливай его без
нужды, а если остановил — верни и напиши об этом.

Твоя задача — фаза 2: вёрстка Telegram строго по пункту 8.
Сообщение становится структурой (layout.Message), порт Notifier принимает
её целиком, Telegram получает HTML с карточками из трёх строк и режется
только между карточками; --dry-run и журнал — простой текст той же структуры.
Тревога цикла (schedule.py) тоже шлёт Message.

Работай по шагам: на каждое поведение — падающий тест ДО правки. Тесты —
только .venv/Scripts/python.exe -m pytest -q, CLI из скрипта — только
с PYTHONIOENCODING=utf-8. Пороги не поднимай (кроме фазы 4 с замером).
Новый метод порта — новый контрактный тест. Схему меняет только
миграция 012 (фаза 3). След звонка пишет только mark, MATCH_COMPARED
не расширяется. Живые проверки Playwright не запускай — впиши в «Долги».
Сам на list.am не ходи. Ни одного числа без команды, которая его напечатала.

В конце сессии допиши «Результат фазы 2» и стартовый промпт фазы 3.
Сделай коммит.
```

---

# Фаза 2. Вёрстка Telegram по пункту 8

**Смысл:** брокер за секунду понимает, кому звонить. Порт уведомления
принимает структуру, а не строку.

**Ожидается после фазы:** ~880 passed, 25 skipped, схема 11.

### Задача 2.1. Модель сообщения

```python
# listam/layout.py
@dataclass
class Span:
    text: str
    bold: bool = False
    href: str | None = None

Line = list[Span]

@dataclass
class Section:              # одна заявка = одно сообщение Telegram
    head: list[Line]        # шапка: повторяется на продолжении с « (продолжение)»
    cards: list[list[Line]] # карточки по три строки; резать можно только между ними
    tail: list[Line] = field(default_factory=list)

@dataclass
class Message:
    sections: list[Section]

def to_html(section: Section) -> str: ...   # экранирует <, >, &; <b>, <a href>
def to_plain(section: Section) -> str: ...  # без тегов; ссылка — «Открыть: <url>»
def plain(message: Message) -> str: ...     # для --dry-run и журнала
```

- [x] `test_html_escapes_listing_text`: улица `ул. <Раффи> & Co` → `&lt;`, `&amp;`.
- [x] `test_plain_has_no_tags`; `test_link_is_a_word_not_a_url` (HTML: `<a href="…">Открыть</a>`).

### Задача 2.2. Карточка, шапка, три вида сообщения

- [x] Карточка события (`card(event, median, config)`), строго три строки:
  1. значок события (🆕 / 📉 / ♻️) · **цена** · площадь · `$/м² (−12% к району)`;
     у 📉 — «(было $171 000)»; у снятого — « · ❌ снято»;
  2. 📍 район, улица · этаж N/M;
  3. 👤 Собственник / 🏢 Агентство · «3 объявления, разброс $8 000» (если кластер > 1) ·
     🟢/🟡/⚪ балл N · 🔗 Открыть.
  Тесты: `test_card_has_three_lines`, `test_cheaper_card_names_the_old_price`,
  `test_score_circle_follows_config` (🟢 ≥ `notify.layout.score_green`, 🟡 ≥
  `match.thresholds.hot`, иначе ⚪), `test_percent_to_district_median` (знак `−`/`+`,
  медианы нет — скобок нет).
- [x] Шапка заявки: `🔥 ЗВОНИ СЕЙЧАС · R-4 · Клиент 4`, строка заявки
  `Канакер-Зейтун · 2 комн. · 80–110 м² · до $175 000`, `━━━━━━━━━━━━━━━`.
  Незаданное в заявке не печатается.
- [x] Хвост «горячего»: `➕ ещё 12 вариантов — в дайджесте вечером`. Никаких
  `python -m listam` в тексте сообщения (`test_no_cli_hints_in_messages`).
- [x] Дайджест: первым сообщением сводка (`📋 ДАЙДЖЕСТ · 23.09 · 20:00` в
  `locale.timezone`; `🆕 новых — N   📉 подешевело — N   ♻️ вернулось — N`;
  `👥 заявок с находками — N из M`; строка на заявку с её счётчиками,
  `⚠️ слишком широкая`, `❌ N` для закрытий). Затем по сообщению на заявку
  со звонками. Заявка только с закрытиями своего сообщения не получает —
  `test_retired_only_request_is_a_summary_line_not_a_message` (решение 8).
- [x] Лента: `🗞 НА ЛЕНТЕ ЗА СУТКИ`, `🆕 N новых · 📉 N подешевели · ❌ N снято`,
  «Самые выгодные против медианы района:», строки `💎` только у тех, кто
  дешевле медианы на `notify.feed.gem_percent` и больше;
  `test_feed_gem_only_below_threshold`.

### Задача 2.3. Порт `Notifier` принимает `Message`

- [x] Контракт `tests/contracts/test_notifier_contract.py` переписать:
  `send(Message)` у `none`, `stdout`, `telegram` (последний — под `skipif`
  без токена, как было).
- [x] `stdout` печатает `plain`. `telegram`: каждая `Section` → `to_html`;
  длиннее 4 096 — режется **между карточками**, продолжение начинается
  с шапки + « (продолжение)»; `parse_mode: "HTML"`,
  `link_preview_options: {"is_disabled": true}`. Прежний `split_message`
  по пустым строкам удаляется вместе со своими тестами — заменяется
  `test_split_only_between_cards`, `test_continuation_repeats_the_head`,
  `test_no_part_exceeds_the_limit`.
- [x] Тревога цикла (`schedule.py`) шлёт `Message` из одной секции.

### Задача 2.4. `notify` на новой вёрстке

- [x] `run_notify` собирает `Message` из той же выборки (`collect_events`,
  `_feed_text` → `feed_message`). Журнал `notifications.text` и
  `--dry-run` — `plain(message)`: `test_dry_run_equals_journal_text`.
- [x] Витрина `matches --new` остаётся терминальной (моноширинная таблица
  для терминала уместна): выборка общая, подача разная (решение 10 спеки M3
  в силе).
- [x] README, «Уведомления и дайджест»: примеры сообщений из пункта 8,
  словарь значков.

### Задача 2.5. Живой канал

- [x] `--env prod notify --digest --dry-run` — посмотреть глазами простой текст.
- [x] Следующий часовой цикл сам уйдёт в новой вёрстке. Отдельной отправки
  не нужно; проверки Т-1…Т-3 уже в «Долгах».

## Результат фазы 2

Сессия 23.09.2026. Вёрстка по пункту 8 готова и закоммичена. Живой отправки
не было: расписание не поставлено (Д-6), по указанию пользователя это не
блокер — первая отправка уйдёт уже в новой вёрстке.

### Проверка расписания (первым делом)

* `schtasks /query /tn listam-hourly /v /fo list` → «ERROR: The system cannot
  find the file specified.»
* `runs` боевой базы (`python -c "…select id,started_at,finished_at,mode from
  runs"`) → одна строка `(1, 2026-09-23T13:02:26…, 2026-09-23T13:11:51…, full)`;
  строки `fresh` нет.
* `ls data/logs` → «No such file or directory»; журнал `notifications` —
  `(0, None)`.
* Спросил пользователя; ответ — «не блокер — в долги». Д-6 дополнен.

### Что сделано

| Задача | Итог |
| --- | --- |
| 2.1 | `listam/layout.py`: `Span`, `Line`, `Section`, `Message`, `to_html` (экранирует `<` `>` `&`, `<b>`, `<a href>`), `to_plain` («Открыть: <url>»), `plain`, `text_message` |
| 2.2 | `card` — три строки; `request_head` (незаданное не печатается); `hot_message` (хвост «➕ ещё N — в дайджесте вечером»), `digest_message` (сводка + сообщение на заявку со звонками; закрытия — строкой «❌ N (причины)» в сводке), `feed_message` (💎 от `notify.feed.gem_percent`) |
| 2.3 | порт `Notifier.send(message: Message, to=None)`; `stdout` печатает `plain`; Telegram — `split_section` (резка только между карточками, продолжение с шапкой « (продолжение)», карточка длиннее лимита — по строкам), `parse_mode: HTML`, `link_preview_options.is_disabled`; `split_message` удалён вместе с тестами; тревога цикла — `text_message` |
| 2.4 | `run_notify` собирает `Message`; `report.text` и журнал — `plain(message)`; `_match_text`/`_feed_text` → `_match_message`/`_feed_message`; пороги вёрстки (`score_green`, `gem_percent`) проверяются в `tuning_for` до замка; витрина `matches --new` не тронута; README: примеры, словарь значков, `layout.py` в «Устройстве» |
| 2.5 | боевые `--dry-run` всех трёх видов просмотрены (ниже) |
| — | `config/prod.yaml`, `config/dev.yaml`: `notify.layout.score_green: 80`, `notify.feed.gem_percent: 15` |
| — | `tests/live/test_telegram_live.py` переведён на `Message`: `test_a_card_arrives_as_html`, `test_long_message_is_cut_between_cards_with_the_head` (не запускались — Д-2) |

### Числа (каждое — с командой)

* Батарея до фазы: `.venv/Scripts/python.exe -m pytest -q` → **888 passed,
  25 skipped** (исходное состояние промпта; первым прогоном сессии не
  перепроверялось).
* Батарея после фазы: та же команда → **934 passed, 27 skipped** (45,3 с).
  План ждал ~880 — он считал от 824, а фаза 1 закончилась на 888. Прирост
  фазы: +46 passed (34 в `tests/test_layout.py` — `pytest --collect-only
  tests/test_layout.py` → «34 tests collected»; остальное — адаптер, контракт,
  уведомления, расписание), +2 skipped (контракт `telegram` с карточками и
  новый живой тест).
* Схема боевой базы — `select max(version) from schema_version` → `(11,)`.
* `PYTHONIOENCODING=utf-8 … --env prod notify --digest --dry-run` → код 0,
  152 строки; первые: «📋 ДАЙДЖЕСТ · 23.09 · 17:32» (13:32 UTC в
  `Asia/Yerevan`), «🆕 новых — 3300   📉 подешевело — 0   ♻️ вернулось — 0»,
  «👥 заявок с находками — 3 из 3», «R-1 · ПРИМЕР узкая — 🆕 25  ⚠️ слишком
  широкая», «R-2 … — 🆕 444 …», «R-3 … — 🆕 2831 …»; карточка R-1:
  «🆕 $109 900 · 75 м² · $1 465/м² (−43% к району)» / «📍 Арабкир · этаж 2/5» /
  «👤 Собственник · 🟢 балл 100 · 🔗 Открыть: https://www.list.am/ru/item/24123988»;
  последняя строка сообщения — «➕ ещё 2821 вариант ниже по баллу»; сводка
  CLI — «Событий: 3300, заявок: 3, не отправлено (пробный прогон)».
* `… notify --hot --dry-run` → код 0; «🔥 ЗВОНИ СЕЙЧАС · R-1 · ПРИМЕР узкая»,
  «Арабкир · 3 комн. · 70–95 м² · до $120 000», хвост «➕ ещё 2268 вариантов —
  в дайджесте вечером», «Событий: 2673, заявок: 3».
* `… notify --feed --dry-run` → код 0; «🆕 20664 новых · 📉 0 подешевели · ❌ 0
  снято» (первое окно ленты — весь первичный обход), «➕ ещё 5342 заметно
  дешевле медианы» (до отсева аномалий).

### Что разошлось с планом и почему

1. **Строка заявки в сводке дайджеста — через « — », а не точками.** В
   примере пункта 8 «R-4 · Клиент 4 ........ 🆕 3  📉 1», но правило того же
   пункта — «никакого выравнивания пробелами»: точки-заполнитель в
   пропорциональном шрифте Telegram разваливаются так же. Сделано
   «R-4 · Клиент 4 — 🆕 3  📉 1».
2. **Закрытия в сводке — с причинами:** «❌ 2 (бюджет 1, район 1)», а не
   голое «❌ 2». Причины уже печатались до фазы (прежний «отпало 2 (…)»), и
   брокеру они отвечают, сужать ли заявку. Значков не прибавилось.
3. **💎 не получает помеченное аномалией.** Боевой `--dry-run` ленты первыми
   показал «$190 · 200 м² · $1/м² (−100%)» и «$100 · 131 м²» — строки с
   `anomaly = price_usd,price_per_sqm` (проверено `select … from listings`).
   Медиана их уже не видит (`stats.clean`); теперь не видит и 💎. Тест
   `test_feed_gem_skips_anomalies`. Неотмеченные выбросы площади — Д-7.
4. **«444 событий» → «444 события».** Боевой `--dry-run` показал ошибку
   согласования в пометке широкой заявки (была и до фазы); тест
   `test_the_wide_mark_agrees_with_its_number`.
5. **Ссылка 💎 — словом «🔗 Открыть»**, а не голым «🔗» примера: правило
   пункта 8 «ссылка — словом „Открыть“».
6. **Карточки из одной строки идут подряд** (💎 ленты), карточки из трёх —
   через пустую строку. Модель сообщения не менялась, это правило рендера.
7. **Пороги вёрстки проверяются на входе:** `score_green: 170` или
   `gem_percent: -5` — код 2 до замка (`test_a_senseless_layout_setting_…`),
   как остальной `notify`. В плане этого не было.
8. **Контракт порта получил два теста, а не переписан один:**
   `test_sends_a_message_with_cards` (структура с `<`, `&`, жирным и ссылкой)
   и `test_stdout_prints_the_plain_text`.
9. **`schedule.py` импортирует `layout`**, а `layout.digest_message` берёт
   `schedule.local_zone` лениво — иначе круговой импорт.

### Находки для следующих фаз

* **Медиана района считается на каждую отправку** —
  `listings_for_matching()` по всей базе (≈20 тыс. строк) раз в час. На
  боевых `--dry-run` это незаметно, но это второе место после подбора, где
  медиана считается заново.
* **💎 ленты и «−43% к району» у R-1** — медианы районов берутся по всей
  ленте, включая выбросы площади, которые проверка не метит (Д-7). Процент к
  району честен ровно настолько, насколько честна медиана.
* **Первый дайджест после установки расписания — 3 сообщения по 10 карточек
  и сводка**, первый «звони сейчас» — 3 по 5: это первичная подборка трёх
  заявок-примеров (решение 14, фаза 4).

### Что осталось (пользователю)

Те же команды, что после фазы 1 (Д-6), — теперь в новой вёрстке:

```
! PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe -m listam --env prod notify --digest
! PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe -m listam --env prod schedule install
! schtasks /query /tn listam-hourly /v /fo list
```

И живые проверки Т-1…Т-3, Т-6 (Д-2).

## Стартовый промпт фазы 3

```
Ты продолжаешь этап M3.5 инструмента мониторинга list.am
в C:\Users\Artur.A.Gevorgyan\Downloads\new_project.

Прочитай docs/superpowers/plans/2026-09-23-m3-5-live-launch.md: разделы
«Global Constraints», «Карта файлов», «Долги», «Результат фазы 2» и свою
«Фазу 3». Чужие фазы не трогай. Спека —
docs/superpowers/specs/2026-09-23-m3-5-live-launch-design.md, решения 1–18
не пересматриваются.

Исходное состояние: HEAD — коммит «Результат фазы 2», дерево чистое,
батарея 934 passed, 27 skipped, схема 11. Боевая база — data/listam-prod.sqlite
(хранилище — data/store; замеры — только на копии), заявки — пример R-1…R-3.
Первым делом проверь, поставил ли пользователь расписание (долг Д-6):
schtasks /query /tn listam-hourly, строка fresh в runs, лог в data/logs,
строка «Расписание» в doctor. Не поставил — спроси, не ставь сам.
Расписание установлено — не останавливай его без нужды, а если остановил —
верни и напиши об этом.

Твоя задача — фаза 3: двухступенчатая воронка.
must_have и nice_to_have начинают работать: миграция 012 (listing_pages,
request_exclusions, matches.origin), разбор страницы объявления, словарь
пожеланий, фактор wishes, шаг pages, открывающий страницу только кандидату
под живую заявку с паузой и потолком, и подбор со страницами. Сначала
разведка 3.1: 2–3 настоящие страницы, снятые инструментом своим Fetcher.

Работай по шагам: на каждое поведение — падающий тест ДО правки. Тесты —
только .venv/Scripts/python.exe -m pytest -q, CLI из скрипта — только
с PYTHONIOENCODING=utf-8. Пороги не поднимай (кроме фазы 4 с замером).
Новый метод порта — новый контрактный тест. Схему меняет только
миграция 012. След звонка пишет только mark, MATCH_COMPARED
не расширяется. Живые проверки Playwright не запускай — впиши в «Долги».
Сам на list.am не ходи. Ни одного числа без команды, которая его напечатала.

В конце сессии допиши «Результат фазы 3» и стартовый промпт фазы 4.
Сделай коммит.
```

---

# Фаза 3. Двухступенчатая воронка

**Смысл:** `must_have` и `nice_to_have` начинают работать; страница
объявления открывается только для кандидатов.

**Ожидается после фазы:** ~940 passed, 25 skipped, схема **12**.

### Задача 3.1. Разведка (без кода в `listam/`)

- [x] Скрипт `tmp/recon_item.py` (вне репозитория): `build_fetcher(config)`
  боевого конфига, `get("https://www.list.am/robots.txt")`. Если для `*`
  запрещён `/item/` или `/ru/item/` — **стоп, спросить пользователя**.
- [x] Тем же `Fetcher` — 3 страницы разных домов (камень, панель, новостройка;
  id — из базы, пауза `funnel.delay_seconds`). Сохранить в
  `tests/fixtures/item-<id>.html`. Телефона на странице нет (он за кнопкой);
  если на странице есть имя продавца — заменить в фикстуре на «Продавец».
- [x] Выписать в отчёт фазы: все подписи характеристик на странице (как
  на сайте, по-русски), значения, где лежат описание и фото. Сверить
  предварительный словарь спеки (`renovation`, `building_type`, `balcony`,
  `elevator`, `ceiling_height`) и поправить **имена и значения** в спеке
  и конфиге под то, что реально есть. Это единственное место фазы, где
  правится спека.

### Задача 3.2. Миграция 012 и модели

```sql
-- listam/migrations/012_funnel.sql
-- Версия 12: воронка и обратная связь.
-- listing_pages — кэш открытой страницы: разобранные поля, не HTML.
--   price_raw — цена карточки в момент открытия: сменилась — открыть снова.
-- request_exclusions — отказы клиента, сузившие заявку. Таблицу заявок
--   ведёт брокер, синхронизация её не перезаписывает — поэтому здесь.
-- matches.origin — market | request: кто родил матч. «Звони сейчас» — только market.
CREATE TABLE IF NOT EXISTS listing_pages (
    listing_id      TEXT PRIMARY KEY,
    fetched_at      TEXT,
    status          TEXT NOT NULL,
    attempts        INTEGER NOT NULL DEFAULT 0,
    price_raw       TEXT,
    fields          TEXT,
    parser_version  INTEGER NOT NULL DEFAULT 1,
    error           TEXT
);
CREATE TABLE IF NOT EXISTS request_exclusions (
    id          INTEGER PRIMARY KEY,
    request_id  INTEGER NOT NULL,
    kind        TEXT NOT NULL,
    value       TEXT,
    reason      TEXT,
    match_id    INTEGER,
    created_at  TEXT NOT NULL
);
CREATE INDEX IF NOT EXISTS idx_exclusions_request ON request_exclusions(request_id);
ALTER TABLE matches ADD COLUMN origin TEXT;
```

```python
# listam/domain/models.py
@dataclass
class PageFields:
    values: dict[str, object] = field(default_factory=dict)  # renovation → "euro", balcony → True
    description: str | None = None
    photos: list[str] = field(default_factory=list)           # ссылки; для M4

@dataclass
class ListingPage:
    listing_id: str
    status: str                      # ok | gone | failed
    fetched_at: datetime | None = None
    attempts: int = 0
    price_raw: str | None = None
    fields: PageFields | None = None
    error: str | None = None

# Match: origin: str | None = None  # market | request; не в MATCH_COMPARED
```

- [x] `tests/test_migrations.py`: 11 → 12 на копии, строки не теряются,
  `origin IS NULL` у старых матчей.

### Задача 3.3. Порт: кэш страниц

```python
# listam/ports/database.py
@abstractmethod
def get_page(self, listing_id: str) -> ListingPage | None: ...
@abstractmethod
def save_page(self, page: ListingPage) -> None:
    """Апсерт по listing_id. attempts считает вызывающий."""
@abstractmethod
def pages_for(self, listing_ids: Iterable[str]) -> dict[str, ListingPage]: ...
@abstractmethod
def listings_paged_since(self, since: datetime) -> set[str]:
    """Чья страница открыта (status ok) после отметки: им пора в подбор."""
```

- [x] Контрактные тесты на каждый метод; `listings_touched_since` включает
  `listings_paged_since` — `test_a_listing_whose_page_just_opened_is_touched`.
- [x] `upsert_matches` пишет `origin` только при вставке:
  `test_origin_is_written_once_and_not_compared`.

### Задача 3.4. Разбор страницы

- [x] `parse_item_page(html) -> PageFields`; снятое объявление —
  `ItemGone`. Тесты на трёх фикстурах: каждое поле из отчёта 3.1,
  `test_unknown_label_is_reported_not_fatal` (подпись, которой нет в разборе,
  уходит в `fields.values["_unknown"]` списком — прогон её считает).
  Парсер не падает из-за отсутствия любого поля (правило M0).

### Задача 3.5. Словарь пожеланий (решения 10, 11)

```python
# listam/domain/wishes.py
@dataclass
class Wish:
    word: str
    field: str
    any_of: list | None = None
    none_of: list | None = None
    is_: bool | None = None
    min: float | None = None

    def check(self, fields: PageFields | None) -> bool | None:
        """True/False — известно; None — поля нет или страница не открыта."""

def vocabulary(config: Config) -> dict[str, Wish]: ...     # ConfigError на кривом словаре
def parse_wishes(text: str | None, vocab) -> tuple[list[Wish], list[str]]:
    """Пожелания и неизвестные слова. Разделитель — запятая; регистр и пробелы не важны."""
```

- [x] Тесты: `check` на всех видах условий; `parse_wishes("Ремонт, балкон ,лифт")`;
  неизвестное слово возвращается, а не глотается.
- [x] `requests_sync`: неизвестное слово в `must_have` — отказ строки
  (`RequestError` с колонкой и словом); в `nice_to_have` — строка
  `⚠ nice_to_have: слово «…» не из словаря funnel.wishes — не учитывается`.

### Задача 3.6. Скоринг

- [x] `score(request, listing, *, page=None, must=(), nice=(), …)`.
- [x] `rejection`: `must` непусто и `page is None` → `"страница не открыта"`;
  `Wish.check is False` → подпись поля («ремонт», «тип дома»); `None` на
  открытой странице — не отказ.
- [x] Фактор `wishes` в `DEFAULT_WEIGHTS` (вес 15 — предварительно,
  фаза 4 уточняет) и в обоих конфигах; `test_wishes_factor_counts_only_known_fields`,
  `test_no_wishes_no_factor` (фактор уходит из знаменателя).
- [x] Тесты «названы все факторы» и README («шесть факторов») — на семь.

### Задача 3.7. Шаг `pages`

- [x] `listam/pages.py`, `run_pages(config, *, max_opens=None, dry_run=False, keep_html=False)`
  на каркасе `working_session`:
  1. заявки, у которых `must` или `nice` непусты;
  2. представители кластеров, прошедшие `rejection` **без** страничных условий
     и с грубым баллом ≥ `match.thresholds.digest`;
  3. минус те, у кого страница свежая (решение 13), `gone` или исчерпала
     `max_attempts`;
  4. порядок — грубый балл по убыванию, при равенстве — новее `first_seen`;
  5. первые `funnel.max_opens_per_run`, пауза `funnel.delay_seconds`
     (`build_fetcher(config, delay_seconds=…)` — новый необязательный
     аргумент `wiring.build_fetcher`).
- [x] Тесты на `FilesFetcher` с фикстурами: `test_only_candidates_are_opened`,
  `test_the_ceiling_is_respected`, `test_a_fresh_page_is_not_reopened`,
  `test_a_price_change_reopens_the_page`, `test_gone_page_is_never_reopened`,
  `test_failed_page_counts_attempts`, `test_request_without_wishes_opens_nothing`,
  `test_dry_run_opens_nothing`.
- [x] Отчёт: кандидатов N, открыто K (потолок P), из кэша M, снято, сбоев,
  неразобранных подписей.
- [x] CLI `listam pages [--max N] [--dry-run] [--keep-html]`; `--max` больше
  потолка — код 2 с именем ключа.

### Задача 3.8. Подбор со страницами

- [x] `run_match` читает `pages_for(ids кандидатов)` одним запросом и отдаёт
  `score` страницу; пожелания заявки — `parse_wishes` по словарю.
- [x] Отказ «страница не открыта» не пишется в матчи и **не закрывает**
  старый матч — `test_unopened_page_does_not_retire` (решение 12).
- [x] `origin`: `request`, если заявка новая или правленая (`_is_edited`),
  иначе `market` — `test_new_request_matches_are_born_as_request`.
- [x] Циклы в обоих конфигах: `pages` после `scrape` и перед `match`.

### Задача 3.9. `doctor` «Воронка» и живой шаг

- [x] `doctor`: кандидатов в очереди (дёшево: без сети), страниц в кэше,
  доля `failed`.
- [x] `--env prod pages --max 5` — один раз, вживую. Отчёт с числами.
  Дальше страницы открывает часовой цикл.

---

## Результат фазы 3

Сессия 23.09.2026. Воронка работает: `must_have` и `nice_to_have` решают,
есть ли матч и какой у него балл; шаг `pages` один раз прошёл вживую.

### Проверка расписания (первым делом)

* `schtasks /query /tn listam-hourly /v /fo list` → «ERROR: The system cannot
  find the file specified.»
* `runs` боевой базы → одна строка `(1, 2026-09-23T13:02:26…, 2026-09-23T13:11:51…, full)`;
  `fresh` нет; `ls data/logs` → «Cannot find path»; `notifications` → `(0, None)`.
* `--env prod doctor` → «⚠ Расписание — часовой: ещё не было; ночной: 23.09
  17:02 (Asia/Yerevan)».
* Спросил пользователя; ответ — «не блокер, оставим в долгах». Д-6 дополнен.
  Расписание не ставил и не трогал.

### Разведка 3.1

`tmp/recon_item.py` (в папке сессии, вне репозитория): `build_fetcher` боевого
конфига (Playwright), 4 запроса — `requests_made` → 4.

* `robots.txt` (3 883 символа): для `User-agent: *` — `Allow: /` и список
  `Disallow` (`/am/*`, `/add*`, `/edit*`, query-фильтры `*category*…=*`…);
  `/item/` и `/ru/item/` **не запрещены**.
* Страницы: `23814256` (новостройка, Кентрон — оказалась «Монолит»),
  `24032612` («Каменное», Арабкир), `24041732` («Панельное», Нор Норк) →
  `tests/fixtures/item-<id>.html`. Имя продавца заменено на «Продавец»:
  2 + 1 + 2 вхождения (в `.username .nmsp` и в JSON-LD `LocalBusiness.name`).
* Вёрстка: характеристики — блоки `div.attr`, пункт — `div.at3`, над блоком —
  заголовок `div.gt`. **В верхнем блоке значение стоит над подписью, в
  остальных — под ней**, поэтому подпись узнаётся по словарю, а не по месту.
  Пункт из одной строки — признак, серый (`disabled`) = «нет».
* Подписи (как на сайте) → поле разбора:

  | Блок | Подпись | Поле | Значения на трёх страницах |
  | --- | --- | --- | --- |
  | верхний | Общая площадь | `area` | «113 кв.м.», «85 кв.м.», «80 кв.м.» |
  | верхний | Этаж / Этажей в доме | `floor` / `floors_total` | 8/12, 3/5, 2/9 |
  | верхний | Высота потолков | `ceiling_height` | «3 м», «2.8 м», «2.7 м» |
  | верхний | Количество комнат / санузлов | `rooms` / `bathrooms` | 3/2, 2/1, 3/1 |
  | О здании | Тип здания | `building_type` | Монолит, Каменное, Панельное |
  | О здании | Новостройка | `new_build` | Да, Нет, Нет |
  | О здании | Лифт | `elevator` | Есть, Нет, Есть |
  | О здании | Домофон, Консьерж, Детская площадка | `intercom`, `concierge`, `playground` | признаки (все серые) |
  | Парковка | Открытая, Закрытая, Гараж | `parking_outdoor`, `parking_covered`, `garage` | только у новостройки |
  | Виды из окон | Вид на двор / улицу / город / парк / Арарат | `view_*` | признаки |
  | О квартире | Балкон | `balcony` + `balcony_type` | Открытый, Открытый, Закрытый |
  | О квартире | Мебель | `furniture` | «С мебелью» ×3 |
  | О квартире | Ремонт | `renovation` | Дизайнерский, Частичный, Косметический |

* Описание — `[itemprop=description]` без `.trans` («Переведено с
  армянского»); фото — список `po113.init('#po113', {img:[…]})` (9, 8, 13).
  Телефона на странице нет.
* Сверка со спекой: латиница значений (`cosmetic`, `stone`, `panel`…) на
  сайте не встречается. Значение поля — **слово сайта строчными**;
  словарь в спеке и в обоих конфигах переписан этими словами, добавлены
  «новостройка» и «высокие потолки» (`min: 3`). Живой шаг показал ещё
  «евроремонт» и «капитальный».

### Что сделано

| Задача | Итог |
| --- | --- |
| 3.2 | `012_funnel.sql` (`listing_pages`, `request_exclusions`, `matches.origin`); `PageFields`, `ListingPage`, `Match.origin`; тест 11 → 12 на заполненной базе |
| 3.3 | порт: `get_page`, `save_page`, `pages_for`, `listings_paged_since`, `page_counts` (для `doctor`); `listings_touched_since` видит открывшиеся страницы; `origin` пишется только при вставке, `MATCH_COMPARED` не тронут |
| 3.4 | `listam/parsers/item_page.py`: `parse_item_page`, `PARSER_VERSION = 1`, `_unknown`, `ItemPageMissing`; 16 тестов на трёх фикстурах |
| 3.5 | `listam/domain/wishes.py`: `Wish.check` (any_of, none_of, is, min), `vocabulary` (кривой словарь — `ConfigError`), `parse_wishes`, `request_wishes`, `field_label`; `requests`: неизвестное в `must_have` — отказ строки, в `nice_to_have` — «⚠ R-1 · nice_to_have: слово «…» не из словаря funnel.wishes — не учитывается» |
| 3.6 | `rejection(..., page=, must=)` → «страница не открыта» / подпись поля; фактор `wishes` (вес 15) в `DEFAULT_WEIGHTS`, конфигах, README («семь факторов») |
| 3.7 | `listam/pages.py`: `funnel_settings`, `plan_pages` (без сети), `run_pages`; `build_fetcher(config, delay_seconds=None)`; `FetchError.status` (404/410 → `gone`); CLI `pages [--max N] [--dry-run] [--keep-html]` |
| 3.8 | `run_match` читает страницы одним `pages_for`, отдаёт их `score`; «страница не открыта» не пишется и не закрывает; `origin` = `request` для новой/правленой заявки, иначе `market`; `pages` в циклах `hourly` и `nightly` между `scrape` и `match` |
| 3.9 | `doctor`: строка «Воронка» (потолок, пауза, словарь, заявок с пожеланиями, кандидатов, очередь, кэш по статусам, доля сбоев; ⚠ от 20 %); живой `pages --max 5` |
| — | README: раздел «Воронка», команды `pages`, колонки `must_have`/`nice_to_have`, циклы, «Устройство» |

### Числа (каждое — с командой)

* Батарея до фазы: `.venv/Scripts/python.exe -m pytest -q` → **934 passed,
  27 skipped** (45,79 с) — первым прогоном сессии.
* Батарея после фазы: та же команда → **1058 passed, 27 skipped** (51,6 с).
  План ждал ~940 (+6) — он недооценил фазу. Прирост +124: `pytest
  --collect-only -q` → `test_parser_item.py` 16, `test_wishes.py` 23,
  `test_pages.py` 24; остальные 61 — тесты, дописанные в существующие
  файлы (контракты базы и Fetcher, подбор, скоринг, заявки, doctor, CLI,
  конфиг, миграции, wiring).
* Схема боевой базы: `select max(version) from schema_version` → `(12,)`
  (мигрировал живой `pages`).
* На копии (`data/listam-prod.sqlite` → папка сессии, `notify.kind: none`):
  * `requests` → «новых 0, обновлённых 0, без изменений 3, закрытых 0» —
    слова R-2 («ремонт», «балкон, лифт») и R-3 («не панель», «евроремонт»)
    все в словаре.
  * `pages --dry-run` → «заявок с пожеланиями 2», «Кандидатов: 3275 …
    в очереди: 3275», «открылись бы 30 (потолок 30)».
  * `match --all` до живых страниц → «новых 0, обновлённых 0, без изменений
    25»; матчи по заявкам (`select … group by r.id`) — R-1 25/25, R-2 445/445,
    R-3 2852/2852 (всего/живых): старые матчи R-2 и R-3 живут (решение 12),
    новых без страниц нет.
  * `doctor --no-network` → «OK Воронка — потолок 30 за прогон, пауза 5 с,
    слов в словаре 9; заявок с пожеланиями 2; кандидатов 3275, в очереди
    3275; страниц в кэше 0 …».
* Живой шаг: `PYTHONIOENCODING=utf-8 … --env prod pages --max 5` → код 0,
  24,4 с; «Открыто: 5 (потолок 5): разобрано 5, снято 0, сбоев 0»; «⚠
  Неразобранных подписей: 24 (Кондиционер ×4, Плита ×4, Посудомоечная машина
  ×4, Стиральная машина ×4, Сушильная машина ×4, Холодильник ×4)»; «база со
  страницами залита в хранилище».
* Что легло в кэш (`select … from listing_pages join listings`): 5 × Кентрон,
  `ok`, попытка 1; тип дома — панельное ×3, монолит, каменное; ремонт —
  капитальный, косметический ×2, евроремонт, частичный; фото 6/14/4/5/23.
* Подбор на свежей копии после живого шага: `match --all` → «обновлённых 2,
  без изменений 25, Закрыто матчей: 3»; у R-3 три панельных матча закрыты с
  `retired_reason = 'тип дома'`, два (монолит, каменное) получили
  `wishes: [0.0, 15.0]` («евроремонт» не выполнен) и балл 86.
  Боевая база подбором не тронута.

### Что разошлось с планом и почему

1. **Значения полей — слова сайта, а не латиница.** План и спека ждали
   `cosmetic`, `stone`, `panel`. На странице их нет, а перевод пришлось бы
   вести вторым словарём, которого брокер не видит. Спека и конфиги поправлены
   (это разрешённое место фазы).
2. **`ItemGone` нет — снятое узнаётся по коду ответа.** Снятых в боевой базе
   0, вёрстку «объявление удалено» снять не на чем. `FetchError` получил
   `status` (http и playwright, контрактные тесты); 404/410 → `gone`.
   Страница без блока объявления — `ItemPageMissing` → `failed`, а не
   «все поля неизвестны». Д-8.
3. **Порт получил пятый метод `page_counts()`** (с контрактным тестом): строке
   «Воронка» нужен счёт всего кэша, а `pages_for` отдаёт только кандидатов.
4. **Исчерпавшая попытки страница — «поля неизвестны», снятая — «не
   открыта».** `matching.known_fields`: `failed` с `attempts >= max_attempts`
   даёт пустые поля (неизвестное не отказ), `gone` — `None`: матч по снятому
   не рождается, ленту догонит ночной обход.
5. **`funnel.max_opens_per_run: null` и `max_attempts: null` — отказ, а не
   «выключено».** Правило «выключается null» здесь означало бы массовый обход.
6. **`--max` больше потолка** — код 2 с именем ключа (`ConfigError` из
   `run_pages`, печатается как «Конфигурация не годится: --max 999 больше
   потолка funnel.max_opens_per_run = 30 …»); `--max 0` отсекает CLI своими
   словами.
7. **Номер строки отказа по словарю — 0.** Неизвестное слово проверяется после
   разбора строки, номера у `Request` нет; `RequestError.render` при 0
   пишет «заявка R-1: must_have = …» вместо «строка N».
8. **`Exclusion` не заведён** — таблица есть (миграция), модель и порт — фаза 6.

### Находки для следующих фаз

* **Очередь кандидатов — 3275, потолок — 30.** Порог грубого сита —
  `match.thresholds.digest` (40), и его проходят почти все: при часовом
  цикле очередь разойдётся за 3275 / 30 ≈ 110 прогонов (арифметика). Это рычаг фазы 4 (порог
  кандидата или разброс балла), а не повод поднимать потолок.
* **Старые матчи R-2/R-3 (445 и 2852) живут до открытия страницы** (решение
  12) и не несут `origin` (NULL = до миграции). Фаза 4 считает NULL как
  `market`; первичная подборка их уже не соберёт.
* **Шаг `pages` падает кодом 1, если не открылась ни одна** (спека), и цикл
  встаёт на нём: Cloudflare на страницах объявлений остановит в этом часе
  и `match --new`, и `notify --hot`. Если это случится вживую — решать
  в фазе 7 (например, `pages` в цикле с допуском), не молча.
* **Лишние подписи.** Блок бытовой техники (Д-9); «капитальный» ремонт —
  в словарь «ремонт» не входит (решать брокеру).
* **Живой шаг занял 24,4 с на 5 страниц** (`time`) — на 30 страниц часовой
  цикл прибавит, по оценке, не меньше 29 × 5 с паузы плюс загрузку; замерить
  в фазе 7 по логам цикла.

### Что осталось (пользователю)

```
! PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe -m listam --env prod schedule install
! schtasks /query /tn listam-hourly /v /fo list
```

Расписание (Д-6) — теперь уже с шагом `pages`. Живые проверки Д-2 не
прибавились. Д-8, Д-9 — в «Долгах».

## Стартовый промпт фазы 4

```
Ты продолжаешь этап M3.5 инструмента мониторинга list.am
в C:\Users\Artur.A.Gevorgyan\Downloads\new_project.

Прочитай docs/superpowers/plans/2026-09-23-m3-5-live-launch.md: разделы
«Global Constraints», «Карта файлов», «Долги», «Результат фазы 3» и свою
«Фазу 4». Чужие фазы не трогай. Спека —
docs/superpowers/specs/2026-09-23-m3-5-live-launch-design.md, решения 1–18
не пересматриваются.

Исходное состояние: HEAD — коммит «Результат фазы 3», дерево чистое,
батарея 1058 passed, 27 skipped, схема 12. Боевая база —
data/listam-prod.sqlite (хранилище — data/store; замеры — только на копии),
заявки — пример R-1…R-3, в кэше страниц 5 строк от живого pages --max 5.
Первым делом проверь, поставил ли пользователь расписание (долг Д-6):
schtasks /query /tn listam-hourly, строки fresh в runs, лог в data/logs,
строки «Расписание» и «Воронка» в doctor. Не поставил — спроси, не ставь сам.
Расписание установлено — не останавливай его без нужды, а если остановил —
верни и напиши об этом.

Твоя задача — фаза 4: единицы, а не сотни. Замер «до» скриптом на копии,
первичная подборка отдельно (origin = request не идёт в «звони сейчас»,
NULL считается market; раздел дайджеста «первичная подборка: N лучших
из M»), разброс балла рычагами — каждый с тестом и строкой «было → стало»,
замер «после». Учти находку фазы 3: очередь кандидатов воронки 3275 при
потолке 30 — порог грубого сита тоже рычаг. Замер на примере заявок —
«предварительно», в долг Д-1.

Работай по шагам: на каждое поведение — падающий тест ДО правки. Тесты —
только .venv/Scripts/python.exe -m pytest -q, CLI из скрипта — только
с PYTHONIOENCODING=utf-8. Пороги меняются только в этой фазе и только
с замером. Новый метод порта — новый контрактный тест. Схему не меняй
(миграция 012 уже была). След звонка пишет только mark, MATCH_COMPARED
не расширяется. Живые проверки Playwright не запускай — впиши в «Долги».
Сам на list.am не ходи. Ни одного числа без команды, которая его напечатала.

В конце сессии допиши «Результат фазы 4» и стартовый промпт фазы 5.
Сделай коммит.
```

---

# Фаза 4. Единицы, а не сотни

**Смысл:** настройка балла по живым данным. Предмет фазы — числа, поэтому
она начинается с замера и кончается замером.

**Ожидается после фазы:** ~960 passed, 25 skipped, схема 12 (вышло 1079 — см. «Результат фазы 4»).

### Задача 4.1. Замер «до»

- [x] Скрипт `tmp/measure_m35.py` на **копии** боевой базы (не оригинал):
  на каждую заявку — матчей, горячих, децили балла, сколько матчей `request`
  и `market`; по журналу `notifications` с фазы 1 — событий на заявку
  в каждом «горячем» сообщении (медиана, 95-й перцентиль).
- [x] Записать таблицу в отчёт. На примере заявок — пометка «предварительно».

### Задача 4.2. Первичная подборка отдельно (решение 14)

- [x] `collect_events`/«горячее»: событие `new` с `origin = request` в
  «звони сейчас» не идёт — `test_request_born_matches_skip_hot`; `NULL`
  (до миграции) считается `market`.
- [x] Дайджест: такие события заявки собираются в раздел «первичная подборка:
  N лучших из M» (`notify.digest.initial_top`, по умолчанию 15) —
  `test_initial_selection_is_a_digest_section`.

### Задача 4.3. Разброс балла

Кандидаты-рычаги (решается замером, не вкусом): вес `price_per_sqm`;
`district` для непервоочередного района (0,5 → меньше); `area_rooms` —
близость к середине диапазона вместо «попал/не попал»; вес `wishes`;
порог `match.thresholds.hot`. Цель на живых заявках: медиана событий
на заявку в «горячем» ≤ 3, 95-й перцентиль ≤ `notify.hot.per_request`.

- [x] Каждый рычаг — тест на его поведение и строка отчёта «было → стало»
  по скрипту 4.1. Изменения, не сдвинувшие замер, откатываются.
- [x] Решение по «подешевевший тонет в хвосте» (открытый вопрос 3 приёмки M3):
  после настройки посчитать, сколько 📉 не попало в показанные. Ноль или
  единицы — «не чинится», записать. Иначе — 📉 первыми в пределах заявки,
  тест.

### Задача 4.4. Замер «после»

- [x] Тот же скрипт, те же колонки. Итог — в отчёт и в долг Д-1 (повторить
  на реальных заявках).

---

## Результат фазы 4

Сессия 23.09.2026. «Звони сейчас» больше не берёт первичную подборку, балл
разведён рычагами, очередь воронки сужена втрое. Замер — на копии и на
примере заявок, поэтому **предварительный** (Д-1).

### Проверка расписания (первым делом)

* `schtasks /query /tn listam-hourly /v /fo list` → «ERROR: The system cannot
  find the file specified.» (`listam-nightly` — то же).
* `runs` боевой базы → одна строка `(1, 2026-09-23T13:02:26…, 2026-09-23T13:11:51…)`,
  `fresh` нет; `ls data/logs` → «No such file or directory»; `notifications` →
  `(0,)`.
* `--env prod doctor --no-network` → «⚠ Расписание — часовой: ещё не было;
  ночной: 23.09 17:02 (Asia/Yerevan)»; «OK Воронка — … кандидатов 3275, в очереди
  3270; страниц в кэше 5».
* Спросил пользователя; ответ — «если не блокер — оставим в долгах». Фаза 4
  мерит на копии пробными прогонами, журнал отправок ей не нужен — не блокер.
  Д-6 дополнен. Расписание не ставил и не трогал.

### Как мерили

`tmp/measure_m35.py` (в репозитории — для повтора на реальных заявках, Д-1):
копирует `data/listam-prod.sqlite` в папку вне репозитория, пишет рядом
боевой конфиг с подменёнными путями, `notify.kind: none` и запасным окном
96 ч, гоняет `requests` (с `--new-request`), `match --all`, `pages --dry-run`,
`notify --hot --dry-run`, `notify --digest --dry-run` и печатает три таблицы.
Рычаг поверх конфига — `--set ключ=значение`. Оригинал не трогается: mtime
`data/listam-prod.sqlite` — 18:10:19 (+04), это живой `pages` фазы 3.

Журнала «горячего» нет (0 строк, Д-6), поэтому «событий на заявку в часовом
сообщении» — **модель**: объявления копии по возрастанию номера режутся на
окна по 100, в каждом — сколько горячих на заявку. Номер list.am растёт со
временем подачи; сколько объявлений приходит за час — неизвестно, окно 100 —
допущение, а не замер. Балл — «сейчас», то есть `score()` без страницы: у R-2
и R-3 (`must_have`) матчи в базе заморожены до открытия страницы (решение 12)
и `match --all` их не пересчитывает, поэтому таблица матчей для них не мерка.

Команда каждой строки: `PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe
tmp/measure_m35.py --out <папка сессии> --label <метка> [--set …]`.

### Замер «до» и рычаги: было → стало

Горячих — представителей кластеров с баллом «сейчас» ≥ hot; окно — медиана /
95-й перцентиль / окон больше `notify.hot.per_request` (5) из 206.

| Шаг (метка) | R-1 горячих · окно | R-2 горячих · окно | R-3 горячих · окно | Кандидатов воронки |
| --- | --- | --- | --- | --- |
| до (`l0-before`, `--set match.secondary_district=0.5`) | 23 · 0/1/0 | 377 · 2/4/3 | 2273 · 10/18/184 | 3275 |
| L1 район не из приоритетных 0,5 → 0 (`l1-district`) | 23 · 0/1/0 | 269 · 1/4/2 | 1497 · 7/14/131 | 3222 |
| L2 веса: бюджет 30 → 10, цена/м² 20 → 40 (`l2-weights`) | 25 · 0/1/0 | 245 · 1/3/1 | 1348 · 6/14/116 | 2931 |
| L3 `hot` 70 → 80, `score_green` 80 → 90 (`l3-hot`) | 23 · 0/1/0 | 155 · 0/3/0 | 670 · 3/8/37 | 2931 |
| L4 кандидат воронки — лучший возможный балл ≥ hot (`l4-after`) | 23 · 0/1/0 | 155 · 0/3/0 | 670 · 3/8/37 | **948** |

* Децили балла «сейчас» (10…90 %) у R-3: до — `62 70 74 78 81 83 83 89 94`,
  после — `49 57 63 68 71 72 78 89 94`; у R-2: `65 72 75 78 80 82 85 88 92` →
  `52 59 64 68 72 75 82 87 90`. Сжатие вверху ушло.
* `notify --hot --dry-run` на копии (окно 96 ч захватывает рождение всех
  старых матчей, `origin` NULL = рынок): «Событий: 2670» → «Событий: 1830».
* `doctor --no-network` на конфиге копии `l4-after` → «кандидатов 948, в очереди
  943»; было «3275, в очереди 3270». Прогонов по 30 до пустой очереди:
  3270 / 30 ≈ 109 → 943 / 30 ≈ 32 (арифметика).
* Цель плана — медиана ≤ 3, 95-й перцентиль ≤ 5. После: R-1 и R-2 в цели
  (0/1, 0/3); R-3 — медиана 3, **p95 8**, 37 окон из 206 выше потолка. R-3
  «нарочно широкая» (3 района, 2–4 комнаты, $250 000, от 60 м²); дальше её
  ведёт пометка «слишком широкая» дайджеста, а не порог для всех.
* Побочный эффект L2: у R-1 горячими стали все 25 (было 23) — у узкой заявки
  варианты в растяжке бюджета теряют меньше. На L3 вернулось к 23.

**Не пробовали кодом (и почему):** `area_rooms` «близость к середине» — у R-3
площадь открыта сверху (`area_max` пуст), середины нет, а доля `area_rooms` = 1
у 2788 из 2849 её матчей только за «попал»; R-1 и R-2 в цели и так. Вес
`wishes` — в кэше 5 страниц, мерить не на чем; оставлен 15, «предварительно».

### Первичная подборка (решение 14)

`l5-new-request` — та же копия плюс R-4 «ПРИМЕР новая» (как R-3, но без
пожеланий, чтобы матчи родились без страниц):

* `requests` → «новых 1»; `match --all` → «новых 2852 … Из них горячих: 695».
* Матчи R-4: `request / market / NULL` = `2852 / 0 / 0`, горячих 670 — столько
  ушло бы в «звони сейчас» до фазы (там отбора по `origin` не было).
* `notify --hot --dry-run` → у R-4 в «горячем» **0** событий; «Событий: 1830,
  заявок: 3» — как без R-4.
* `notify --digest --dry-run` → сводка «📋 первичная подборка — 2495», строка
  «R-4 · ПРИМЕР новая — 📋 первичная подборка 2495» и раздел «📋 ДАЙДЖЕСТ · R-4
  · ПРИМЕР новая — первичная подборка: 15 лучших из 2495».

### «Подешевевший тонет в хвосте» (открытый вопрос 3 приёмки M3)

Не мерен: на копии `price_history` — 20 664 строки на 20 664 объявления,
объявлений с двумя ценами — 0; 📉 в пробных прогонах — 0. Решать нечем → Д-10.

### Что сделано

| Задача | Итог |
| --- | --- |
| 4.1 | `tmp/measure_m35.py`: копия базы и конфига, CLI-шаги, три таблицы, модель окон, `--set`, `--new-request` |
| 4.2 | `domain.events.is_initial` (рождение по заявке; подешевевший и вернувшийся — рынок; NULL — рынок); `EventsPage.market()`; «звони сейчас» — без первичной подборки; дайджест — раздел «первичная подборка: N лучших из M» после рыночного, в сводке «📋 первичная подборка — M», широту подборка не мерит; `notify.digest.initial_top` (15, через `positive`) |
| 4.3 | L1: `match.secondary_district` (доля 0…1; `null`, строка, `true` — отказ с именем ключа), `score(..., secondary_district=)`; без приоритетных районов все названные — полная доля. L2: веса в обоих конфигах и `DEFAULT_WEIGHTS`. L3: `hot` 80, `score_green` 90 (иначе 🟡 исчез бы), `DEFAULT_HOT` один — в `domain/scoring.py`. L4: `scoring.best_case`; `plan_pages` пускает в очередь только того, кто может стать горячим (hot выключен — digest) |
| 4.4 | замер «после» — таблица выше; Д-1 дополнен командой повтора |
| — | README: веса, пороги, кружки, кандидат воронки, первичная подборка |

### Числа (каждое — с командой)

* Батарея до фазы: `.venv/Scripts/python.exe -m pytest -q` → **1058 passed,
  27 skipped** (51,56 с).
* Батарея после фазы: та же команда → **1079 passed, 27 skipped** (52,77 с).
  План ждал ~960 — число плана от 824 устарело ещё в фазе 1. Прирост +21:
  `test_matching.py` +13 (доля района — 7 с параметрами, три конфиг-теста
  по 2 — prod и dev), `test_notifications.py` +3, `test_scoring.py` +2,
  `test_pages.py` +2, `test_events.py` +1.
* Схема не менялась: 12.
* Остальные числа — в таблицах выше, с метками прогонов скрипта.

### Что разошлось с планом и почему

1. **Журнала «горячего» нет** — расписание не стоит (Д-6), `notifications` 0
   строк. Медиана и 95-й перцентиль — модель окон по номеру объявления, а не
   журнал отправок. Приёмка M3.5 (п. 2) всё равно мерит журнал недели.
2. **Балл мерится «сейчас», а не по `matches`.** У заявок с `must_have`
   матчи заморожены до страницы (решение 12): `match --all` на L1 не сдвинул
   в таблице матчей ни одного числа R-2/R-3. Скрипт считает `score()` без
   страницы по представителям кластеров — с этим баллом матч и родится
   (плюс `wishes`).
3. **Пятый рычаг — порог кандидата воронки** (находка фазы 3). Это не новый
   ключ, а правило: кандидат — тот, чей лучший возможный балл (все
   `nice_to_have` выполнены) не ниже `hot`. Цена: у заявок с `must_have`
   матчи ниже hot больше не рождаются — дайджест их не увидит.
4. **`score_green` 80 → 90** — не рычаг разброса, а следствие L3: при
   `hot` 80 🟡 не осталось бы вовсе. Тест держит «🟢 выше hot».
5. **Непервоочередной район 0 потребовал правки правила**: заявка без
   приоритетных районов получала бы 0 за район целиком. Теперь без
   приоритета все названные районы — полная доля (тест).
6. **Unit-тест `is_initial` дописан после кода** (поведение уже держали
   тесты `notify`); остальные тесты писались до правки и падали по делу.

### Находки для следующих фаз

* **Очередь воронки → «звони сейчас»** (Д-11): решение 9 шлёт матч,
  родившийся после открытия страницы, в «горячее» новым, `origin` у него
  `market`. 943 в очереди — около 32 часовых прогонов, в каждом до 30 таких
  матчей у R-2/R-3. Потолок `per_request` 5 держит сообщение, хвост уходит
  в дайджест; если на живых заявках это окажется «сотнями в хвосте» —
  вопрос к спеке, а не правка фазы.
* **R-3 выше цели (p95 8)** — широкая заявка остаётся широкой; следующий
  рычаг, если реальные заявки такие же, — не общий порог, а совет сузить
  (он уже есть в дайджесте).
* **Фазе 5:** `find` зовёт `score()` — передать `secondary_district` из
  `settings(config)`, иначе `find` и подбор разойдутся в балле.
* **Фазе 6:** в дайджесте у заявки теперь два раздела (рынок и подборка) —
  исключения из отказов видны в шапке обоих.

### Что осталось (пользователю)

```
! PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe -m listam --env prod schedule install
! schtasks /query /tn listam-hourly /v /fo list
```

Расписание (Д-6). Реальные заявки и повтор замера — Д-1 (команда там же).
Новые долги — Д-10, Д-11. Живые проверки Д-2 не прибавились.

## Стартовый промпт фазы 5

```
Ты продолжаешь этап M3.5 инструмента мониторинга list.am
в C:\Users\Artur.A.Gevorgyan\Downloads\new_project.

Прочитай docs/superpowers/plans/2026-09-23-m3-5-live-launch.md: разделы
«Global Constraints», «Карта файлов», «Долги», «Результат фазы 4» и свою
«Фазу 5». Чужие фазы не трогай. Спека —
docs/superpowers/specs/2026-09-23-m3-5-live-launch-design.md, решения 1–18
не пересматриваются.

Исходное состояние: HEAD — коммит «Результат фазы 4», дерево чистое,
батарея 1079 passed, 27 skipped, схема 12. Боевая база —
data/listam-prod.sqlite (хранилище — data/store; замеры — только на копии,
скрипт tmp/measure_m35.py), заявки — пример R-1…R-3, в кэше страниц 5 строк.
Первым делом проверь, поставил ли пользователь расписание (долг Д-6):
schtasks /query /tn listam-hourly, строки fresh в runs, лог в data/logs,
строки «Расписание» и «Воронка» в doctor. Не поставил — спроси, не ставь сам.
Расписание установлено — не останавливай его без нужды, а если остановил —
верни и напиши об этом.

Твоя задача — фаза 5: find — быстрый поиск. «Арабкир, 3 комнаты,
до $120k» одной командой, без CSV: флаги собирают строку заявки и идут
через тот же parse_row и тот же score(), что подбор (решение 16), — с весами,
порогами и secondary_district из settings(config), настроенными фазой 4.
Заявку find не пишет; страницы открывает только --open N под своим потолком
funnel.find_max_opens и честно говорит, у скольких поле известно.

Работай по шагам: на каждое поведение — падающий тест ДО правки. Тесты —
только .venv/Scripts/python.exe -m pytest -q, CLI из скрипта — только
с PYTHONIOENCODING=utf-8. Пороги не поднимай (кроме фазы 4 с замером).
Новый метод порта — новый контрактный тест. Схему меняет только
миграция 012 (фаза 3). След звонка пишет только mark, MATCH_COMPARED
не расширяется. Живые проверки Playwright не запускай — впиши в «Долги».
Сам на list.am не ходи. Ни одного числа без команды, которая его напечатала.

В конце сессии допиши «Результат фазы 5» и стартовый промпт фазы 6.
Сделай коммит.
```

---

# Фаза 5. `find` — быстрый поиск

**Смысл:** «Арабкир, 3 комнаты, до $120k» — одной командой, без CSV.

**Ожидается после фазы:** ~980 passed, 25 skipped, схема 12 (вышло 1106 — см. «Результат фазы 5»).

- [x] Флаги → строка-словарь → `domain.requests.parse_row` → `Request`
  (решение 16): `--district` (повторяемый), `--rooms 2-3`, `--max-price`,
  `--area 60-90`, `--floor-not-first`, `--floor-not-last`, `--wish "…"`,
  `--owner`, `--limit`, `--open N`. Ошибки разбора — код 2 с названной
  колонкой (`test_find_rejects_nonsense`).
- [x] Поиск читает базу без замка (`open_for_reading`), кластеры, медианы,
  `pages_for`; сортировка по баллу; печать — таблица витрины + строка
  на пожелание: «ремонт: известно у 34 из 120, подходит 12; неизвестно у 86 —
  `--open 20` откроет лучшие» (`test_find_counts_known_and_unknown`).
- [x] `--open N`: под `working_session`, `pages` только по найденным,
  не больше `funnel.find_max_opens` (больше — код 2), затем перескоринг
  и печать (`test_find_open_respects_its_ceiling`).
- [x] Только собственники: `--owner`. Заявку `find` не пишет —
  `test_find_writes_no_request`.
- [x] README: таблица команд и короткий раздел с тремя примерами.

## Результат фазы 5

Сессия 23.09.2026. `find` работает: флаги → строка заявки → `parse_row` →
тот же `score()`, что подбор, с `settings(config)`; заявку и матчи не пишет,
без `--open` читает базу без замка, `--open N` — под своим потолком.

### Проверка расписания (первым делом)

* `schtasks /query /tn listam-hourly /fo list` → «ERROR: The system cannot
  find the file specified.» (`listam-nightly` — то же).
* `runs` боевой базы (чтение `mode=ro`) → одна строка
  `(1, 'full', 2026-09-23T13:02:26…, 2026-09-23T13:11:51…)`, `fresh` нет;
  `ls data/logs` → «No such file or directory»; `notifications` → `(0,)`.
* `--env prod doctor --no-network` → «⚠ Расписание — часовой: ещё не было;
  ночной: 23.09 17:02 (Asia/Yerevan)»; «OK Воронка — … кандидатов 948, в
  очереди 943; страниц в кэше 5 (разобрано 5, снято 0, сбоев 0 (0%))».
* Спросил пользователя; ответ — «если уверен, что не блокер — оставь в
  долгах». `find` читает `listings` и `listing_pages`, журнал отправок ему
  не нужен — не блокер. Д-6 дополнен. Расписание не ставил и не трогал.

### Что сделано

| Шаг | Итог |
| --- | --- |
| флаги → `Request` | `listam/find.py`: `Query` → строка с колонками `districts`, `rooms`, `budget_max`, `area_min`/`area_max` (из `--area 60-90`, `60-`, `-90`), `no_first_floor`/`no_last_floor`, `nice_to_have` → `domain.requests.parse_row`. Ошибка разбора — код 2: «--max-price не годится: budget_max = '120k' — не число». Неизвестное слово `--wish` — код 2 со списком словаря. Без единого условия сита — код 2 |
| поиск | `open_for_reading` (без замка, без миграций), кластеры и медианы по всей базе, `pages_for` найденных, `score(..., weights, stretch_percent, nice, secondary_district)` из `settings(config)`; представители кластеров, лучший балл первым; шапка — найдено, горячих и в дайджест по `match.thresholds`; строки — та же `match_lines`, что у витрины; строка на пожелание «известно у K из N, подходит F; неизвестно у U — --open M откроет лучшие» |
| `--open N` | под `working_session`: открывает страницы найденных без страницы, с устаревшей (`is_fresh`) или со сбоем в запасе попыток — лучшие первыми, не больше N; N > `funnel.find_max_opens` — код 2, `null` в конфиге — отказ конфига; без `--wish` — код 2; затем перескоринг и заливка «база со страницами» |
| `--owner`, `--limit` | только собственники; строк — `--limit` или `match.limit`, хвост «…и ещё N» |
| заявку не пишет | `test_find_writes_no_request`: после поиска и `--open` — `requests` 0, `matches` 0, `listing_pages` 1 |
| общий код | `matches_view.match_lines` — строка витрины (её печатают `matches` и `find`); `pages.open_page`, `pages.is_fresh` — публичные (были `_open`, `_is_fresh`) |
| README | строки `find` в таблице команд, раздел «Быстрый поиск: `find`» с тремя примерами |

Новых методов порта нет (`pages_for`, `save_page` — фаза 3), контрактных
тестов не прибавилось. Схема не менялась.

### Числа (каждое — с командой)

* Батарея до фазы: `.venv/Scripts/python.exe -m pytest -q` → **1079 passed,
  27 skipped** (54,05 с).
* Батарея после фазы: та же команда → **1106 passed, 27 skipped** (61,88 с).
  План ждал ~980 — число от 824 устарело ещё в фазе 1. Прирост +27 — весь
  в `tests/test_find.py` (`pytest -q tests/test_find.py` → 27 passed:
  10 параметров отказа, 2 — порог hot, 15 остальных).
* Схема: `python -c "from listam.adapters.db_sqlite import
  latest_schema_version as v; print(v())"` → 12.
* Живой `find` — на копии (`tmp/measure_m35.prepare` в папку сессии вне
  репозитория, `--env prod --config-dir <копия>/config`), оригинал не
  тронут: `ls -l --time-style=full-iso data/listam-prod.sqlite` до и после →
  18:10:19 (+04), как в фазе 4.
  * `find --district Арабкир --rooms 3 --max-price 120000 --limit 5` →
    «Поиск: Арабкир · 3 ком. · до $120,000 (с растяжкой — до $132,000)»,
    «Кластеров: найдено 58, горячих 43 (балл от 80), в дайджест 15 (от 40)»,
    5 строк, «…и ещё 53», код 0; `time` → real 1,566 с.
  * `find --district "Канакер-Зейтун" --district "Нор Норк" --rooms 2-3
    --area 80-110 --max-price 175000 --not-first-floor --wish "ремонт,
    балкон, лифт" --limit 3` → «найдено 447, горячих 288 …, в дайджест 159»;
    по каждому пожеланию «известно у 0 из 447, подходит 0; неизвестно у 447 —
    --open 20 откроет лучшие» (5 страниц кэша к этим 447 не относятся).
  * `find --district Арабкир --max-price 120k` → код 2, «--max-price не
    годится: budget_max = '120k' — не число»; `--wish ремонт --open 21` →
    код 2, «--open 21 больше потолка funnel.find_max_opens = 20».
* `--open` вживую не запускался (Playwright и list.am — нельзя) → Д-2, Т-7.

### Что разошлось с планом и почему

1. **`--wish` — пожелание (`nice_to_have`), а не `must_have`.** Жёсткое
   пожелание без страницы — отказ «страница не открыта» (решение 9), и `find`
   показал бы только объявления из кэша (5 штук), то есть пустую выдачу.
   Решение 16: «поле страницы неизвестно — строка говорит об этом»; это ровно
   фактор `wishes` плюс счёт известных. Тест
   `test_find_wish_is_a_score_factor_not_a_filter`.
2. **Флаги этажа — в двух написаниях.** Спека: `--not-first-floor`; план:
   `--floor-not-first`. Приняты оба (одна опция argparse), в README — спековое.
3. **Два отказа сверх плана:** `find` без единого условия сита (вся база —
   не поиск) и `--open` без `--wish` (балл страница меняет только через
   пожелания) — код 2.
4. **`secondary_district` передаётся, но сейчас балл `find` не меняет:**
   у поиска нет приоритетных районов, а без приоритета все названные районы —
   полная доля (правило фазы 4). Передаётся ради равенства с подбором;
   `test_find_scores_exactly_like_matching` сверяет балл `find` со `score()`
   на `settings(config)`, тест весов — что веса берутся из конфига. Флага
   «приоритетный район» план не называл — не добавлял.
5. **Тест после кода — один:** `test_find_ceiling_null_is_refused_by_the_config`
   дописан после `find_ceiling` и зелёный сразу. Остальные 26 писались до
   кода и падали по делу («invalid choice: 'find'»). Тест порога hot сначала
   был неверен (hot 101 — вне шкалы, конфиг прав), переписан на 100 и 50.

### Находки для следующих фаз

* **Длинные номера объявлений.** В выдаче `find` первым стоит
  `item/995103744` (Нор Норк, 82 м², $85 000); на копии объявлений с номером
  длиннее 8 цифр — 260 (`select count(*) from listings where length(id)>8`).
  Карточки штатные, но нумерация не та, что у ~24 млн; кластер берёт имя по
  `(len, id)` — такие для него «младше» всех. Проверить страницу глазами —
  пользователю, не агенту.
* **Фазе 6:** `find` не читает `request_exclusions` — у поиска нет заявки,
  исключать нечего; `mark` к `find` отношения не имеет.
* **Фазе 7:** в неделю приёмки `find` без `--open` можно — он базу не пишет
  и замок не берёт; `find --open` пишет кэш под замком, как `pages`.

### Что осталось (пользователю)

```
! PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe -m listam --env prod schedule install
! schtasks /query /tn listam-hourly /v /fo list
```

Расписание (Д-6). Живой `find --open` — Д-2, Т-7 (команда там). Реальные
заявки — Д-1.

## Стартовый промпт фазы 6

```
Ты продолжаешь этап M3.5 инструмента мониторинга list.am
в C:\Users\Artur.A.Gevorgyan\Downloads\new_project.

Прочитай docs/superpowers/plans/2026-09-23-m3-5-live-launch.md: разделы
«Global Constraints», «Карта файлов», «Долги», «Результат фазы 5» и свою
«Фазу 6». Чужие фазы не трогай. Спека —
docs/superpowers/specs/2026-09-23-m3-5-live-launch-design.md, решения 1–18
не пересматриваются.

Исходное состояние: HEAD — коммит «Результат фазы 5», дерево чистое,
батарея 1106 passed, 27 skipped, схема 12. Боевая база —
data/listam-prod.sqlite (хранилище — data/store; замеры — только на копии,
tmp/measure_m35.py умеет её готовить), заявки — пример R-1…R-3, в кэше
страниц 5 строк. Первым делом проверь, поставил ли пользователь расписание
(долг Д-6): schtasks /query /tn listam-hourly, строки fresh в runs, лог
в data/logs, строки «Расписание» и «Воронка» в doctor. Не поставил — спроси,
не ставь сам. Расписание установлено — не останавливай его без нужды, а если
остановил — верни и напиши об этом.

Твоя задача — фаза 6: «звонил» и «отказ». Порт: add_exclusions,
exclusions_for, drop_exclusions — у каждого контрактный тест
(tests/contracts/test_database_contract.py); таблица request_exclusions
уже есть (миграция 012). listam mark <заявка> <объявление>
called|rejected|new [--reason "…"] в listam/feedback.py под working_session:
матч по объявлению, иначе по кластеру; rejected всегда исключает кластер,
причина из feedback.reasons сужает заявку, неизвестная — записывается и
не применяется; new откатывает отметку и её исключения; после отметки —
run_match по этой заявке. Подбор видит исключения как жёсткий критерий
(«клиент отказал: …»); шапка заявки в сообщении — исключения словами,
в обоих разделах дайджеста (рынок и первичная подборка).

Работай по шагам: на каждое поведение — падающий тест ДО правки. Тесты —
только .venv/Scripts/python.exe -m pytest -q, CLI из скрипта — только
с PYTHONIOENCODING=utf-8. Пороги не поднимай. Новый метод порта — новый
контрактный тест. Схему меняет только миграция 012 (фаза 3) — нужна вторая,
это находка в отчёт, а не файл 013. След звонка (matches.status,
reject_reason) пишет только mark, MATCH_COMPARED не расширяется. Живые
проверки Playwright не запускай — впиши в «Долги». Сам на list.am не ходи.
Ни одного числа без команды, которая его напечатала.

В конце сессии допиши «Результат фазы 6» и стартовый промпт фазы 7.
Сделай коммит.
```

---

# Фаза 6. «Звонил» и «отказ»

**Смысл:** цикл «клиент отказал → следующие матчи уже»: закрывается
обратная связь из спеки.

**Ожидается после фазы:** ~1005 passed, 25 skipped, схема 12 (вышло 1154 passed, 27 skipped — см. «Результат фазы 6»).

### Задача 6.1. Порт: исключения

```python
@abstractmethod
def add_exclusions(self, exclusions: list[Exclusion]) -> None: ...
@abstractmethod
def exclusions_for(self, request_id: int) -> list[Exclusion]: ...
@abstractmethod
def drop_exclusions(self, match_id: int) -> int: ...
```

- [x] Контрактные тесты; `iter_requests`/`get_request` исключений не несут —
  подбор читает их отдельно одним запросом на прогон.

### Задача 6.2. `listam mark`

- [x] `mark <заявка> <объявление> called|rejected|new [--reason "…"]`
  (`listam/feedback.py`, каркас `working_session`). Матч ищется по
  объявлению, а если его нет — по кластеру объявления (карточку могли
  сменить на дешёвую). Нет — код 1 со словами.
- [x] `rejected`: всегда исключение `cluster`; причина разбирается по
  `feedback.reasons` — `first_floor`, `last_floor`, `district` (район этого
  объявления), поле страницы (значение этого объявления со страницы; поля
  нет — только запись причины, и команда так и говорит). Неизвестная
  причина — записана в `reject_reason`, заявку не сужает, строка об этом.
- [x] `new` — откат: статус `new`, `reject_reason` пуст, `drop_exclusions(match_id)`.
- [x] После отметки — подбор по этой заявке (`run_match(config, external_id=…)`),
  чтобы отпавшее ушло в вечерний дайджест со словами «клиент отказал: …».
- [x] Тесты: `test_rejected_excludes_the_cluster_always`,
  `test_first_floor_reason_narrows_the_request`,
  `test_district_reason_uses_the_listing_district`,
  `test_unknown_reason_is_recorded_not_applied`, `test_new_undoes_the_mark`,
  `test_mark_finds_the_match_by_cluster`.

### Задача 6.3. Подбор и вёрстка видят исключения

- [x] `rejection`: исключения заявки — ещё жёсткие критерии; причина
  закрытия — «клиент отказал: <слова>».
- [x] Шапка заявки в сообщении: «· без 1-го этажа, без панели» (исключения
  словами; кластеры не перечисляются).
- [x] README: раздел «Отметки и отказы».

## Результат фазы 6

Сессия 23.09.2026. `mark` работает: отметка матча, исключения в
`request_exclusions`, подбор по заявке следом; подбор и шапка сообщения
видят исключения.

### Проверка расписания (первым делом)

* `schtasks /query /tn listam-hourly /fo list` → «ERROR: The system cannot
  find the file specified.» (`listam-nightly` — то же).
* `runs` боевой базы (чтение `mode=ro`) → одна строка
  `(1, 'full', 2026-09-23T13:02:26…, 2026-09-23T13:11:51…)`, `fresh` нет;
  `ls data/logs` → «No such file or directory»; `notifications` → `(0,)`;
  `listing_pages` → `(5,)`; `request_exclusions` → `(0,)`.
* `--env prod doctor --no-network` → «⚠ Расписание — часовой: ещё не было;
  ночной: 23.09 17:02 (Asia/Yerevan)»; «OK Воронка — … кандидатов 948, в
  очереди 943; страниц в кэше 5 (разобрано 5, снято 0, сбоев 0 (0%))».
* Спросил пользователя; ответ — «если уверен, что не блокер — оставляй в
  долгах». `mark` сам зовёт подбор и журнал отправок не читает — не блокер.
  Д-6 дополнен. Расписание не ставил и не трогал.

### Что сделано

| Шаг | Итог |
| --- | --- |
| 6.1 порт | `Exclusion` в `domain/models.py`; `add_exclusions`, `exclusions_for`, `drop_exclusions` в порту и в `SqliteDatabase`; шесть контрактных тестов (пусто без отказов, читается как записано, заявки не смешиваются, пустой список ничего не пишет, `drop` снимает только свои, `get_request`/`iter_requests` исключений не несут) |
| 6.2 `mark` | `listam/feedback.py`, `listam mark <заявка> <объявление> called\|rejected\|new [--reason "…"]` под `working_session`. Матч — по объявлению, иначе по `listings.cluster_id` (живой прежде закрытого); нет — код 1 «у заявки R-1 нет матча на … и его кластер». `rejected`: исключение `cluster` (значение — `matches.cluster_id`, нет — id объявления) всегда; причина — слова через запятую по `feedback.reasons`: `first_floor`/`last_floor`, `district` (район объявления матча), поле страницы (значение с открытой страницы объявления матча, затем названного); неизвестное слово или нет поля — строка «причина записана, заявку не сужает». Повторная отметка заменяет исключения прошлой; `new` — статус `new`, причина пуста, свои исключения сняты. Затем `run_match(config, external_id=…)` — после сессии (у подбора свой замок); у заявки не `active` подбор не идёт, строка об этом. `--reason` не у `rejected` — код 2; кривой `feedback.reasons` — код 2 до работы |
| 6.3 подбор | `scoring.rejection(…, refused=, cluster_id=)`: после грубого сита (бюджет честнее) и до страничных условий; причина — «клиент отказал» или «клиент отказал: <слова брокера>». `matching._write_matches` читает `exclusions_for` одним запросом на заявку и отдаёт кластер прохода (`representatives[…].cluster_id`, а не вчерашнюю колонку); страницы читает и у заявки без пожеланий, если есть отказ по полю страницы |
| 6.3 вёрстка | `EventsPage.exclusions` (id заявки → исключения; `market()` их несёт), `collect_events` заполняет; `layout.request_head(…, refused)` — «· без 1-го этажа, без панели» в конце второй строки; одинаково в «звони сейчас», рыночном разделе и первичной подборке дайджеста. Слова — `scoring.refusal_words`: кластеры не перечисляются, повторы схлопываются |
| конфиг | `feedback.reasons` в `config/prod.yaml` и `config/dev.yaml` — пять слов спеки |
| README | строки `mark` в таблице команд, раздел «Отметки и отказы: `mark`» |

Схема не менялась, второй миграции не понадобилось: `request_exclusions`
из 012 хватило как есть.

### Числа (каждое — с командой)

* Батарея до фазы — не перезапускал; исходное из промпта — 1106 passed,
  27 skipped.
* Батарея после фазы: `.venv/Scripts/python.exe -m pytest -q` →
  **1154 passed, 27 skipped** (54,98 с). План ждал ~1005 — число от 824
  устарело ещё в фазе 1. Прирост +48: `git diff -U0 <файл> | grep -c
  '^+def test_'` → контракт 6, `test_scoring` 9, `test_matching` 4,
  `test_layout` 4, `test_notifications` 1; `pytest -q tests/test_feedback.py`
  → 24 passed (20 функций, из них две параметризованы: 4 кривых словаря
  и 2 конфига).
* Схема: `python -c "from listam.adapters.db_sqlite import
  latest_schema_version as v; print(v())"` → 12; `ls listam/migrations |
  tail -2` → `011_notification_channel.sql`, `012_funnel.sql`.
* Живой `mark` — на копии (`tmp/measure_m35.prepare` в папку сессии вне
  репозитория, `--env prod --config-dir <копия>/config`), оригинал не
  тронут: `ls -l --time-style=full-iso data/listam-prod.sqlite` до и после →
  18:10:19 (+04).
  * На копии у R-3 матчей на первом этаже — 234 (`select count(*) from
    matches … where external_id='R-3' and l.floor=1`).
  * `mark R-3 22985823 rejected --reason "первый этаж, дорого"` → код 0,
    «Заявка сужена: без 1-го этажа», ««дорого» нет в feedback.reasons —
    причина записана, заявку не сужает», «Закрыто матчей: 237»; `time` →
    real 1,991 с. Причины закрытия (`group by retired_reason`) →
    «клиент отказал: первый этаж» 233, «клиент отказал: первый этаж,
    дорого» 1 (сам матч), «тип дома» 3. Последние три — не отметка: первый
    полный подбор R-3 после фазы 3 закрыл три матча с открытой панельной
    страницей (`must_have: не панель`); ночной `match --all` сделал бы то же.
  * `notify --digest --dry-run` на копии → код 0; шапка «📋 ДАЙДЖЕСТ · R-3 ·
    ПРИМЕР широкая» / «Кентрон, Арабкир, Давташен · 2–4 комн. · от 60 м² ·
    до $250 000 · без 1-го этажа»; в сводке «❌ 237 (тип дома 3, клиент
    отказал: первый этаж 233, клиент отказал: первый этаж, дорого 1)».
  * `mark R-3 22985823 called --reason x` → код 2; `mark R-3 999 called` →
    код 1, «у заявки R-3 нет матча на 999 и его кластер».
  * `mark R-3 22985823 new` → код 0, «Снято исключений прошлой отметки: 2»;
    `request_exclusions` → 0 строк, матч `('new', None)`; закрытых
    «клиент отказал: первый этаж» осталось **233** — см. Д-13.
* Живая отправка в Telegram не делалась → Д-2, Т-8.

### Что разошлось с планом и почему

1. **Кластер исключения берётся из прохода, а не из колонки.** `rejection`
   получает `cluster_id` представителя от подбора: `listings.cluster_id`
   бывает вчерашним (об этом `_count_clusters`). Тест
   `test_the_cluster_of_the_pass_beats_the_stale_one_on_the_listing`.
2. **Отказ по полю страницы при неизвестном поле — не отказ.** Так же, как
   у пожеланий (решение 13): иначе отказ «тип дома» превратил бы всю заявку
   в кандидатов «страница не открыта». Цена — Д-13: `pages` ради исключений
   страниц не открывает.
3. **Слова шапки.** Спека даёт «без 1-го этажа, без панели». Для
   `building_type` — словарь «панельное → без панели», «каменное → без
   камня», «монолит → без монолита»; остальное — «ремонт не
   «косметический»», район — «не Кентрон».
4. **Три записи отметки — три транзакции.** `drop_exclusions`,
   `set_match_status`, `add_exclusions` открывают каждый свою, а вложенных
   у порта нет (`BEGIN` внутри `BEGIN` — ошибка SQLite). Оборвётся
   посередине — повтор той же команды доводит базу: повторная отметка
   заменяет исключения прошлой.
5. **Сверх плана:** несколько причин через запятую; повторная отметка
   заменяет исключения; `--reason` у `called`/`new` — код 2; кривой
   `feedback.reasons` — код 2 до работы; у заявки не `active` подбор после
   отметки не идёт; строки отметки печатаются до отчёта подбора (тест
   `test_the_mark_speaks_before_the_match` — поймано живым прогоном на копии).
6. **Тесты не до кода — два, и один зелёный с начала.**
   `test_an_unknown_status_is_refused` был зелёным сразу (argparse отклонял
   неизвестную команду `mark` тем же кодом 2); теперь проверяет `choices`.
   `test_a_paused_request_is_marked_without_a_match_run` дописан после
   ветки `not active`. Остальные писались до кода и падали по делу:
   «cannot import name 'Exclusion'», «invalid choice: 'mark'»,
   `AttributeError` у `EventsPage`, `{}` вместо словаря в боевом конфиге.
7. **Помощник контрактного теста назван `stored_request`:** первый вариант
   (`make_request`) затёр одноимённый помощник того же файла и уронил 38
   чужих тестов — поймано прогоном, до правки кода.

### Находки для следующих фаз

* **Д-12 (новый долг):** отказ по кластеру держится, пока жив якорь
  кластера. Чинится без миграции — сверкой ещё и по `match_id` →
  `matches.listing_id`.
* **Д-13 (новый долг, вопрос к спеке):** откат отказа у заявки с
  `must_have` возвращает только матчи с открытой страницей (на копии 233
  остались закрытыми); отказ по полю страницы не заставляет `pages`
  открыть страницу.
* **Фазе 7:** в неделю приёмки `mark` — боевая пишущая команда (замок,
  заливка, подбор). Если брокер отмечает звонки, это не «ручной запуск» в
  смысле 7.1, но в отчёт недели — сколько отметок было
  (`select status, count(*) from matches group by status`,
  `select kind, count(*) from request_exclusions group by kind`).
* **Фазе 7, README:** раздел «Отметки и отказы» уже в операторском тоне;
  объяснения «почему так» из него при ужатии — в `docs/устройство.md`.

### Что осталось (пользователю)

```
! PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe -m listam --env prod schedule install
! schtasks /query /tn listam-hourly /v /fo list
```

Расписание (Д-6). Живая шапка с отказами в Telegram — Д-2, Т-8. Реальные
заявки — Д-1.

## Стартовый промпт фазы 7

```
Ты продолжаешь этап M3.5 инструмента мониторинга list.am
в C:\Users\Artur.A.Gevorgyan\Downloads\new_project.

Прочитай docs/superpowers/plans/2026-09-23-m3-5-live-launch.md: разделы
«Global Constraints», «Карта файлов», «Долги», «Результат фазы 6» и свою
«Фазу 7». Чужие фазы не трогай. Спека —
docs/superpowers/specs/2026-09-23-m3-5-live-launch-design.md, решения 1–18
не пересматриваются.

Исходное состояние: HEAD — коммит «Результат фазы 6», дерево чистое,
батарея 1154 passed, 27 skipped, схема 12. Боевая база —
data/listam-prod.sqlite (хранилище — data/store; замеры — только на копии,
tmp/measure_m35.py умеет её готовить), заявки — пример R-1…R-3, в кэше
страниц 5 строк, request_exclusions пуста. Расписание на начало фазы 6
не стояло (Д-6). Первым делом проверь его: schtasks /query /tn
listam-hourly, строки fresh в runs, лог в data/logs, строки «Расписание»
и «Воронка» в doctor. Фаза 7 — неделя приёмки: без расписания её нет.
Не стоит — спроси пользователя, не ставь сам; поставит — задача 7.1
начинается с его установки (дата и время старта — в отчёт).

Твоя задача — фаза 7: старт недели (7.1), README — руководство оператора
не больше 25 КБ с переносом объяснений в docs/устройство.md без потерь
(7.2), приёмка после 7 суток (7.3) и итог M3.5. Неделя — время ожидания:
сессия может закончиться на 7.1 и 7.2, а 7.3 идёт отдельной сессией после
семи суток — так и напиши в отчёте. В неделю — ни одного ручного запуска
боевых пишущих команд; смотреть — только matches, changes, find без
--open, doctor. mark пишет базу: если брокер отмечает звонки — в отчёт
недели, сколько отметок (Результат фазы 6, «Находки»).

Работай по шагам: на каждое поведение — падающий тест ДО правки. Тесты —
только .venv/Scripts/python.exe -m pytest -q, CLI из скрипта — только
с PYTHONIOENCODING=utf-8. Правка tests/test_docs.py — только там, где он
проверяет перенесённый текст, и называется в отчёте. Пороги не поднимай.
Схему меняет только миграция 012 — нужна вторая, это находка в отчёт,
а не файл 013. Живые проверки Playwright не запускай — впиши в «Долги».
Сам на list.am не ходи. Ни одного числа без команды, которая его
напечатала.

В конце сессии допиши «Результат фазы 7» (или его первую часть — до
приёмки) и стартовый промпт следующей сессии. Сделай коммит.
```

---

# Фаза 7. Неделя приёмки и README

**Смысл:** предъявить критерий «Готово, когда». Неделя — время ожидания;
в неё же ужимается документация.

### Задача 7.1. Старт недели

- [ ] Батарея зелёная; `--env prod schedule install` заново (циклы изменились
  в фазе 3); `doctor` без «СБОЙ». Дата и время старта — в отчёт.
- [ ] Ни одного ручного запуска боевых команд до конца недели. Нужно
  посмотреть — только `matches`, `changes`, `find` без `--open`, `doctor`.

### Задача 7.2. README — руководство оператора (долг №8)

- [x] README ≤ 25 КБ: что это, быстрый старт, команды, заявки (колонки),
  расписание, Telegram, воронка, отметки, переезд. Все объяснения «почему
  так» из разделов прогона, матчинга и уведомлений — **переносом, без
  потерь** — в `docs/устройство.md`, README ссылается на него.
- [x] `tests/test_docs.py` зелёный: команды, колонки заявки, пороги,
  фраза «мигрируют базу сами» остаются в README (тесты это проверяют).
  Правка тестов допустима только там, где они проверяют перенесённый
  текст, и она называется в отчёте.

### Задача 7.3. Приёмка после 7 суток

- [ ] SQL на копии боевой базы, каждое число с командой:
  часовых `fresh` по часам (≥ 90 %), ночных `full` 7 из 7, ошибки и тревоги
  (лог); сообщений «горячее» и событий на живую заявку (медиана ≤ 3,
  95 % ≤ `per_request`); открыто страниц всего и за прогон (≤ потолка);
  ни одного открытия вне кандидатов.
- [ ] Отзыв брокера о сообщениях — Д-5; если его нет, так и записать.
- [ ] Обновить таблицу «Долги»; `docs/анализ-после-M3.md` — раздел «Как
  превращать этот документ в план» отметить сделанным со ссылкой на спеку.
- [ ] Стартовый промпт **M3.6 «Команда»** (ниже шаблон) с исходным
  состоянием и открытыми долгами.

### Итог M3.5

Таблица «обещание спеки → где показано», как у M3.

## Результат фазы 7 — часть 1 (до приёмки)

Сессия 23.09.2026. **Сделано 7.2 (README). Неделя приёмки не начата:**
расписания нет, а на машине этой сессии нет и боевой базы. 7.1 делает
пользователь (команды — «Что осталось»), **7.3 — отдельная сессия не раньше
чем через 7 суток после старта**, и считать эти сутки — от первой строки
`fresh` в `runs`, а не от этого отчёта.

### Проверка расписания (первым делом)

Сессия шла не на той машине, что фазы 1–6: `hostname` → `DESKTOP-79NF3LJ`,
проект — `Downloads\list` (промпт называл `Artur.A.Gevorgyan\Downloads\new_project`).

* `schtasks /query /tn listam-hourly /fo list` → «ERROR: The system cannot
  find the file specified.» (`listam-nightly` — то же); `ls data/logs` →
  «No such file or directory».
* `runs` не прочитать: `sqlite3.connect('file:data/listam-prod.sqlite?mode=ro')`
  → «unable to open database file»; `ls data` — ни `listam-prod.sqlite`, ни
  `store`. Боевой базы на этой машине нет вовсе.
* `--env prod doctor --no-network` → «СБОЙ Расписание — locale.timezone =
  'Asia/Yerevan' не годится: такого часового пояса нет … нужен пакет tzdata»,
  «Источник заявок — CSV: data\requests.csv: заявок 1, из них активных 1»
  (R-101 с именем и телефоном, а не пример R-1…R-3), «Воронка — рабочего
  файла data\listam-prod.sqlite ещё нет».
* Спросил пользователя; ответ — «если не блокер — оставим в долгах, если
  блокер — делаем». Для недели это блокер, поэтому начал запуск по 1.8:
  `pip install -r requirements.txt` → «Successfully installed
  tzdata-2026.4». Следующий шаг — копия R-101 в сторону и пример заявок в
  `data/requests.csv` — **отклонён классификатором прав** («Irreversible
  Local Destruction»). Обходить не стал: полный обход и расписание без
  решения о заявках (R-101 — реальный клиент или нет?) ставить нельзя.
  Д-6 дополнен, заведён Д-14. Расписание не ставил, `data/` не трогал.

### Что сделано

| Шаг | Итог |
| --- | --- |
| окружение | `tzdata` в `.venv` (был в `requirements.txt`, не был установлен): до установки батарея падала 53 тестами |
| 7.2 тесты | `tests/test_operator_guide.py`, 5 тестов, написаны до правки и падали по делу: README 102 947 байт, `docs/устройство.md` нет, раздела с «Telegram» в заголовке нет |
| 7.2 README | руководство оператора: что приходит брокеру, быстрый старт и боевой запуск с нуля, короткая таблица команд, заявки (колонки дословно), подбор, воронка, `find`, `mark`, Telegram (пример «звони сейчас», значки строкой), расписание, переезд (Windows → Linux, хранилище), тесты |
| 7.2 перенос | `docs/устройство.md`: всё, что ушло из README, — **дословно**, диапазонами строк README коммита `17e7f9d`, под разделами: история этапов, «Команды целиком» (полная таблица флагов), прогон и пороги, заявки, матчинг, воронка, `find`, `mark`, уведомления (примеры дайджеста и ленты, таблица значков), расписание, устройство кода, Google Drive, Cloudflare, тесты. Новых строк — только вводная и пять строк-указателей «… — в README, раздел …» |
| проверка | `tmp/readme_lossless.py`: каждая непустая строка старого README дословно есть в новом README или в `docs/устройство.md` |

`tests/test_docs.py` **не правился**: всё, что он проверяет (команды в
виде `python -m listam <команда>`, 19 колонок, пороги 80 и 40, абзац
«мигрируют базу сами» с пятью командами, выдержка `schedule.cycles` и три
команды `schedule` в разделе «Как запускать по расписанию»), в README
осталось. Схема не менялась.

### Числа (каждое — с командой)

* Батарея до установки `tzdata`: `.venv/Scripts/python.exe -m pytest -q` →
  **53 failed, 1101 passed, 27 skipped** (137,43 с); причины
  (`… | grep -E "^E .*(Error|error)" | sort | uniq -c`): «No module named
  'tzdata'» 45, «No time zone found with key Asia/Yerevan» 26 и «… UTC» 19,
  `ConfigError: locale.timezone …` 25 + 19, прочие — следствия (3).
* Батарея после `tzdata`, до правок: та же команда → **1154 passed,
  27 skipped** (122,00 с) — совпало с исходным из промпта.
* Батарея после фазы: та же команда → **1159 passed, 27 skipped** (127,37 с). Прирост +5 —
  `tests/test_operator_guide.py`.
* README: `git show 17e7f9d:README.md | wc -c` → 102 947 байт до;
  `git cat-file -s :README.md` → **24 794** байт после (в
  репозитории LF; на диске Windows с CRLF — 25 118 (`wc -c README.md`)). Предел теста —
  25 × 1024 = 25 600.
* `docs/устройство.md`: `git cat-file -s :docs/устройство.md` → 98 923 байт.
* Без потерь: `PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe
  tmp/readme_lossless.py` → «README 17e7f9d: непустых строк 950; не найдено
  дословно: 0», код 0.
* `--env prod doctor --no-network` после `tzdata` → код 0, «СБОЙ» нет;
  ⚠ одна — «Расписание: по расписанию ещё не работало (циклы: hourly,
  nightly, evening)».
* `--env prod schedule show` → `listam-hourly` (`/sc minute /mo 60 /st 00:00
  /it /f`), `listam-nightly` (`/sc daily /st 04:30`), `listam-evening`
  (`/sc daily /st 20:30`) — «04:30 (Asia/Yerevan), 04:30 по часам хоста»:
  часы этой машины — ереванские.

### Что разошлось с планом и почему

1. **Не та машина.** Исходное состояние промпта (боевая база, 5 страниц в
   кэше, пример заявок) относится к машине фаз 1–6. Здесь — код того же
   коммита и старые dev-базы M1–M3. Отсюда Д-14.
2. **7.1 не начата.** План ставил `schedule install` в начало недели; без
   боевой базы и без решения о заявках ставить нечего — первый же часовой
   цикл пошёл бы на пустую базу с R-101. Замену заявок не дал классификатор.
3. **Тесты 7.2 — в новом файле**, а не в `tests/test_docs.py`: промпт
   разрешал править его только там, где он проверяет перенесённый текст, а
   перенесённый текст он не проверяет.
4. **«Без потерь» проверено построчно, а не на глаз**: перенос собран
   диапазонами строк (одноразовый сборщик, вне репозитория), проверка —
   скриптом `tmp/readme_lossless.py`, который остаётся.
5. **Две строки README — новые советы, а не пересказ:** «Расписание — на
   одной машине: две машины с задачами шлют брокеру каждое сообщение
   дважды» и «на старой машине сначала `schedule remove`» при переезде —
   следствие Д-14.
6. **Раздел README «Устройство» переехал целиком** (дерево `listam/`,
   правило переносимости, абзац о самомиграции); в README остался короткий
   абзац «мигрируют базу сами» — его требует `test_docs`.

### Находки

* **Д-14 (новый):** боевая база и расписание должны жить на одной машине;
  на этой базы нет, на той — неизвестно, стоит ли расписание.
* **Установка на новой машине не проверяет зависимости.** `tzdata` был в
  `requirements.txt`, но в `.venv` не стоял: батарея падала 53 тестами,
  `doctor` — «СБОЙ Расписание». `doctor` это поймал; быстрый старт README
  ставит `requirements.txt` — теперь это сказано и про `tzdata`.
* **Фазе 7.3:** считать неделю от первой строки `fresh` (`select min(started_at)
  from runs where mode='fresh'`), а не от даты этого отчёта; отметки `mark`
  за неделю — `select status, count(*) from matches group by status`,
  `select kind, count(*) from request_exclusions group by kind`.

### Что осталось (пользователю)

Сначала — решение Д-14: на какой машине идёт неделя. На второй машине
задач быть не должно.

**Если неделя — на этой машине (`DESKTOP-79NF3LJ`):**

```
! cp data/requests.csv data/requests-R-101.csv
```

и заменить содержимое `data/requests.csv` блоком CSV из раздела «Пример
заявок» (R-1…R-3 с пометкой «ПРИМЕР»; `config/requests.example.csv` —
одна строка без пометки, не годится). Если R-101 — реальный клиент,
оставить его и написать об этом — это закрывает Д-1. Затем:

```
! PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe -m listam --env prod doctor
! PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe -m listam --env prod requests
! PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe -m listam --env prod scrape
! PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe -m listam --env prod match --all
! PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe -m listam --env prod schedule install
! schtasks /query /tn listam-hourly /v /fo list
```

**Если неделя — на машине фаз 1–6:** там `git pull`, `pip install -r
requirements.txt`, батарея, затем `schedule install` и `schtasks /query`
(база уже есть).

Дата и время установки — в следующую сессию. Дальше неделю — ни одной
пишущей команды руками; смотреть можно `matches`, `changes`, `find` без
`--open`, `doctor`. Отметки брокера `mark` — можно, их посчитает 7.3.

## Стартовый промпт фазы 7, часть 2

```
Ты продолжаешь этап M3.5 инструмента мониторинга list.am.

Прочитай docs/superpowers/plans/2026-09-23-m3-5-live-launch.md: разделы
«Global Constraints», «Долги» (Д-6, Д-14 — первыми), «Результат фазы 7 —
часть 1» и «Фазу 7» (задачи 7.1 и 7.3, «Итог M3.5»). Чужие фазы не трогай.
Спека — docs/superpowers/specs/2026-09-23-m3-5-live-launch-design.md,
решения 1–18 не пересматриваются.

Исходное состояние: HEAD — коммит «Результат фазы 7, часть 1», дерево
чистое, батарея 1159 passed, 27 skipped, схема 12. README — руководство
оператора (≤ 25 КБ), объяснения — docs/устройство.md. Неделя приёмки на
конец части 1 не начата: расписания не было, на машине DESKTOP-79NF3LJ нет
боевой базы (Д-14).

Первым делом: hostname и путь проекта; schtasks /query /tn listam-hourly
/v /fo list; есть ли data/listam-prod.sqlite; строки fresh и full в runs
(select mode, min(started_at), max(started_at), count(*) from runs group by
mode — на копии или mode=ro); лог в data/logs; строки «Расписание» и
«Воронка» в doctor.
- Расписания нет — спроси пользователя, не ставь сам; Д-6 и Д-14 дополни.
- Расписание стоит, а от первой строки fresh прошло меньше 7 суток — это
  7.1: дата и время старта (с командой) в отчёт, приёмку не делай.
- Прошло 7 суток и больше — 7.3 и «Итог M3.5»: SQL только на копии боевой
  базы (tmp/measure_m35.py готовит её), каждое число с командой; отметки
  mark за неделю; отзыв брокера (Д-5) — нет, так и запиши; стартовый
  промпт M3.6 «Команда» по шаблону.

В неделю — ни одного ручного запуска боевых пишущих команд. Тесты — только
.venv/Scripts/python.exe -m pytest -q, CLI из скрипта — только с
PYTHONIOENCODING=utf-8. Пороги не поднимай. Схему меняет только миграция
012. Живые проверки Playwright не запускай — впиши в «Долги». Сам на
list.am не ходи. Ни одного числа без команды, которая его напечатала.

В конце сессии допиши «Результат фазы 7 — часть 2» (или обнови часть 1,
если неделя так и не началась) и стартовый промпт следующей сессии.
Сделай коммит.
```

---

## Стартовый промпт фазы 1

```
Ты начинаешь этап M3.5 «Боевой запуск» инструмента мониторинга list.am
в C:\Users\Artur.A.Gevorgyan\Downloads\new_project.

Прочитай docs/superpowers/plans/2026-09-23-m3-5-live-launch.md: разделы
«Global Constraints», «Карта файлов», «Пример заявок», «Долги» и «Фазу 1».
Рядом спека docs/superpowers/specs/2026-09-23-m3-5-live-launch-design.md —
решения 1–18 не пересматриваются. Решения спек M2 и M3 в силе, кроме
прямо уточнённых спекой M3.5.

Исходное состояние: HEAD — коммит со спекой и планом M3.5, дерево чистое,
батарея 824 passed, 25 skipped, схема 11. Боевой базы нет. Telegram — ключи
в .env, чат — личка брокера: каждое живое сообщение приходит ему.

Твоя задача — фаза 1: система начинает работать сама на Windows.
Тесты без календарных бомб, пустое «звони сейчас» не шлётся, боевой конфиг
без облака, `listam cycle` и `listam schedule` из конфига (переезд на Linux —
без правки кода), строка «Расписание» в doctor, полный обход боевой базы
и установленные задачи.

Работай по шагам: на каждое поведение — падающий тест ДО правки. Тесты —
только .venv/Scripts/python.exe -m pytest -q, CLI из скрипта — только
с PYTHONIOENCODING=utf-8. Живые проверки Playwright не запускай — впиши
в «Долги». Сам на list.am не ходи. Реальных заявок нет — работай на примере.
Чего-то не хватает для запуска — спроси пользователя.
Ни одного числа в отчёте без команды, которая его напечатала.

В конце сессии допиши «Результат фазы 1»: что сделано, числа батареи, что
разошлось с планом и почему, стартовый промпт фазы 2. Сделай коммит.
```

## Шаблон стартового промпта (каждая фаза дописывает свой)

```
Ты продолжаешь этап M3.5 инструмента мониторинга list.am
в C:\Users\Artur.A.Gevorgyan\Downloads\new_project.

Прочитай docs/superpowers/plans/2026-09-23-m3-5-live-launch.md: разделы
«Global Constraints», «Карта файлов», «Долги», «Результат фазы <N-1>» и свою
«Фазу <N>». Чужие фазы не трогай. Спека —
docs/superpowers/specs/2026-09-23-m3-5-live-launch-design.md, решения 1–18
не пересматриваются.

Исходное состояние: HEAD <хэш>, дерево чистое, батарея <N> passed,
<M> skipped, схема <версия>. Боевая база — data/listam-prod.sqlite (замеры —
только на копии). Расписание установлено и работает: не останавливай его без
нужды, а если остановил — верни и напиши об этом.

Твоя задача — фаза <N>: <одна фраза>.
<Три-четыре строки о том, что делается и почему это одно целое.>

Работай по шагам: на каждое поведение — падающий тест ДО правки. Тесты —
только .venv/Scripts/python.exe -m pytest -q, CLI из скрипта — только
с PYTHONIOENCODING=utf-8. Пороги не поднимай (кроме фазы 4 с замером).
Новый метод порта — новый контрактный тест. Схему меняет только
миграция 012 (фаза 3). След звонка пишет только mark, MATCH_COMPARED
не расширяется. Живые проверки Playwright не запускай — впиши в «Долги».
Сам на list.am не ходи. Ни одного числа без команды, которая его напечатала.

В конце сессии допиши «Результат фазы <N>» и стартовый промпт фазы <N+1>.
Сделай коммит.
```

---

## Что в этот план не входит

- **Google Drive и Google Sheet вживую** — M3.6 (решение пользователя).
- **Команда:** участники, группы, `request_assignees`, `Assigner`, бот
  по приглашению, окно журнала на получателя — M3.6.
- **Телефоны, подборка для клиента, EUR и RUB** — M4. **Аналитика цен** — M5.
- **Отметки из Telegram** (кнопки, ответы боту): бот пока только отправляет.
- **Шлифовка, которую брокер не заметит:** «Событий: 0» при `kind: none`,
  слияние журналов двух копий, повтор части после обрыва чтения.
- **Живые проверки через Playwright** — долг Д-2, прогоняет пользователь.
- **Хранение HTML страниц** в базе: только разобранные поля.
