# Разбор QA-аудита M1: план починки

> **Для исполнителя:** шаги отмечаются чекбоксами (`- [ ]` → `- [x]`) по мере выполнения.
> Каждая фаза выполняется **в отдельной сессии**. Фаза заканчивается разделом
> «Результат фазы N» в этом файле и стартовым промптом для следующей сессии —
> следующая сессия читает отчёт предыдущей отсюда, а не из чата.
> Порядок фаз менять нельзя: фаза 2 переписывает пороги, на которые смотрит фаза 3,
> а фаза 5 трогает боевую базу уже починенным кодом.

**Цель:** закрыть 13 находок строгого QA-аудита M1. Главная из них — F-01: при том
расписании, которое README сам и предлагает (`--fresh` раз в час, полный обход раз
в сутки), раздел «Снято» в `changes` человек не увидит **никогда**. Пометка снятых
работает, колонка в `.xlsx` заполняется, а витрина дельты о ней молчит — треть критерия
M1 предъявлена коду, но не человеку.

**Подход:** на каждую находку — падающий тест **до** правки. Ни одна находка не требует
похода на сайт: всё воспроизводится на фикстурах и временных базах. Сначала чинится код
(фазы 1–4), и только потом (фаза 5) починенным кодом трогается боевая база и живая лента.

**Стек:** Python 3.12, BeautifulSoup + lxml, SQLite, openpyxl, pytest.

**Аудит:** сессия 21.09.2026, прогон по коду M1 + зонды на временных базах. Все находки
воспроизведены, для каждой ниже записано, чем именно.
**План M1:** `docs/superpowers/plans/2026-09-21-m1-delta-and-history.md`
**План M0:** `docs/superpowers/plans/2026-09-21-m0-parser-crawl.md`
**Разбор QA-аудита M0:** `docs/superpowers/plans/2026-09-21-m0-qa-fixes.md`
**Спека:** `list.am → заявки покупателей MVP-спека и роадмап.md`

## Исходное состояние (снято 21.09.2026 перед началом работ)

| Что | Значение |
| --- | --- |
| Батарея тестов | 363 passed, 12 skipped |
| HEAD | `2e13f8a` «docs: M1 закрыт — инкрементальный прогон, снятые объявления, дельта» |
| `git status` | чисто |
| Боевая база | `data/listam.sqlite`, `schema_version = 4`, `listings 20 798` (20 636 `active`, 162 `gone`), `price_history 20 867`, `runs 6` |
| Прогон 5 | `full`, 215 страниц, новых 222, сменили цену 68, снято 186, ошибок 0 |
| Прогон 6 | `fresh`, 6 страниц, новых 7, ошибок 0 |
| Колонок в `.xlsx` | 22, автофильтр `A1:V` |

Прогоны 1–4 — это M0, у них `mode IS NULL`; код читает их как `full` через
`IFNULL(mode, 'full')`. Это важно для фазы 2: любая правка режимов обязана оставить
их полными, иначе мерка полноты `last_successful_run` на боевой базе провалится
в прогон 4, а проверка недобора страниц замолчит.

## Общие ограничения (из спеки и планов M0/M1, нарушать нельзя)

- Пагинация только путевая: `/ru/category/60/2`. Query-параметры фильтров запрещены `robots.txt`.
- Пауза между запросами — из конфига (`scrape.delay_seconds`). Параллельных пачек нет.
- Все отметки времени в UTC, пути относительные от корня проекта.
- Ни один путь, ключ, идентификатор и имя адаптера не зашит в код: внешнее — за портом,
  выбор — в конфиге, секреты — только в `.env`, подключение — в `listam/wiring.py`.
- Схема базы меняется **только** новой версионированной миграцией в `listam/migrations/`.
- Новый метод порта — это новый контрактный тест в `tests/contracts/`.
- Парсер не падает из-за отсутствия поля: нет поля — `None`.
- Правки отлаживаются на `tests/fixtures/` и временных базах, а не походом на сайт.
- Телефоны (`contacts`) — M4, заявки и матчинг (`requests`, `matches`) — M2. Не трогаем.
- **Пороги в конфиге не поднимаются, чтобы тест позеленел.** Порог, который мешает, —
  это либо находка, либо решение; и то и другое записывается, а не обходится.

**Запуск тестов — интерпретатором окружения проекта:**

```bash
.venv/Scripts/python.exe -m pytest -q
```

Системный python не годится: в нём нет openpyxl.

**Вывод на русском под Windows:** дочерний процесс кодирует stdout в cp1252 и глотает
кириллицу целиком. Любой прогон CLI из скрипта — только с `PYTHONIOENCODING=utf-8`
(это уже записано отклонением 3 фазы 5 плана M1).

Числа «ожидается N passed» в шагах — арифметика от 363 плюс тесты этого плана.
Разошлось на один-два — не повод подгонять: сверь, что именно добавилось, и поправь
число в плане.

## Находки аудита

Severity: **Б** — блокер (ломает критерий M1 или портит данные), **С** — средний,
**Н** — низкий.

| № | Severity | Находка | Чем воспроизведена |
| --- | --- | --- | --- |
| F-01 | Б | `changes` теряет раздел «Снято» после первого же следующего прогона: окно считается от начала **последнего** прогона, а снятых ставит только **полный** обход. При расписании из README (`--fresh` раз в час, полный раз в сутки) человек не увидит снятых никогда | зонд: полный обход → пометка → `changes` показывает 1 снятое; `scrape --fresh` → `changes` показывает 0 |
| F-02 | Б | `scrape.max_gone_percent: 0` не выключает пометку, а **снимает предохранитель**: `gone_refusal` возвращает `None`, и оборванный обход помечает снятой всю базу | `gone_refusal(missing=9, active_total=10, max_percent=0)` → `None` |
| F-03 | Б | `--max-pages 0` превращается в **полный** обход: `limit = int(limit) if limit else None` съедает ноль. Прогон записывается как `mode=full`, становится меркой полноты и помечает снятых | зонд: `run_scrape(max_pages=0)` → `mode=full`, пройдены все страницы |
| F-04 | С | Второй `--resume` откатывается на страницу первого: `last_run(mode="full")` всегда отдаёт один и тот же оборванный полный прогон, и страницы, пройденные первым продолжением, выбрасываются | зонд: полный встал на 100, `--resume` дошёл до 150 и встал → второй `--resume` снова идёт со 101 |
| F-05 | С | `changes` двоит карточку, сменившую цену дважды в окне, и счётчик «Сменили цену» считает **точки истории**, а не объявления. На боевой базе это уже видно: `changes --hours 24` говорит 69, `runs.price_changed` прогона 5 — 68 | зонд: две смены цены одной карточки → две строки; боевая база: 69 против 68 |
| F-06 | С | Возврат объявления с того света не считается ничем: `upsert_listing` ставит `status='active'`, `gone_at=NULL` и возвращает `"unchanged"`. Ни счётчика в `runs`, ни раздела в `changes`. На боевой базе таких **24 из 186** | зонд: пометить снятым → обход → `new=0`, `updated=0`, статус `active` |
| F-07 | С | `scrape.max_pages` в конфиге навсегда выключает пометку снятых и проверку недобора: все прогоны становятся `partial`, `last_successful_run()` пуст. Молча | зонд: `max_pages: 2` в конфиге → `mode=partial` у обычного `scrape` |
| F-08 | С | `scrape.fresh_stop_after_known_pages: 0` превращает `--fresh` в полный обход под меткой `fresh`: условие остановки отключается, а вместе с ним — и пометка снятых, и проверка недобора | зонд: порог 0 → обход дошёл до конца ленты, `errors=0`, `mode=fresh` |
| F-09 | Н | Сообщение об оборвавшемся обходе печатает человеку `scrape.max_pages = None` | зонд: лента в одну страницу → нота с `= None` |
| F-10 | Н | `--fresh --max-pages 1` объясняет отказ ключом `scrape.fresh_max_pages = 1`, которого человек не задавал, и записывает прогон ошибкой с отказом от заливки | зонд: `run_scrape(fresh=True, max_pages=1)` → `errors=1` |
| F-11 | Н | `changes` не проверяет ввод: `--hours -5` берёт окно **в будущем**, `--limit 0` печатает «…и ещё 7» вместо строк, `--limit -1` молча режет последнюю | прогоны CLI на боевой базе |
| F-12 | Н | Колонка «Статус» в `.xlsx` печатает `active` / `gone` латиницей, когда соседние `Продавец`, `Новостройка`, `Проверено` переведены | чтение `exporter_xlsx.COLUMNS` и `_present` |
| F-13 | Н | `doctor` ничего не знает про пороги M1: опасные значения из F-02, F-07 и F-08 не видны, пока не навредят | чтение `listam/doctor.py` |

**Что аудит проверил и претензий не имеет** (переделывать не надо): миграция 004 и её
накат на схему 3 с данными; отказ `changes` и `export` на младшей схеме с кодом возврата 1;
`--fresh` и `--resume` вместе — код 2; `mark_gone` не двигает `last_seen` и бережёт первую
дату снятия через `COALESCE`; `gone_at` отдельной колонкой и обнуление при возврате;
22 колонки и `A1:V` в выгрузке; `IFNULL(mode,'full')` для прогонов M0; отсутствие пути,
на котором `_row_to_run` читал бы колонки, которых нет в младшей схеме; скорость `changes`
на боевой базе (2,8 с на 20 798 строк).

**Находки 1 и 2 отчёта фазы 5 плана M1** (голова ленты сортируется по свежести хуже
ожидаемого; 13% пометок «снято» самоисправляются) аудит подтверждает и повторно не
предъявляет: они записаны честно и лечения в M1 не требуют. F-06 — их следствие:
самоисправление работает, но нигде не видно.

## Принятые решения (в фазах не пересматриваются)

1. **Окно дельты считается по виду изменения, а не одной меркой на всё.** Новое и цены —
   с начала последнего прогона (он их и приносит). Снятые — с начала последнего прогона,
   **который имел право их пометить**, то есть последнего `full`. Так раздел «Снято»
   переживает часовые `--fresh` и гаснет только со следующим полным обходом.
2. **Ноль в пороге значит «ноль», а не «выключено».** Порог, который хотят выключить,
   выключается `null`, а не нулём. Это касается `max_gone_percent`,
   `fresh_stop_after_known_pages`, `fresh_max_pages`, `max_pages`. Ноль, прочитанный
   как «предохранителя нет», уже стоил бы базы.
3. **Бессмысленный ввод отклоняется на входе, а не истолковывается.** `--max-pages 0`,
   `--max-pages -1`, `--hours -5`, `--limit 0` — это отказ с кодом возврата 2 и внятным
   текстом, а не обход в 215 страниц и не пустой список.
4. **`--resume` продолжает обход, а не прогон.** Мерка — последний прогон, который шёл
   по **полной** ленте: это `full` и продолжающие его `resume`. Выбирается из них
   максимальный `last_page`, а не последняя строка журнала.
5. **Возврат с того света — отдельный исход апсерта** `"returned"`, отдельный счётчик
   `runs.returned` и отдельный раздел в `changes`. Считать его обновлением нельзя:
   «объявление вернулось на рынок» — это событие рынка, а не правка поля.
6. **Одна карточка — одна строка в разделе «Цены».** Показывается цена на начало окна
   и цена на конец; промежуточные точки схлопываются. Счётчик «Сменили цену» считает
   объявления, и тогда он сходится с `runs.price_changed`.
7. **`doctor` предупреждает, но не чинит.** Опасный порог — это `⚠` в отчёте и
   ненулевой код возврата на явном вредительстве (`max_gone_percent: 0`), но конфиг
   `doctor` не правит: конфиг — территория человека.

## Распределение находок по фазам

| Фаза | Находки | Про что | Готово, когда |
| --- | --- | --- | --- |
| 1 | F-01, F-05, F-11 | Витрина дельты: окно, схлопывание цен, проверка ввода | `changes` показывает снятых после часового `--fresh`; карточка в «Ценах» одна; дурной ввод отклонён кодом 2 |
| 2 | F-02, F-03, F-07, F-08 | Пороги и режимы: ноль значит ноль, ввод проверяется | Ноль в любом из четырёх порогов не снимает предохранитель; `--max-pages 0` отклонён; `max_pages` в конфиге не делает прогон `partial` |
| 3 | F-04, F-06 | Продолжение обхода и возврат объявлений | Второй `--resume` идёт дальше первого; возврат считается счётчиком и виден в `changes` |
| 4 | F-09, F-10, F-12, F-13 | Сообщения, выгрузка, `doctor` | В нотах нет `None` и чужих ключей; «Статус» по-русски; `doctor` показывает пороги M1 |
| 5 | — | Боевая база, живая лента, документация | Полный обход и `--fresh` через час на починенном коде, `changes` со всеми четырьмя разделами, README и контрольный список |

---

## Фаза 1. Витрина дельты (F-01, F-05, F-11)

**Зачем:** ради этой фазы аудит и затевался. Пометка снятых работает, а человек её
не видит: `changes` спрашивает «что принёс последний прогон», а снятых приносит только
полный — раз в сутки. Первый же часовой `--fresh` сдвигает окно, и раздел «Снято»
обнуляется до следующей ночи.

### Задача 1.1. Окно дельты считается по виду изменения

- [x] **Шаг 1. Падающий тест.**

```python
# tests/test_changes.py

def test_gone_survive_an_incremental_run(project):
    """Снятых ставит только полный обход, а `--fresh` ходит каждый час.
    Если окно двигает любой прогон, раздел «Снято» человек не увидит никогда."""
    run_scrape(project)                       # полный: набили базу
    database = opened(project)
    victim = next(iter(database.active_ids()))
    database.mark_gone([victim], gone_at=datetime.now(timezone.utc))
    database.close()

    run_scrape(project, fresh=True)           # обычный час спустя

    report = run_changes(project)
    assert [item.id for item in report.gone] == [victim]


def test_a_full_run_resets_the_gone_section(project):
    """Следующий полный обход — новая мерка: снятые прошлого в список не тянутся."""
    ...  # пометить, полный обход, убедиться, что список снятых пуст
```

- [x] **Шаг 2. Порт: спросить журнал про последний полный прогон, уже завершённый.**
  `last_successful_run()` не годится: он отдаёт только удачные, а снятых мог пометить
  и прогон, у которого ошибка случилась позже — при заливке.

```python
# listam/ports/database.py — новый метод, значит и новый контрактный тест
@abstractmethod
def last_run_that_could_mark_gone(self) -> Run | None:
    """Последний завершённый полный обход: только он ставит `status='gone'`.

    Мерка для раздела «Снято» в `changes`. Инкрементальный прогон, прошедший
    между ними, снятых не ставил, и двигать им окно — значит стереть раздел.
    """
```

```python
# listam/adapters/db_sqlite.py
"SELECT * FROM runs WHERE finished_at IS NOT NULL "
"AND IFNULL(mode, 'full') = 'full' ORDER BY id DESC LIMIT 1"
```

- [x] **Шаг 3. Контрактный тест** в `tests/contracts/test_database_contract.py`:
  прогоны `full` → `fresh` → `partial` в журнале, метод отдаёт `full`; журнал пуст —
  `None`; незавершённый `full` в выборку не попадает.

- [x] **Шаг 4. `Changes` получает вторую отметку и второй текст.**

```python
# listam/changes.py
@dataclass
class Changes:
    since: datetime | None = None          # новое и цены
    since_note: str = ""
    gone_since: datetime | None = None     # снятые: мерка другая
    gone_note: str = ""
    ...
```

`run_changes` берёт `gone_since` из нового метода порта; журнал пуст или полных
прогонов в нём нет — падаем на ту же отметку, что и остальное, и говорим об этом.
`--hours N` задаёт **обе** отметки: человек попросил окно в часах, и делить его
надвое незачем.

- [x] **Шаг 5. `render` объясняет вторую мерку** одной строкой под заголовком раздела
  «Снято», и только когда мерки разошлись: `снятые — с полного обхода 5 (16:07 UTC)`.

- [x] **Шаг 6.** `.venv/Scripts/python.exe -m pytest -q`. Ожидается 367 passed
  (363 + 2 в `test_changes.py` + 1 контрактный + 1 на пустой журнал), 12 skipped.

### Задача 1.2. Одна карточка — одна строка в «Ценах» (F-05)

- [x] **Шаг 1. Падающий тест.**

```python
# tests/contracts/test_database_contract.py
def test_two_moves_of_one_listing_collapse_into_one_row(database):
    """Человеку важно «было → стало» за окно, а не каждая точка истории.
    Две строки на одну квартиру он читает как две квартиры."""
    # три точки: 59 500 → 100 000 → 110 000 в одном окне
    rows = database.price_changes_since(since)
    assert len(rows) == 1
    assert rows[0][1] == 59_500      # цена на начало окна
    assert rows[0][2] == 110_000     # цена на конец окна
```

- [x] **Шаг 2. Переписать `price_changes_since`.** Сейчас запрос идёт по точкам
  и на каждую подтягивает предыдущую. Нужно — по объявлениям: для каждого `listing_id`,
  у которого в окне есть точка с предшественником, берём **последнюю** точку окна
  как «стало» и **последнюю точку до окна** как «было». Точка до окна есть не всегда
  (объявление появилось внутри окна) — такие строки по-прежнему отбрасываются:
  это появление, а не смена цены.

```sql
WITH moved AS (
  SELECT listing_id, MAX(id) AS last_id
    FROM price_history WHERE seen_at >= :since GROUP BY listing_id
)
SELECT m.listing_id,
       (SELECT price_usd FROM price_history WHERE id = m.last_id) AS new_price,
       (SELECT price_usd FROM price_history p
         WHERE p.listing_id = m.listing_id AND p.seen_at < :since
         ORDER BY p.id DESC LIMIT 1) AS old_price,
       EXISTS (SELECT 1 FROM price_history p
                WHERE p.listing_id = m.listing_id AND p.seen_at < :since) AS has_previous
  FROM moved m
```

**Осторожно:** `seen_at` — текст, сравнение строковое. Все отметки пишет `to_iso`
в одном формате (`+00:00`), и это работает; но менять формат в этой фазе нельзя.

- [x] **Шаг 3. Тест на границу:** объявление, появившееся внутри окна и сменившее
  цену внутри же, в «Цены» не попадает — оно уже в «Новых».

- [x] **Шаг 4. Тест на счётчик:** после прогона со сменой цены
  `len(changes.moved) == run.price_changed`. Это та самая пара 69/68 с боевой базы.

- [x] **Шаг 5.** Тесты. Ожидается 370 passed.

### Задача 1.3. `changes` проверяет ввод (F-11)

- [x] **Шаг 1. Падающие тесты** в `tests/test_cli.py`: `changes --hours -5` → код 2
  и текст про то, что окно не бывает отрицательным; `changes --hours 0` → код 2;
  `changes --limit 0` и `--limit -1` → код 2.

- [x] **Шаг 2. Проверка в `cli.main`,** рядом с отказом `--fresh --resume`: разбор
  аргументов — дело командной строки, `run_changes` про коды возврата ничего не знает.

- [x] **Шаг 3.** Тесты. Ожидается 374 passed, 12 skipped.

- [x] **Шаг 4. Коммит.**

```bash
git add -A
git commit -m "fix(changes): снятые переживают инкрементальный прогон, цены не двоятся"
```

**Результат фазы 1**

Сделано всё, что просила фаза: F-01, F-05 и F-11 закрыты, на каждое поведение
тест написан до правки и посмотрен красным.

**Батарея:** было 363 passed, 12 skipped → стало **376 passed, 12 skipped**
(`.venv/Scripts/python.exe -m pytest -q`, 55 с). Добавлено 13 тестов.

**F-01 — снятые переживают инкрементальный прогон.**
Новый метод порта `Database.last_run_that_could_mark_gone()` и его реализация
в `db_sqlite`: последний **завершённый** полный обход, без фильтра по ошибкам —
пометка снятых случается в конце обхода, а ошибка чаще после неё, при заливке.
`Changes` получил вторую пару полей (`gone_since`, `gone_note`), `run_changes` —
функцию `gone_since_point`, `render` — строку под заголовком «Снято», которая
печатается только когда мерки разошлись. `--hours N` по-прежнему задаёт обе
отметки. Журнал без полных прогонов — падаем на общую мерку и говорим об этом.
Красный до правки: `[] == ['24100001']` — ровно то, что предъявил аудит.

**F-05 — одна карточка, одна строка.**
`price_changes_since` переписан с обхода точек истории на обход объявлений
(`WITH moved AS … MAX(id) … GROUP BY listing_id`): «было» — последняя точка
**до** окна, «стало» — последняя точка **в** окне. Строка без точки до окна
отбрасывается: объявление появилось внутри окна, это «Новое».
Красный до правки: `2 == 1` на трёх точках одной карточки.

**F-11 — бессмысленный ввод отклоняется.**
Проверка в `cli.main` рядом с отказом `--fresh --resume`: `--hours <= 0` и
`--limit <= 0` — код возврата 2 с внятным текстом. `run_changes` про коды
возврата по-прежнему ничего не знает. Проверка стоит **до** похода за базой:
красные тесты падали с кодом 1 «схема базы 0» — то есть команда успевала
залезть в хранилище прежде, чем отказать.

**Сверка на боевой базе (только чтение, `data/listam.sqlite` не тронута):**

| Мерка | Было | Стало |
| --- | --- | --- |
| снятых видно после `--fresh` (прогон 6) | 0 | **162** |
| `price_changes_since(старт прогона 5)` против `runs.price_changed` | 69 против 68 | **68 против 68** |

**Что разошлось с планом**

1. **Тестов на три больше, чем считала арифметика плана** (376 вместо 374).
   План считал 2 теста в `test_changes.py` + 1 контрактный + 1 на пустой журнал.
   Написано: 3 в `test_changes.py` (переживают `--fresh`; полный обход сбрасывает
   раздел; журнал без полных прогонов), отдельный тест на строку `render` про
   вторую мерку (шаг 5 плана — это поведение, а не оформление, и ему нужен свой
   тест) и **два** контрактных вместо одного: мерка при `full → fresh → идущий full`
   и пустой журнал. Плюс два контрактных на цены вместо одного: схлопывание
   и граница «родилось внутри окна».
2. **Тест «полный обход сбрасывает раздел Снято» зелёный с самого начала.**
   Это не находка, а предохранитель: он ловит противоположную ошибку — окно,
   уехавшее слишком далеко назад. Записано честно, а не подогнано.
3. **Первые два контрактных теста на цены сначала падали не по делу.**
   `make_listing` держит `price_raw` неизменным, и апсерт не видел смены цены:
   точек истории не появлялось вовсе (`0 == 1` вместо ожидаемого `2 == 1`).
   Тесты поправлены (`price_raw` задаётся явно, как в соседнем
   `test_a_price_change_carries_the_price_it_had_before`) и пересмотрены красными
   по правильной причине до правки кода.
4. **`changes --hours 24` на боевой базе теперь показывает 0 смен цены, а не 69.**
   Это не регрессия, а следствие решения 6: боевой базе меньше суток (первая точка
   истории — 09:06 того же дня), поэтому в окне 24 часа **все** объявления
   появились внутри окна, и цены на начало окна у них нет. Они целиком в разделе
   «Новые», и показывать их ещё и в «Ценах» — это и есть двойной счёт, из-за
   которого было 69 вместо 68. С меркой по умолчанию (начало прогона 5) счётчик
   сходится: 68 = 68. Проверить это на базе старше суток можно будет в фазе 5.
5. **Пороги конфига не тронуты** — как требуют общие ограничения.

**Чего фаза 1 намеренно не делала:** возврат объявления с того света (F-06)
в разделах `changes` по-прежнему не виден — это фаза 3; `max_gone_percent: 0`
по-прежнему снимает предохранитель — это фаза 2.

**Стартовый промпт для фазы 2**

> Ты продолжаешь работу над инструментом мониторинга list.am в `C:\Users\Admin\Downloads\list`.
>
> Прочитай `docs/superpowers/plans/2026-09-21-m1-qa-fixes.md` целиком: разделы
> «Общие ограничения», «Находки аудита», «Принятые решения», «Результат фазы 1»
> и «Фаза 2». Фазы 3–5 не трогай — они для следующих сессий.
>
> Исходное состояние: HEAD — коммит фазы 1 «fix(changes): снятые переживают
> инкрементальный прогон, цены не двоятся», дерево чистое, батарея
> **376 passed, 12 skipped**, боевая база на схеме 4, 20 798 объявлений
> (20 636 active, 162 gone), runs 6, не тронута.
>
> Твоя задача — фаза 2: пороги и режимы. Четыре находки:
>  F-02 (блокер) — `scrape.max_gone_percent: 0` не выключает пометку, а снимает
>    предохранитель: `gone_refusal` отдаёт `None`, и оборванный обход помечает
>    снятой всю базу.
>  F-03 (блокер) — `--max-pages 0` превращается в полный обход, записывается как
>    `mode=full`, становится меркой полноты и помечает снятых.
>  F-07 — `scrape.max_pages` в конфиге навсегда делает все прогоны `partial`:
>    снятых не помечает никто, `last_successful_run()` пуст, и всё это молча.
>  F-08 — `scrape.fresh_stop_after_known_pages: 0` превращает `--fresh` в полный
>    обход под меткой `fresh`.
>
> Работай по шагам задач 2.1, 2.2 и 2.3: на каждое поведение — падающий тест ДО
> правки. Тесты гоняй только `.venv/Scripts/python.exe -m pytest -q`.
> Пороги в конфиге не поднимаются, чтобы тест позеленел. Помни решение 2:
> ноль значит ноль, выключается порог значением `null`; исключение —
> `hard_page_limit: 0`, он остаётся «выключено», и это записывается комментарием
> в коде. Числа «ожидается N passed» в плане фазы 2 считаны от 374, а батарея
> сейчас 376 — сверяй, что именно добавилось, и правь число в плане, а не тесты.
>
> В конце сессии допиши в план раздел «Результат фазы 2»: что сделано, числа
> батареи, что разошлось с планом и почему, и стартовый промпт для фазы 3.
> Сделай коммит.

---

## Фаза 2. Пороги и режимы (F-02, F-03, F-07, F-08)

**Зачем:** три из четырёх находок этой фазы — молчаливые. Конфиг с `max_gone_percent: 0`
выглядит как «пометку выключили», а работает как «предохранитель сняли»; `max_pages: 2`
в конфиге выглядит как «пока ходим коротко», а работает как «снятых не помечаем никогда
и недобор не проверяем». Ни один из этих случаев не виден ни в журнале, ни в `doctor`.

### Задача 2.1. Ноль значит ноль (F-02, F-08)

- [x] **Шаг 1. Падающие тесты.**

```python
# tests/test_gone.py
def test_a_zero_threshold_forbids_marking_instead_of_allowing_everything():
    """Порог 0 — это «пропало хоть что-то, значит сбой», а не «предохранителя нет».
    Выключается порог значением null, а не нулём."""
    assert gone_refusal(missing=1, active_total=1000, max_percent=0) is not None

def test_a_null_threshold_turns_the_guard_off():
    assert gone_refusal(missing=999, active_total=1000, max_percent=None) is None
```

```python
# tests/test_crawler_fresh.py
def test_a_zero_stop_threshold_is_not_an_endless_crawl():
    """Порог 0 страниц без новых — это «встань на первой же такой странице»."""
    assert incremental_stop(pages_without_new=0, threshold=0,
                            pages_fetched=1, ceiling=20)[0] is not None
```

- [x] **Шаг 2. Разделить «ноль» и «не задано» на чтении конфига.** Сейчас
  `int(config.get(...) or 0)` схлопывает `null`, `0` и отсутствие ключа в один ноль.
  Завести в `listam/config.py` одну общую мерку и звать её отовсюду:

```python
def threshold(config, key: str, default):
    """Порог из конфига. `null` — выключено (None), число — число, в том числе 0.

    `or default` здесь нельзя: ноль — это заданное значение, и на порогах
    безопасности он означает самый строгий режим, а не отсутствие проверки.
    """
```

- [x] **Шаг 3. Поправить всех читателей порогов** в `crawler.run_scrape`:
  `max_gone_percent`, `fresh_stop_after_known_pages`, `fresh_max_pages`,
  `min_cards_per_page`, `max_cards_per_page`, `hard_page_limit`,
  `max_pages_drop_percent`, `expected_pages_min`. Проверить каждый: какой из них
  при нуле становится опаснее, а какой — строже. **`hard_page_limit: 0` должен
  оставаться «выключено»** — это не предохранитель данных, а защита от зацикленного
  пагинатора; записать это решением прямо в коде комментарием.

- [x] **Шаг 4. `gone_refusal` принимает `float | None`** и различает три случая:
  `None` — проверки нет; `0` — пропало хоть что-то, значит сбой; число — доля.

- [x] **Шаг 5. Тест на конфиг-по-умолчанию:** `config/dev.yaml` и `config/prod.yaml`
  читаются, все восемь порогов имеют осмысленные значения, ни один не ноль.

- [x] **Шаг 6.** Тесты. Ожидается 380 passed. → **Вышло 388 passed, 12 skipped** (арифметика плана считана от 374, а батарея после фазы 1 — 376; сверка добавленного — в отчёте фазы).

### Задача 2.2. `--max-pages` проверяется на входе (F-03)

- [x] **Шаг 1. Падающие тесты** в `tests/test_cli.py`: `scrape --max-pages 0` → код 2,
  `scrape --max-pages -1` → код 2, текст про то, что страниц должно быть хотя бы одна.

- [x] **Шаг 2. Тест на то, что ноль не проваливается в полный обход**
  в `tests/test_crawler.py`: `run_scrape(config, max_pages=0)` — это `ValueError`
  с внятным текстом, а не обход всей ленты. CLI ловит его раньше, но прогон зовут
  и из кода.

- [x] **Шаг 3. Правка:** `limit` считается через `threshold()` из задачи 2.1,
  ноль и отрицательные отвергаются в `run_scrape` до похода за курсом.

- [x] **Шаг 4.** Тесты. Ожидается 383 passed. → **Вышло 391 passed, 12 skipped.**

### Задача 2.3. `max_pages` из конфига не делает прогон укороченным (F-07)

**Решение:** режим смотрит на то, **просил ли человек укоротить обход**, а не на то,
есть ли в конфиге потолок. `scrape.max_pages` в конфиге — это рабочая настройка
окружения (в `prod.yaml` она `null`, но поставить её туда никто не запрещал), и она
не должна навсегда выключать пометку снятых.

- [x] **Шаг 1. Падающий тест.**

```python
# tests/test_gone.py
def test_a_config_ceiling_does_not_switch_off_marking(project):
    """`max_pages` в конфиге — потолок окружения, а не «человек укоротил обход».
    Пока каждый прогон partial, снятых не помечает никто и мерки полноты нет."""
    project.data["scrape"]["max_pages"] = 2      # ровно лента фикстуры
    run = run_scrape(project)
    assert run.mode == "full"
```

- [x] **Шаг 2. Правка `run_mode`:** принимает не `limit`, а `limit_asked` —
  значение флага `--max-pages`, а не итоговый потолок.

```python
def run_mode(*, fresh: bool, resume: bool, limit_asked: int | None) -> str:
```

- [x] **Шаг 3. Сторожевой тест:** конфиг с `max_pages: 1` и лента из двух страниц —
  обход проходит одну, `mode == "full"`, и **проверка недобора страниц срабатывает**
  (`pages_shortfall` против прошлого полного). Иначе мы просто перенесли дыру.
  Если проверка не срабатывает — это находка, её надо записать в отчёт фазы,
  а не обойти.

- [x] **Шаг 4. Тест на то, что `--max-pages 1` с командной строки по-прежнему
  `partial`** и снятых не помечает: правка не должна открыть обратную дыру.

- [x] **Шаг 5.** Тесты. Ожидается 386 passed, 12 skipped. → **Вышло 394 passed, 12 skipped.**

- [x] **Шаг 6. Коммит.**

```bash
git add -A
git commit -m "fix(scrape): ноль в пороге значит ноль, потолок из конфига не укорачивает режим"
```

**Результат фазы 2**

Сделано всё, что просила фаза: F-02, F-03, F-07 и F-08 закрыты, на каждое
поведение тест написан до правки и посмотрен красным.

**Батарея:** было 376 passed, 12 skipped → стало **394 passed, 12 skipped**
(`.venv/Scripts/python.exe -m pytest -q`, 54 с). Добавлено 18 тестов.

**Одна мерка порога на всех.** В `listam/config.py` появилась функция
`threshold(config, key, default)`: `null` — выключено (`None`), число — число,
в том числе 0, ключа нет — значение по умолчанию. Прежнее
`int(config.get(...) or 0)` схлопывало `null`, `0` и отсутствие ключа в один
ноль. В `crawler.run_scrape` через неё читаются все восемь порогов, а `_as_int`
и `_as_float` приводят их к числу, не трогая выключенное.

**F-02 — ноль в `max_gone_percent` запрещает пометку, а не разрешает всё.**
`gone_refusal` принимает `float | None` и различает три случая: `None` —
проверки нет, `0` — пропало хоть что-то, значит сбой, число — доля.
Красный до правки: `assert None is not None` и `TypeError` на `None <= 0`.

**F-08 — ноль в `fresh_stop_after_known_pages` останавливает на первой же
странице.** `incremental_stop` проверяет `threshold is not None` и
`ceiling is not None` вместо истинности. Красный до правки: `assert None is not None`.

**F-03 — `--max-pages 0` отклонён на входе.** Проверка в `cli.main` рядом с
отказом `--fresh --resume` (код возврата 2) и `ValueError` в `run_scrape` до
похода за курсом: прогон зовут и из кода. `limit` считается через `threshold()`,
`int(limit) if limit else None` убрано. Красный до правки: `assert 0 == 2` —
ноль проваливался в обход всей ленты.

**F-07 — потолок из конфига не укорачивает режим.** `run_mode` принимает
`limit_asked` — значение флага `--max-pages`, а не итоговый потолок. Туда же
переведена проверка недобора страниц (`if limit_asked is None …`), иначе дыру
просто перенесли бы. Красный до правки: `assert 'partial' == 'full'`.

**Что вышло с каждым из восьми порогов (шаг 3 задачи 2.1)**

| Порог | Что значит 0 теперь | Изменилось ли поведение |
| --- | --- | --- |
| `max_gone_percent` | пропало хоть что-то — сбой | **да**, F-02 |
| `fresh_stop_after_known_pages` | встань на первой же странице без новых | **да**, F-08 |
| `fresh_max_pages` | потолок 0 — обход сразу упирается, и это ошибка | да, тем же условием |
| `max_pages_drop_percent` | короче прошлого удачного быть не должно вовсе | **да**, новый тест |
| `expected_pages_min` | 0 страниц хватит — проверка не срабатывает | нет: `pages_fetched < 0` не бывает |
| `min_cards_per_page` | 0 карточек хватит — проверка не срабатывает | нет: `len(cards) < 0` не бывает |
| `max_cards_per_page` | любая карточка — перебор | да, но случай умозрительный |
| `hard_page_limit` | **выключено** — исключение из решения 2 | нет, и это записано комментарием в коде |

`hard_page_limit` оставлен «выключено» намеренно: это не предохранитель данных,
а защита от зацикленного пагинатора, и «потолок в ноль страниц» не значит ничего,
кроме «обхода не будет». Решение записано комментарием в `run_scrape` и
сторожевым тестом.

**Что разошлось с планом**

1. **Тестов 18, а не 12, и батарея 394, а не 386.** Арифметика плана считана
   от 374, а фаза 1 оставила 376. Сверх плана написаны: тест на `null` в пороге
   остановки, два теста на `max_pages_drop_percent` (ноль и `null`), сторожевой
   тест на `hard_page_limit: 0`, три теста на саму `threshold()` и сторожевой
   тест на то, что `--max-pages 1` по-прежнему `partial` и снятых не помечает.
   Числа в шагах фазы поправлены на фактические, тесты не тронуты.
2. **Три теста зелёные с самого начала** — и все три сторожевые, а не подогнанные:
   `null` в пороге остановки (истинность его и так глотала), `hard_page_limit: 0`
   как «выключено» (исключение из решения 2 — тест его сторожит) и
   `--max-pages 1` → `partial` (обратная дыра правки F-07). Записано честно.
3. **Тест на конфиги по умолчанию (шаг 5 задачи 2.1) вышел параметрическим**
   по `dev`/`prod` — это два теста, а не один, и он тоже зелёный с начала.
   Чтобы он не был украшением, его зубы проверены: `max_gone_percent` в
   `config/dev.yaml` временно поставлен в 0 — тест покраснел (`assert 0 != 0`),
   конфиг возвращён. **Пороги в конфиге не тронуты.**
4. **Сторожевой тест шага 3 задачи 2.3 сработал с первого раза:** после переноса
   условия на `limit_asked` конфиг с `max_pages: 1` при прошлом полном прогоне
   в 9 страниц даёт `mode == "full"` **и** ноту «обход оборвался». Новой находки
   здесь нет — дыра не перенесена.
5. **Прогоны M0 (`mode IS NULL`) не затронуты:** `IFNULL(mode,'full')` в
   `db_sqlite` не трогался, режим считается только при записи нового прогона.
   Мерка полноты на боевой базе по-прежнему видит прогоны 1–5.

**Чего фаза 2 намеренно не делала:** второй `--resume` по-прежнему откатывается
на страницу первого (F-04) и возврат с того света по-прежнему не считается
ничем (F-06) — это фаза 3; нота про `scrape.max_pages = None` (F-09) и отказ
по чужому ключу `fresh_max_pages` (F-10) остались как были, и `doctor` про
новые пороги пока молчит (F-13) — это фаза 4.

**Стартовый промпт для фазы 3**

> Ты продолжаешь работу над инструментом мониторинга list.am в `C:\Users\Admin\Downloads\list`.
>
> Прочитай `docs/superpowers/plans/2026-09-21-m1-qa-fixes.md` целиком: разделы
> «Общие ограничения», «Находки аудита», «Принятые решения», «Результат фазы 1»,
> «Результат фазы 2» и «Фаза 3». Фазы 4–5 не трогай — они для следующих сессий.
>
> Исходное состояние: HEAD — коммит фазы 2 «fix(scrape): ноль в пороге значит
> ноль, потолок из конфига не укорачивает режим», дерево чистое, батарея
> **394 passed, 12 skipped**, боевая база на схеме 4, 20 798 объявлений
> (20 636 active, 162 gone), runs 6, не тронута.
>
> Твоя задача — фаза 3: продолжение обхода и возврат объявлений. Две находки:
>  F-04 — второй `--resume` откатывается на страницу первого: `last_run(mode="full")`
>    всегда отдаёт один и тот же оборванный полный прогон, и страницы, пройденные
>    первым продолжением, выбрасываются.
>  F-06 — возврат объявления с того света не считается ничем: `upsert_listing`
>    ставит `status='active'`, `gone_at=NULL` и возвращает `"unchanged"`.
>    Ни счётчика в `runs`, ни раздела в `changes`. На боевой базе таких 24 из 186.
>
> Работай по шагам задач 3.1 и 3.2: на каждое поведение — падающий тест ДО правки.
> Тесты гоняй только `.venv/Scripts/python.exe -m pytest -q`. Помни ограничения:
> новый метод порта — это новый контрактный тест в `tests/contracts/`, схема базы
> меняется только новой версионированной миграцией в `listam/migrations/`,
> пороги в конфиге не поднимаются, чтобы тест позеленел. Учти решение 4
> (`--resume` продолжает обход: мерка — максимальный `last_page` среди `full`
> и продолжающих его `resume`) и решение 5 (возврат — отдельный исход апсерта
> `"returned"`, отдельный счётчик `runs.returned`, отдельный раздел в `changes`).
> Числа «ожидается N passed» в плане фазы 3 считаны от 374, а батарея сейчас 394 —
> сверяй, что именно добавилось, и правь число в плане, а не тесты.
>
> В конце сессии допиши в план раздел «Результат фазы 3»: что сделано, числа
> батареи, что разошлось с планом и почему, и стартовый промпт для фазы 4.
> Сделай коммит.

---

## Фаза 3. Продолжение обхода и возврат объявлений (F-04, F-06)

**Зачем:** `--resume` придуман для 215 страниц на плохой связи — и ровно в этом случае
ломается: второй обрыв откатывает обход на страницу первого. А возврат объявления
с того света — событие рынка, которое база переживает правильно, но нигде не показывает:
на боевой базе таких 24 из 186, и на M2 по ним будут считаться медианы.

### Задача 3.1. `--resume` продолжает обход, а не прогон (F-04)

- [x] **Шаг 1. Падающий тест.**

```python
# tests/test_crawler_fresh.py (или новый tests/test_resume.py)
def test_a_second_resume_goes_on_from_where_the_first_one_stopped(database):
    """Полный встал на 100-й, --resume дошёл до 150-й и тоже встал.
    Второй --resume обязан идти со 151-й, а не выбрасывать 50 страниц."""
    # full: last_page=100, errors=1
    # resume: last_page=150, errors=1
    assert resume_start_page(database.crawl_to_resume()) == (151, ...)
```

- [x] **Шаг 2. Новый метод порта + контрактный тест.**

```python
# listam/ports/database.py
@abstractmethod
def crawl_to_resume(self) -> Run | None:
    """Прерванный обход по полной ленте — тот, который продолжает `--resume`.

    Обход — это не одна строка журнала: полный прогон и продолжающие его
    `resume` идут по одной и той же ленте. Мерка — самая дальняя пройденная
    страница среди них, а не последняя строка: иначе второе продолжение
    выбрасывает всё, что прошло первое.
    """
```

Реализация: взять последний `full` (`IFNULL(mode,'full')='full'`), и если он
не завершён удачно — догнать `last_page` максимумом по строкам `mode='resume'`
с `id >` его id. Завершённый без ошибок полный обход продолжать нечего —
правило из M1 остаётся: идём с первой страницы.

- [x] **Шаг 3. Контрактные тесты:** полный без продолжений; полный + два продолжения;
  полный удачный (продолжать нечего); полный, за которым идёт `fresh` (он не в счёт);
  журнал пуст.

- [x] **Шаг 4. `crawler` зовёт новый метод** вместо `last_run(mode="full")`.
  Тест M1 `test_resume_continues_the_interrupted_full_crawl_not_the_fresh_one`
  обязан остаться зелёным — он про то же правило с другой стороны.

- [x] **Шаг 5.** Тесты. Ожидается 401 passed (394 + 7).

### Задача 3.2. Возврат с того света — отдельное событие (F-06)

- [x] **Шаг 1. Падающие тесты.**

```python
# tests/contracts/test_database_contract.py
def test_a_listing_back_from_the_dead_reports_itself(database):
    """«Вернулось на рынок» — событие рынка, а не правка поля.
    Апсерт обязан сказать об этом отдельно, иначе возврат нигде не виден."""
    # записать, пометить снятым, встретить снова тем же самым
    assert database.upsert_listing(same, seen_at=later) == "returned"
```

```python
# tests/test_gone.py
def test_a_run_counts_the_listings_that_came_back(project):
    ...  # run.returned == 1, и в ноте прогона про это сказано
```

```python
# tests/test_changes.py
def test_returned_listings_have_their_own_section(project):
    ...  # changes.returned непуст, render печатает раздел «Вернулись»
```

- [x] **Шаг 2. Миграция 005.** Схема меняется только миграцией.

```sql
-- listam/migrations/005_returned.sql
-- runs.returned — сколько снятых объявлений снова встретились на ленте.
--   Живая лента переставляет карточки под ногами у многочасового обхода:
--   часть пометок «снято» самоисправляется следующим же прогоном (на боевой
--   базе 24 из 186). Пока этого числа нет в журнале, самоисправление выглядит
--   как ничего не происходящее, а на M2 по снятым считаются медианы.
ALTER TABLE runs ADD COLUMN returned INTEGER DEFAULT 0;
```

- [x] **Шаг 3. Тест миграции** в `tests/test_migrations.py`: 005 накатывается
  на заполненную схему 4, старые строки живы, `returned` пуст.

- [x] **Шаг 4. `Run.returned`, `_row_to_run`, `finish_run` (`allowed`),
  `_Counters.returned`.** Порядок правок — как в задаче 1.2 плана M1.

- [x] **Шаг 5. `upsert_listing` возвращает `"returned"`,** когда существующая строка
  была `status='gone'`. Возврат — **не** `price_changed`: если у вернувшегося заодно
  сменилась цена, точка в `price_history` ставится как обычно, но исход прогон читает
  как `returned`, и в `updated_listings` он **не** входит (решение 5).
  Тест на эту развилку — обязателен.

- [x] **Шаг 6. `changes`:** новый раздел «Вернулись». Мерка — та же, что у снятых
  (последний полный обход), потому что вернуться может только помеченное.
  Порт: `listings_returned_since` — и контрактный тест на него.

  **Осторожно:** отдельной даты возврата в схеме нет, и заводить её в этой фазе
  не будем — `gone_at` у вернувшегося обнулён, `first_seen` не двигался. Мерка
  возврата — `last_seen >= since AND status='active'` по строкам, которые в окне
  были помечены снятыми. Если выяснится, что этого не хватает (например, `last_seen`
  двигает каждый прогон и отличить возврат от обычной встречи нечем), — это находка:
  записать её в отчёт фазы и завести колонку `returned_at` миграцией 006, а не
  подгонять выборку.

- [x] **Шаг 7. Строка итога `scrape`** в `cli._scrape` — новый счётчик.

- [x] **Шаг 8.** Тесты. Ожидается 412 passed, 12 skipped (401 + 11).

- [x] **Шаг 9. Коммит.**

```bash
git add -A
git commit -m "feat(db): возврат объявления — отдельный исход, счётчик и раздел дельты"
```

**Результат фазы 3**

Сделано: F-04 и F-06 закрыты.

**F-04 — `--resume` продолжает обход, а не строку журнала.** Заведён метод порта
`crawl_to_resume()`: он берёт последний `full` (`IFNULL(mode,'full')='full'`),
догоняет `last_page` максимумом по строкам `mode='resume'` с `id >` его id и
отдаёт строку **последнего прогона обхода** — по ней видно, закрыт обход или
оборвался. `crawler` зовёт его вместо `last_run(mode="full")`. Теперь полный,
вставший на 100-й, плюс продолжение до 150-й дают второму `--resume` страницу
151, а не 101.

**F-06 — возврат с того света стал событием.** `upsert_listing` отдаёт новый
исход `"returned"`, когда строка лежала `status='gone'`; он перекрывает
`price_changed` и `updated` (точка истории цен при этом ставится как обычно) и
в `updated_listings` не входит. Заведены счётчик `runs.returned`, нота прогона
«вернулось на ленту: N», колонка в строке итога `scrape` и четвёртый раздел
`changes` — «Вернулись» со стрелкой `↩`. Мерка у возврата та же, что у снятых
(последний полный обход): вернуться может только помеченное.

| Что | Значение |
| --- | --- |
| Батарея до фазы | 394 passed, 12 skipped |
| Батарея после фазы | **412 passed, 12 skipped** |
| Добавлено тестов | 18: 7 на задачу 3.1, 11 на задачу 3.2 |
| Схема базы | 4 → **6** (`005_returned.sql`, `006_returned_at.sql`) |
| Боевая база | не тронута: `schema_version = 4`, `listings 20 798`, `runs 6` |

**Что разошлось с планом**

1. **Понадобилась миграция 006 — ровно та находка, которую предупреждал шаг 6.**
   Мерка «`last_seen >= since AND status='active'` по помеченным снятыми» не
   работает вовсе, а не «не хватает в редком случае»: у вернувшегося `gone_at`
   обнулён, `first_seen` не двигался, а `last_seen` прогон двигает **всей
   ленте разом** — под такую выборку попадает каждое активное объявление,
   встреченное прогоном. Отличить возврат от обычной встречи в схеме 5 нечем.
   Поэтому заведена `listings.returned_at` миграцией 006, как план и велел на
   этот случай. Дата ставится только строке, которая лежала снятой, и гаснет
   при следующем снятии: колонка отвечает на вопрос «когда это объявление
   вернулось», а не «когда оно возвращалось когда-то». Контрактный тест
   `test_returned_listings_are_the_ones_that_came_back_after_the_mark`
   сторожит именно эту развилку: рядом с вернувшимся стоит объявление, которое
   просто встретилось снова, и в раздел оно не попадает.
2. **Батарея 412, а не 400.** Арифметика плана считана от 374, а фаза 2
   оставила 394. Тестов добавлено 18 — столько, сколько шаги и просили.
   Числа в шагах фазы поправлены на фактические, тесты не тронуты.
3. **Два теста зелёные с самого начала, и оба сторожевые:**
   `test_a_listing_still_on_the_feed_is_not_a_return` (обычная встреча возвратом
   не становится — иначе счётчик считал бы всю ленту) и
   `test_a_listing_that_came_back_is_not_counted_as_an_update` (возврат не
   подмешивается к обновлениям). Они сторожат правку с той стороны, с которой
   она могла бы перестараться. Записано честно.
4. **Сверх плана — один контрактный тест на `crawl_to_resume`:**
   `test_a_crawl_finished_by_a_continuation_has_nothing_left_to_resume`.
   Список шага 3 его не называл, а случай настоящий: продолжение дошло до конца
   ленты без ошибок, и обход закрыт — следующий `--resume` обязан пойти с первой
   страницы, а не досматривать досмотренное. Отсюда и решение отдавать строку
   **последнего** прогона обхода, а не самого `full`: по `full` обход всегда
   выглядел бы оборванным.
5. **Нота под разделом «Вернулись» — своими словами.** Мерка у возврата общая
   со снятыми, но нота «снятые — с полного обхода N» под разделом «Вернулись»
   читается как чужая. Печатается «вернувшиеся — с полного обхода N»;
   на это есть тест
   `test_the_returned_section_explains_its_yardstick_in_its_own_words`.
6. **Правлен один существующий тест.** В
   `test_migration_004_adds_delta_columns_to_a_filled_database` было
   `assert database.schema_version() == 4`; после 005 и 006 это уже неправда.
   Стало `>= 4`: тест про колонки миграции 004, а не про номер старшей миграции
   на диске. Пороги в конфиге не тронуты, ничего под зелёный цвет не
   подгонялось.
7. **Возврат считает любой обход, а не только полный.** Встретить снятое
   объявление на голове ленты `--fresh` умеет не хуже: счётчик и нота стоят
   до развилки «помечать ли снятых», которая полному обходу оставлена.

**Важно для фазы 5:** схема, которую ждёт код, теперь **6**, а боевая база
стоит на **4**. До наката миграций `changes` и `export` на ней откажутся
работать с кодом возврата 1 — это штатный отказ по версии, а не поломка.
Первым делом фазы 5 (после резервной копии) должен быть `python -m listam recheck`.

**Чего фаза 3 намеренно не делала:** нота про `scrape.max_pages = None` (F-09),
отказ по чужому ключу `fresh_max_pages` (F-10), латинский «Статус» в выгрузке
(F-12) и молчание `doctor` про пороги M1 (F-13) остались как были — это фаза 4.
Колонки `returned` в `.xlsx` нет и не планировалось: выгрузка — про объявления,
а не про журнал.

**Стартовый промпт для фазы 4**

> Ты продолжаешь работу над инструментом мониторинга list.am в `C:\Users\Admin\Downloads\list`.
>
> Прочитай `docs/superpowers/plans/2026-09-21-m1-qa-fixes.md` целиком: разделы
> «Общие ограничения», «Находки аудита», «Принятые решения», «Результат фазы 1»,
> «Результат фазы 2», «Результат фазы 3» и «Фаза 4». Фазу 5 не трогай — она для
> следующей сессии.
>
> Исходное состояние: HEAD — коммит фазы 3 «feat(db): возврат объявления —
> отдельный исход, счётчик и раздел дельты», дерево чистое, батарея
> **412 passed, 12 skipped**, схема, которую ждёт код, — **6**; боевая база
> по-прежнему на схеме 4, 20 798 объявлений, runs 6, не тронута (мигрирует её
> фаза 5).
>
> Твоя задача — фаза 4: сообщения, выгрузка и `doctor`. Четыре находки:
>  F-09 — сообщение об оборвавшемся обходе печатает человеку
>    `scrape.max_pages = None`: это утечка кода в текст, а не объяснение.
>  F-10 — `--fresh --max-pages 1` объясняет отказ ключом `scrape.fresh_max_pages`,
>    которого человек не задавал, и записывает прогон ошибкой.
>  F-12 — колонка «Статус» в `.xlsx` печатает `active` / `gone` латиницей, когда
>    соседние `Продавец`, `Новостройка`, `Проверено` переведены.
>  F-13 — `doctor` ничего не знает про пороги M1: опасные значения из F-02, F-07
>    и F-08 не видны, пока не навредят.
>
> Работай по шагам задач 4.1, 4.2 и 4.3: на каждое поведение — падающий тест ДО
> правки. Тесты гоняй только `.venv/Scripts/python.exe -m pytest -q`. Помни
> ограничения: новый метод порта — это новый контрактный тест в `tests/contracts/`,
> схема базы меняется только новой версионированной миграцией, пороги в конфиге
> не поднимаются, чтобы тест позеленел. Учти решение 7: `doctor` предупреждает,
> но не чинит — опасный порог это `⚠` в отчёте и ненулевой код возврата на явном
> вредительстве (`max_gone_percent: 0`), а конфиг остаётся территорией человека.
> Числа «ожидается N passed» в плане фазы 4 считаны от 374, а батарея сейчас 412 —
> сверяй, что именно добавилось, и правь число в плане, а не тесты.
>
> В конце сессии допиши в план раздел «Результат фазы 4»: что сделано, числа
> батареи, что разошлось с планом и почему, и стартовый промпт для фазы 5.
> Сделай коммит.

---

## Фаза 4. Сообщения, выгрузка и `doctor` (F-09, F-10, F-12, F-13)

**Зачем:** мелочь, но вся она — ровно в том месте, где человек читает результат.
Нота с `None`, чужой ключ конфига в объяснении отказа и латинское `gone` в русской
таблице — это то, по чему брокер решает, доверять инструменту или нет.

### Задача 4.1. В нотах нет `None` и чужих ключей (F-09, F-10)

- [x] **Шаг 1. Падающие тесты.**

```python
# tests/test_crawler.py
def test_a_single_page_feed_is_explained_without_none(project):
    """«хотя scrape.max_pages = None» — это не объяснение, а утечка кода в текст."""
    run = run_scrape(project)                 # лента ровно в одну страницу
    assert "None" not in run.notes

# tests/test_crawler_fresh.py
def test_a_hand_written_limit_is_blamed_on_the_flag_not_on_the_config(project):
    """Человек написал --max-pages 1; ключа scrape.fresh_max_pages он не видел."""
    run = run_scrape(project, fresh=True, max_pages=1)
    assert "--max-pages" in run.stop_reason
    assert "scrape.fresh_max_pages" not in run.stop_reason
```

- [x] **Шаг 2. Правка текста «обход кончился на первой странице»:** без лимита —
  «обход кончился на первой странице, хотя лента категории — это сотни страниц».

- [x] **Шаг 3. `incremental_stop` получает, откуда взялся потолок** (`"config"` /
  `"flag"`), и называет в тексте то, что человек задавал.

- [x] **Шаг 4. Решить и записать: остаётся ли `--fresh --max-pages N` ошибкой
  прогона.** Аудит считает, что нет: человек явно попросил короткий обход, он его
  и получил; помечать такой прогон ошибкой и отказывать в заливке базы — перебор.
  Предлагаемое решение: при потолке **с флага** — `stop_reason` без ошибки,
  при потолке **из конфига** — ошибка, как сейчас (обход не дошёл до известных,
  и об этом надо знать). Тест на обе ветки.

- [x] **Шаг 5.** Тесты. Ожидается 404 passed. → **Вышло 419 passed, 12 skipped** (арифметика плана считана от 374, а фаза 3 оставила 412; добавлено 7 тестов).

### Задача 4.2. «Статус» по-русски (F-12)

- [x] **Шаг 1. Падающий тест** в `tests/contracts/test_exporter_contract.py`:
  в колонке «Статус» — «на ленте» / «снято», латиницы нет.

- [x] **Шаг 2. Правка `_present`** — рядом с `SELLER_TYPES`, тем же приёмом.
  Колонок по-прежнему 22, `A1:V` — это проверяется существующим тестом, он обязан
  остаться зелёным.

- [x] **Шаг 3.** Тесты. Ожидается 405 passed. → **Вышло 421 passed, 12 skipped** (добавлено 2 теста).

### Задача 4.3. `doctor` показывает пороги M1 (F-13)

- [x] **Шаг 1. Падающие тесты** в `tests/test_doctor.py`: конфиг с
  `max_gone_percent: 0` → проверка не `ok`, в тексте сказано, чем это грозит;
  конфиг с `max_pages: 2` → `⚠` про то, что обход укорочен потолком окружения;
  боевой конфиг → проверка зелёная и перечисляет значения.

- [x] **Шаг 2. Новая проверка «Пороги прогона»** в `run_doctor`: печатает
  `max_pages`, `fresh_stop_after_known_pages`, `fresh_max_pages`, `max_gone_percent`,
  `expected_pages_min`, `max_pages_drop_percent` и объясняет опасные значения.
  Конфиг `doctor` не правит (решение 7).

- [x] **Шаг 3.** Тесты. Ожидается 408 passed, 12 skipped. → **Вышло 427 passed, 12 skipped** (добавлено 6 тестов).

- [x] **Шаг 4. Коммит.**

```bash
git add -A
git commit -m "fix(ui): ноты без None, «Статус» по-русски, doctor знает про пороги"
```

**Результат фазы 4**

Сделано: F-09, F-10, F-12 и F-13 закрыты, на каждое поведение тест написан до
правки и посмотрен красным.

| Что | Значение |
| --- | --- |
| Батарея до фазы | 412 passed, 12 skipped |
| Батарея после фазы | **427 passed, 12 skipped** (`.venv/Scripts/python.exe -m pytest -q`, 60 с) |
| Добавлено тестов | 15: 7 на задачу 4.1, 2 на 4.2, 6 на 4.3 |
| Правлен существующий тест | 1 (`test_a_gone_listing_shows_its_status_and_the_day_it_left`) |
| Схема базы | не трогалась — по-прежнему **6** |
| Конфиги | не трогались |
| Боевая база | не трогалась |

**F-09 — в ноте про обход в одну страницу больше нет `None`.** Текст называет
человеку то, что он задавал: потолка не было — «пагинатор не дал следующей
страницы: обход кончился на первой, а лента категории — это сотни страниц»;
потолок с флага — «хотя просили `--max-pages N`»; потолок из конфига — «хотя
`scrape.max_pages = N`», как было. Красный до правки: `'None' is contained here`
ровно в том месте, которое предъявил аудит.

**F-10 — потолок инкрементального обхода знает, откуда он взялся.**
`incremental_stop` получил `ceiling_source` (`"config"` / `"flag"`) и
`ceiling_key`. Потолок **с флага** — просьба: `stop_reason` «инкрементальный
обход остановлен на странице N: столько и просили (`--max-pages N`)» и **ноль
ошибок**, то есть база заливается. Потолок **из конфига** — ошибка, как и была:
обход не дошёл до известных, и об этом надо знать. Третий случай назван своим
ключом: если потолок опустил `scrape.max_pages` из конфига, в тексте стоит он,
а не `scrape.fresh_max_pages`. Красный до правки: `assert 1 == 0` на
`--fresh --max-pages 1` и `TypeError` на неизвестном параметре.

**F-12 — «Статус» по-русски.** Рядом с `SELLER_TYPES` появился
`STATUSES = {"active": "на ленте", "gone": "снято"}` и ветка в `_present` —
тем же приёмом, что и продавец. 22 колонки и `A1:V` остались: существующий тест
на них зелёный.

**F-13 — `doctor` показывает пороги M1.** Новая проверка «Пороги прогона»
печатает все шесть значений (`max_pages`, `fresh_stop_after_known_pages`,
`fresh_max_pages`, `max_gone_percent`, `expected_pages_min`,
`max_pages_drop_percent`) через ту же `threshold()`, которой их читает прогон, —
и объясняет опасные. `Check` получил третье состояние `warn`: `⚠` в отчёте,
но код возврата 0. Конфиг `doctor` не правит (решение 7).

| Значение | Что говорит `doctor` |
| --- | --- |
| `max_gone_percent: 0` | **СБОЙ**, код возврата 1: снятыми не будет помечено ничего, раздел «Снято» останется пустым навсегда |
| `max_gone_percent: null` | `⚠` предохранителя нет: оборванный обход пометит снятыми всё, до чего не дошёл |
| `max_pages: N` | `⚠` обход укорочен потолком окружения: полным считается, но всю ленту не видит |
| `fresh_stop_after_known_pages: null` | `⚠` останавливаться `--fresh` нечему — дойдёт до потолка и запишется ошибкой |
| `fresh_stop_after_known_pages: 0` | `⚠` встанет на первой же странице без новых |
| `fresh_max_pages: 0` | `⚠` упрётся в потолок, не пройдя ни страницы |
| `max_pages_drop_percent: null` и `expected_pages_min: null` | `⚠` недобор страниц не проверяется ничем |

**Проверено на живом конфиге (только чтение, `config/dev.yaml` не тронут):**
`python -m listam doctor --no-network` — строка «Пороги прогона» зелёная и
перечисляет значения, итог «Всё на месте». Копия конфига во временной папке
с `max_gone_percent: 0` и `max_pages: 2` даёт **СБОЙ** и код возврата **1**;
копия с одним `max_pages: 2` — `⚠` и код возврата **0**.

**Что разошлось с планом**

1. **Батарея 427, а не 408.** Арифметика плана считана от 374, а фаза 3
   оставила 412. По шагам плана выходило 6 новых тестов (2 + 1 + 3), написано 15:
   на F-09 три (без потолка, потолок из конфига, потолок с флага), на F-10
   четыре (три на сам `incremental_stop` и один сквозной), на «Статус» два
   (перевод и отсутствие латиницы), на `doctor` шесть (перечисление значений,
   вредительство, два предупреждения, `⚠` в тексте отчёта и честный итог).
   Числа в шагах фазы поправлены на фактические, тесты не тронуты.
2. **Тест из плана на F-09 дословно не годился.** `run_scrape(project)` на
   фикстуре из двух страниц ноту про одну страницу не печатает вовсе — ленту
   надо сначала свести к одной странице (`only_page`, как в соседнем
   `test_run_that_never_left_the_first_page_is_a_failure`). Поведение то же,
   заготовка из плана — нет.
3. **Тест на F-10 из плана проверял `stop_reason`, а красным был `errors`.**
   Написаны обе половины: три теста на сам `incremental_stop` (текст с флага,
   отсутствие ошибки с флага, ошибка из конфига) и один сквозной на
   `run_scrape(fresh=True, max_pages=1)` — `errors == 0` и текст без чужого ключа.
4. **Один тест зелёный с самого начала и оставлен сторожевым:**
   `test_a_ceiling_from_the_config_is_named_by_its_own_key` — потолок из конфига
   и до правки назывался своим ключом, и тест сторожит правку F-09 с той
   стороны, с которой она могла бы перестараться и стереть верный текст.
   Записано честно.
5. **Правлен один существующий тест.** В
   `test_a_gone_listing_shows_its_status_and_the_day_it_left` было
   `== "gone"` — ровно та латиница, которую фаза убирает; стало `== "снято"`.
   Это не подгонка под зелёный цвет, а то самое поведение, которое меняет F-12;
   красным его посмотрели вместе с двумя новыми.
6. **Сверх плана — третье состояние проверки и честный итог.** `Check.warn`
   в плане не назван, но без него `⚠` из шага 1 задачи 4.3 не выражается:
   `ok` — это код возврата, а укороченный обход кодом возврата ронять нельзя.
   Заодно поправлен итог отчёта: «Всё на месте» поверх `⚠` — неправда, теперь
   печатается «Запускать можно, но есть предупреждения». На оба поведения есть
   тесты.
7. **Новых методов порта фаза не завела** — контрактных тестов в `tests/contracts/`
   прибавилось два, и оба на выгрузку («Статус» по-русски и отсутствие латиницы).

**Чего фаза 4 намеренно не делала:** боевая база по-прежнему на схеме 4 и
не тронута, на сайт никто не ходил, README и контрольный список не правлены —
это фаза 5.

**Стартовый промпт для фазы 5**

> Ты продолжаешь работу над инструментом мониторинга list.am в `C:\Users\Admin\Downloads\list`.
>
> Прочитай `docs/superpowers/plans/2026-09-21-m1-qa-fixes.md` целиком: разделы
> «Общие ограничения», «Находки аудита», «Принятые решения», «Результат фазы 1»,
> «Результат фазы 2», «Результат фазы 3», «Результат фазы 4» и «Фаза 5».
>
> Исходное состояние: HEAD — коммит фазы 4 «fix(ui): ноты без None, «Статус»
> по-русски, doctor знает про пороги», дерево чистое, батарея
> **427 passed, 12 skipped**, схема, которую ждёт код, — **6**; боевая база
> `data/listam.sqlite` по-прежнему на схеме **4**, 20 798 объявлений, runs 6,
> не тронута.
>
> Твоя задача — фаза 5: боевая база, живая лента и документация. Это
> **единственная** фаза, которая трогает `data/listam.sqlite` и ходит на сайт.
> Первым делом — резервная копия (шаг 1), только потом `recheck`.
>
> Учти: шаг 2 плана говорит «довести схему до 5» — это число устарело, фаза 3
> завела миграции 005 и 006, и `recheck` доведёт базу до **6**. До наката
> `changes` и `export` на боевой базе откажутся работать с кодом возврата 1 —
> это штатный отказ по версии, а не поломка.
>
> Любой прогон CLI из скрипта — только с `PYTHONIOENCODING=utf-8`, иначе
> кириллица в дочернем процессе пропадёт целиком. Тесты гоняй только
> `.venv/Scripts/python.exe -m pytest -q`. Пороги в конфиге не поднимаются,
> чтобы что-то позеленело; схема меняется только новой версионированной
> миграцией; новый метод порта — это новый контрактный тест.
>
> В конце сессии заполни «Результат фазы 5» и «Контрольный список приёмки»,
> допиши README и сделай коммит.

---

## Фаза 5. Боевая база, живая лента и документация

**Зачем:** до этой фазы всё проверено на фикстурах. Починка предъявлена тогда, когда
`changes` показал человеку все четыре раздела на настоящей базе.

**Осторожно:** это единственная фаза, которая трогает `data/listam.sqlite` и ходит
на сайт. Копия базы снимается до всего остального.

- [x] **Шаг 1. Снять копию боевой базы.**

```bash
cp data/listam.sqlite "data/listam-before-m1qa-$(date -u +%Y%m%d-%H%M%S).sqlite"
```

- [x] **Шаг 2. Довести схему до 6.** (В плане стояло «до 5» — число
  устарело до того, как фаза 3 завела миграции 005 и 006.)

```bash
PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe -m listam recheck
```

Ожидается: схема **6**, строк 20 798, ошибок 0, `runs.returned` пуст у всех.

- [x] **Шаг 3. `doctor` на боевом конфиге** — новая проверка порогов зелёная.

- [x] **Шаг 4. Полный обход.**

```bash
PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe -m listam scrape
```

Записать: страниц, карточек, новых, сменивших цену, снятых, **вернувшихся**, ошибок,
`stop_reason`, `mode`. Ждём `mode=full` и непустой `returned`: 162 снятых лежат
в базе со вчерашнего дня, часть из них живая.

- [x] **Шаг 5. `changes` сразу после полного обхода** — четыре раздела, счётчик
  «Сменили цену» сходится с `runs.price_changed` (это F-05, предъявить числом).

- [x] **Шаг 6. Через час — инкрементальный обход.**

```bash
PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe -m listam scrape --fresh
```

- [x] **Шаг 7. `changes` после инкрементального — главная проверка этого плана (F-01).**
  Раздел «Снято» обязан остаться непустым и назвать свою мерку: «снятые — с полного
  обхода N». Если он пуст — фаза 1 не закрыта, и это блокер, а не «ну почти».

- [x] **Шаг 8. Выгрузка.**

```bash
PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe -m listam export
```

Проверить: 22 колонки, `A1:V`, «Статус» — «на ленте» / «снято», «Снято» непусто
ровно у снятых.

- [x] **Шаг 9. Прогнать контрольный список приёмки** (таблица ниже) целиком,
  вывод показать в сессии.

- [x] **Шаг 10. Обновить README:** раздел про дельту (две мерки окна и почему),
  раздел про возврат объявлений, `doctor` и пороги, проверка ввода команд,
  пересчитанные числа базы и размер батареи. Шапка: M1 закрыт после разбора аудита.

- [x] **Шаг 11. Заполнить «Результат фазы 5» и контрольный список,** дописать
  стартовый промпт для сессии M2.

- [ ] **Шаг 12. Коммиты и чистое дерево.**

```bash
git status --short
git add -A
git commit -m "docs: разбор QA-аудита M1 закрыт"
```

**Результат фазы 5**

Фаза прошла целиком и **не потребовала ни одной правки кода**: всё, что чинили
фазы 1–4 на фикстурах, подтвердилось на боевой базе и живой ленте с первого
раза. Единственная фаза плана, которая трогала `data/listam.sqlite` и ходила
на сайт; правлены только `README.md` и этот файл.

| Что | Значение |
| --- | --- |
| Батарея | **427 passed, 12 skipped** — та же, что оставила фаза 4: кода фаза не меняла |
| Схема боевой базы | 4 → **6** (`recheck`, миграции 005 и 006) |
| Копия до работ | `data/listam-before-m1qa-20260921-200619.sqlite` (`quick_check: ok`, схема 4, 20 798 строк, runs 6) |
| Объявлений после фазы | 20 826 — 20 619 активных, 207 снятых |
| Прогонов в журнале | 6 → 8 |
| Правлено файлов | 2: `README.md`, план |

**Шаг 2 — накат схемы.** `recheck`: схема 6, 20 798 строк, изменено 0, ошибок 0.
`runs.returned` = 0 у всех шести прежних прогонов, `listings.returned_at` пуст —
миграции легли на живые данные, ничего не переписав. (Число «до 5» в заголовке
шага поправлено на 6 прямо в плане: оно устарело до того, как фаза 3 завела
миграции 005 и 006.)

**Шаг 3 — `doctor`.** Строка «Пороги прогона» зелёная и перечисляет все шесть
значений боевого конфига; «Схема базы — версия 6». Итог «Всё на месте».

**Шаг 4 — полный обход (прогон 7, 20:07–20:18 UTC, 11 минут).**

| Мерка | Значение |
| --- | --- |
| режим | `full` |
| страниц | 215 |
| карточек | 20 613 |
| новых | 21 |
| обновлённых | 32 |
| сменили цену | 25 |
| снято | 46 |
| **вернулось** | **1** |
| ошибок | 0 |
| `stop_reason` | конец ленты: пагинатор не дал следующей страницы |

**Шаг 5 — `changes` сразу после полного обхода.** Четыре раздела, и число строк
в каждом в точности равно своему счётчику: Новых 21, Сменили цену 25, Снято 46,
Вернулось 1. **F-05 предъявлен числом: 25 = 25 = `runs.price_changed` прогона 7.**
Претензия аудита (69 против 68, карточка в списке дважды) закрыта.

**Шаг 6 — инкрементальный обход через час (прогон 8, 21:18 UTC).**
`mode=fresh`, **3 страницы вместо 215**, 288 карточек, 7 новых, 1 смена цены,
снято 0, ошибок 0, встал по `scrape.fresh_stop_after_known_pages = 2`.

**Шаг 7 — главная проверка плана (F-01). Закрыта на живой базе.**
`changes` после часового `--fresh`:

```
Изменения с начала прогона 8 (2026-09-21 21:18 UTC, режим fresh)
Новых: 7   Сменили цену: 1   Снято: 46   Вернулось: 1

Снято
  снятые — с полного обхода 7 (2026-09-21 20:07 UTC)
Вернулись
  вернувшиеся — с полного обхода 7 (2026-09-21 20:07 UTC)
```

Окно новых и цен уехало на прогон 8, а разделы «Снято» и «Вернулись» остались
на прогоне 7 и **назвали свою мерку вслух**, каждый своими словами. До починки
здесь был бы ноль: раздел «Снято» гас первым же часовым прогоном, и при
расписании из README человек не увидел бы снятых никогда. Обратная сторона
(пункт 2 списка) тоже предъявлена: раздел показывает 46 — те, кого пометил
прогон 7, — а не 186 прогона 5 и не 207 снятых, лежащих в базе всего.

**Шаг 8 — выгрузка.** 22 колонки, автофильтр `A1:V20827`. «Статус» по-русски:
«на ленте» 20 619, «снято» 207. Колонка «Снято» заполнена ровно у 207 снятых
и **ни у одного** активного.

**Что разошлось с планом**

1. **Вернулось 1, а не «часть из 162», как ждал шаг 4.** План рассчитывал, что
   заметная доля вчерашних снятых окажется живой (отчёт фазы 5 плана M1 мерил
   самоисправление в 13%, то есть ожидание было порядка двух десятков).
   Полный обход нашёл ровно одно вернувшееся объявление — `23593951`,
   Эчмиадзин, 3 комнаты, 90 000 $. Механика F-06 предъявлена на живых данных
   end-to-end — счётчик `runs.returned`, нота прогона, раздел `changes`,
   колонка `returned_at`, — но запас тоньше, чем обещал план. Это записано
   как есть, а не подогнано: поднимать `--hours`, чтобы затянуть в раздел
   побольше строк, значило бы мерить не то. Причина, скорее всего, в сроке:
   между прогоном 5 (16:07) и прогоном 7 (20:07) прошло четыре часа, а не сутки,
   и большая часть вчерашних пометок ещё не успела ни подтвердиться, ни отпасть.
   Проверить это можно будет на базе, прожившей несколько суток, — но уже вне
   этого плана.
2. **`--fresh` прошёл 3 страницы, а не 6, как прогон 6.** Лента за час принесла
   7 новых объявлений, и две страницы подряд без новых набрались раньше. Порог
   не трогался; это разброс головы ленты, а не изменение поведения.
3. **Батарея не выросла: 427 passed, 12 skipped — столько же, сколько оставила
   фаза 4.** Это не пропущенная работа: фаза 5 по замыслу не пишет кода, она
   предъявляет написанное на живых данных. Новых методов порта не заводилось,
   значит и новых контрактных тестов не требовалось.
4. **Все 46 снятых показаны как «висело 0 дней».** Не находка: база набрана
   21.09, и между `first_seen` и `gone_at` у них меньше суток. На базе постарше
   колонка начнёт различать.
5. **Зонд F-09 пришлось строить на странице без пагинатора.** Свести ленту к
   одной странице, просто убрав вторую, недостаточно: пагинатор фикстуры ведёт
   на неё, и обход падает с «страница не получена» — это другая нота, не та,
   что чинит F-09. Нужный текст («пагинатор не дал следующей страницы: обход
   кончился на первой, а лента категории — это сотни страниц») появляется
   только на странице, которая сама никуда не ведёт. На поведение кода это
   не влияет — влияет на то, как его проверять.
6. **Пороги в конфиге не тронуты, схема — только миграциями 005 и 006,
   заведёнными фазой 3.** Ни одного нового ключа, ни одной правки `config/*.yaml`.

**Что фаза не делала намеренно:** не трогала находки 1 и 2 отчёта фазы 5 плана
M1 (тонкий запас у порога остановки, самоисправление пометок) — план их прямо
исключает; не заводила колонку `returned` в `.xlsx` — выгрузка про объявления,
а не про журнал; не пересматривала формат отметок времени и курсы EUR/RUB.

## Контрольный список приёмки

Прогнан целиком 22.09.2026 в конце фазы 5. Пункты 1–3, 11, 14 — на боевой базе
и живой ленте; 5–10, 12, 13 — на временных базах и фикстурах (ходить на сайт
ради порога, который и так виден на фикстуре, незачем); 15 — на боевом конфиге
и на его испорченной копии во временной папке, сам `config/dev.yaml` не тронут.

| № | Проверка | Как проверяем | Ожидаем | Вышло |
| --- | --- | --- | --- | --- |
| 1 | F-01 на боевой базе | `changes` после часового `--fresh` | раздел «Снято» непуст, мерка названа | ✅ Снято 46, «снятые — с полного обхода 7 (2026-09-21 20:07 UTC)» |
| 2 | F-01, обратная сторона | полный обход → `changes` | список снятых пересобран с новой мерки | ✅ 46 прогона 7, а не 186 прогона 5 и не 207 всего |
| 3 | F-05 | `changes` против `runs.price_changed` | числа сошлись, карточка в «Ценах» одна | ✅ 25 = 25 = 25 после прогона 7; 1 = 1 = 1 после прогона 8 |
| 4 | F-11 | `changes --hours -5`, `--limit 0` | код 2, внятный отказ | ✅ код 2 на обоих, плюс `--limit -1` |
| 5 | F-02 | временная база, `max_gone_percent: 0` | помечено 0, в ноте про порог | ✅ помечено 0, нота «…больше порога scrape.max_gone_percent = 0%», объявление осталось `active` |
| 6 | F-02, вторая ветка | `max_gone_percent: null` | порога нет, помечено всё пропавшее | ✅ помечено 1, `status='gone'`, `gone_at` проставлен, ошибок 0 |
| 7 | F-03 | `scrape --max-pages 0` | код 2, обход не начался | ✅ код 2 мгновенно: ни сети, ни замка; то же на `-1` |
| 8 | F-07 | конфиг с `max_pages`, обычный `scrape` | `mode=full`, снятые помечены | ✅ `mode=full`, помечено 1, ошибок 0 |
| 9 | F-08 | `fresh_stop_after_known_pages: 0` | обход встал на первой же странице без новых | ✅ 1 страница, «1 страниц подряд без новых объявлений (… = 0)», ошибок 0 |
| 10 | F-04 | журнал: `full` → `resume` → `resume` | третий идёт дальше второго | ✅ `crawl_to_resume().last_page = 150` при `full(100)` + `resume(150)` → следующий идёт со 151, а не со 101 |
| 11 | F-06 | боевой полный обход | `returned > 0`, раздел «Вернулись» в `changes` | ✅ `runs.returned = 1`, раздел есть и называет свою мерку. Запас тоньше плана — см. расхождение 1 |
| 12 | F-09 | лента в одну страницу | в ноте нет `None` | ✅ «пагинатор не дал следующей страницы: обход кончился на первой, а лента категории — это сотни страниц», `None` нет |
| 13 | F-10 | `--fresh --max-pages 1` | ошибок 0, отказ назван флагом | ✅ ошибок 0, «столько и просили (`--max-pages 1`)», чужого `fresh_max_pages` в тексте нет |
| 14 | F-12 | `.xlsx` | «Статус» по-русски, 22 колонки, `A1:V` | ✅ 22 колонки, `A1:V20827`, «на ленте» 20 619 / «снято» 207, колонка «Снято» заполнена ровно у снятых |
| 15 | F-13 | `doctor` на боевом и на испорченном конфиге | пороги показаны, опасные помечены | ✅ боевой — `OK` и все шесть значений; копия с `max_gone_percent: 0` — `СБОЙ`, код 1; копия с одним `max_pages: 2` — `⚠`, код 0 |
| 16 | Регрессия M1 | вся батарея | passed сошлось с планом, skipped 12 | ✅ 427 passed, 12 skipped — столько же, сколько оставила фаза 4: кода фаза 5 не писала |
| 17 | Дерево | `git status --short` | чисто | ✅ после коммита чисто |

## Что в этот план не входит

- Находки 1 и 2 отчёта фазы 5 плана M1 (тонкий запас у порога остановки; 13% пометок
  «снято» самоисправляются). Обе записаны честно и лечения не требуют; F-06 закрывает
  только их видимость, а не саму механику.
- Матчинг, заявки, кластеры — M2. Телефоны — M4. Уведомления — M3.
- Курсы EUR и RUB. 57 строк по-прежнему живут с суммой в валюте оригинала.
- Формат отметок времени в базе (текст ISO-8601, сравнение строковое). Работает,
  потому что всё пишет один `to_iso`; трогать его ради красоты в этом плане незачем.

---

## Стартовый промпт для новой сессии (фаза 1)

```
Ты продолжаешь работу над инструментом мониторинга list.am в C:\Users\Admin\Downloads\list.

Прочитай docs/superpowers/plans/2026-09-21-m1-qa-fixes.md целиком: разделы
«Общие ограничения», «Находки аудита», «Принятые решения» и «Фаза 1».
Фазы 2–5 не трогай — они для следующих сессий.

Исходное состояние: HEAD 2e13f8a, дерево чистое, батарея 363 passed, 12 skipped,
боевая база на схеме 4, 20 798 объявлений (20 636 active, 162 gone), runs 6.

Твоя задача — фаза 1: витрина дельты. Три находки:
 F-01 (блокер) — `changes` теряет раздел «Снято» после первого же инкрементального
   прогона, потому что окно считается от начала любого последнего прогона, а снятых
   ставит только полный обход. При расписании из README человек их не увидит никогда.
 F-05 — карточка, сменившая цену дважды в окне, попадает в список дважды, и счётчик
   «Сменили цену» считает точки истории, а не объявления (на боевой базе 69 против 68).
 F-11 — `changes --hours -5` берёт окно в будущем, `--limit 0` печатает «…и ещё 7».

Работай по шагам задач 1.1, 1.2 и 1.3: на каждое поведение — падающий тест ДО правки.
Тесты гоняй только .venv/Scripts/python.exe -m pytest -q. Пороги в конфиге не трогай.
Новый метод порта — это новый контрактный тест в tests/contracts/.

В конце сессии допиши в план раздел «Результат фазы 1»: что сделано, числа батареи,
что разошлось с планом и почему, и стартовый промпт для фазы 2. Сделай коммит.
```

---

## Разбор QA-аудита M1 закрыт

Все 13 находок закрыты за пять фаз, каждая — падающим тестом до правки.
Батарея выросла с 363 до **427 passed, 12 skipped**, схема базы — с 4 до **6**
(миграции 005 и 006), боевая база и живая лента предъявлены фазой 5.

| Фаза | Находки | Батарея после |
| --- | --- | --- |
| 1 | F-01, F-05, F-11 | 376 |
| 2 | F-02, F-03, F-07, F-08 | 394 |
| 3 | F-04, F-06 | 412 |
| 4 | F-09, F-10, F-12, F-13 | 427 |
| 5 | — (боевая база, лента, документация) | 427 |

Что осталось незакрытым намеренно — в разделе «Что в этот план не входит».
Отдельно стоит помнить расхождение 1 фазы 5: механика возврата объявлений
предъявлена на боевой базе одним объявлением, а не двумя десятками, как ждал
план. Мерить её как следует можно будет на базе, прожившей несколько суток.

## Стартовый промпт для новой сессии (M2)

```
Ты продолжаешь работу над инструментом мониторинга list.am в C:\Users\Admin\Downloads\list.

M0 и M1 закрыты, разбор строгого QA-аудита M1 закрыт тоже — все 13 находок,
пять фаз, отчёты по каждой в docs/superpowers/plans/2026-09-21-m1-qa-fixes.md.
Прочитай в нём «Общие ограничения», «Принятые решения» и «Результат фазы 5»,
а также спеку «list.am → заявки покупателей MVP-спека и роадмап.md» — раздел M2.

Исходное состояние: дерево чистое, батарея 427 passed, 12 skipped, схема базы 6,
боевая база — 20 826 объявлений (20 619 активных, 207 снятых), прогонов 8,
последний полный — 7, последний инкрементальный — 8.

Твоя задача — M2: заявки и матчинг. Заявки покупателей приходят из внешнего
источника (порт RequestsSource, адаптеры gsheet/csv — в конфиге сейчас
`requests: {kind: none}`), объявления подбираются под заявку, результат ложится
в `matches`. Таблицы `requests` и `matches` в схеме уже есть с M0 — посмотри,
что в них заведено, прежде чем менять.

Ограничения те же и нарушать их нельзя: пороги в конфиге не поднимаются, чтобы
что-то позеленело; схема меняется только новой версионированной миграцией;
новый метод порта — это новый контрактный тест в tests/contracts/; ни один путь,
ключ и адаптер не зашит в код. Тесты гоняй только
.venv/Scripts/python.exe -m pytest -q, любой прогон CLI из скрипта — только с
PYTHONIOENCODING=utf-8.

Начни с брейншторма и плана (docs/superpowers/plans/), а не с кода.
```
