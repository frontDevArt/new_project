# M0: парсер карточек и обход категории — план

> **Для исполнителя:** шаги отмечаются чекбоксами (`- [ ]` → `- [x]`) по мере выполнения.
> Порядок задач менять нельзя: каждая следующая опирается на результат предыдущей.

**Цель:** закрыть этап M0 — получить базу на 20 000 объявлений и `.xlsx`, который можно открыть и отсортировать.

**Подход:** парсер пишется и отлаживается на сохранённых страницах (`scrape.kind: files`), сайт не трогаем.
Когда разбор подтверждён на фикстурах — включаем `playwright`, идём 20 страниц, смотрим статистику,
потом полный обход. Каждая задача заканчивается проверяемым результатом.

**Стек:** Python 3.12, BeautifulSoup + lxml, SQLite, openpyxl, pytest.

**Спека:** `list.am → заявки покупателей MVP-спека и роадмап.md`

## Что уже готово (до этого плана)

Каркас, порты, миграция `001_initial.sql`, `doctor`, `export`, три реализации `Fetcher`
(`http`, `playwright`, `files`), контрактные тесты. База пустая: `listings 0`, `runs 0`.
Парсера карточек и команды обхода нет — это и есть содержание плана.

## Общие ограничения (из спеки, нарушать нельзя)

- Пагинация только путевая: `/ru/category/60/2`. Query-параметры фильтров (`n=`, `price1=`, `rooms=`, `srt=`, …) запрещены в `robots.txt`.
- Пауза между запросами — из конфига, по умолчанию 1.5 с. Никаких параллельных пачек.
- Все отметки времени в UTC, пути относительные от корня проекта.
- Ни один путь, ключ и идентификатор не зашит в код — только конфиг и `.env`.
- Парсер не падает из-за отсутствия любого поля в карточке: нет поля — `None`.
- Схема базы меняется только новой версионированной миграцией.
- Телефоны на M0 не трогаем вообще.

## Разбор вёрстки (снято с живых страниц 21.09.2026)

Одна страница ленты — 105 ссылок `/item/<id>`, из них 9 в блоке `#tp` («Топ объявления»).
Топ-блок повторяется от страницы к странице и не подчиняется сортировке по дате, поэтому
**он исключается**: парсим `#contentr` без поддерева `#tp` → 96 карточек на страницу.

| Поле | Откуда берётся | Пример |
| --- | --- | --- |
| `id` | `/ru/item/(\d+)` из `href` | `24221707` |
| `url` | абсолютный адрес из `href` без query | `https://www.list.am/ru/item/24221707` |
| цена + валюта | `.category-data-list-card__amount`, символ внутри `.category-data-list-card__currency` | `290,000` + `$`; `33,912,000` + `֏`; встречается `€` |
| `title` | `div.l` | `2-комн. квартира в новостройке на ул. Ачаряна в Аване, 56 кв.м., 7/18 этаж` |
| `rooms`, `area`, `floor`, `floors_total` | `div.at` (первый, не `__location`) | `2 ком., 56 кв.м., 7/18 этаж` |
| `district` | `.category-data-list-card__location` | `Кентрон`, `Арабкир`, `Аван` |
| `seller_type` | `span.ge3` = `Агентство` → `agency`; значка нет → `owner` | 183 агентства из 210 карточек |
| `verified` | `span.pr93`: класс `unverified` → `False`; `Проверено по кадастру` / `Собственность подтверждена` → `True` | |
| `new_build` | в заголовке `в новостройке` | |
| `street` | из заголовка: `на ул. X` / `на пр. X` / `на ул-е X` | `Ачаряна` |

Значки `po77` (`Срочно`, `Супер цена`, `Красивый вид`) — маркетинг продавца, не сохраняем.

---

## Задача 1. Парсер карточек ленты

**Файлы:**
- Создать: `listam/parsers/__init__.py`, `listam/parsers/category_list.py`
- Создать: `tests/fixtures/category-60-page1.html` (обрезанный реальный снимок), `tests/test_parser_category.py`

**Интерфейс (на него опирается задача 2):**

```python
def parse_listing_cards(html: str, base_url: str = "https://www.list.am/ru") -> list[Listing]
def parse_next_page(html: str) -> int | None   # номер следующей страницы или None
```

`Listing` заполняется без курса: `price_raw`, `currency`, и «сырая» сумма — в `price_amd`
или `price_usd` по валюте. Пересчёт и `price_per_sqm` — задача 2, там есть `RateProvider`.

- [x] **Шаг 1.** Собрать фикстуру: из `data/pages/category-60.html` взять блок `#tp` (2 карточки),
  6 карточек из `#contentr` (доллар, драм, евро, агентство, собственник, проверено по кадастру)
  и один пагинатор. Плюс одна синтетическая карточка без цены, площади и района.
- [x] **Шаг 2.** Написать падающие тесты `tests/test_parser_category.py`: количество карточек (топ-блок исключён),
  поля первой карточки целиком, драмовая цена, евро, `seller_type=owner` без значка,
  `verified=True` по кадастру, карточка-калека даёт `Listing` с `None` вместо полей, `parse_next_page` → 2.
- [x] **Шаг 3.** Прогнать: `python -m pytest tests/test_parser_category.py -q` — ожидается падение (модуля нет).
- [x] **Шаг 4.** Реализовать `listam/parsers/category_list.py`.
- [x] **Шаг 5.** Прогнать всю батарею: `python -m pytest -q` — зелено.
- [x] **Шаг 6.** Показать разбор 2–3 карточек с живого снимка — показано, жду подтверждения.
- [x] **Шаг 7.** Коммит — вошёл в первый коммит репозитория `9bb6958`
  (`feat: каркас проекта, парсер карточек category/60 и команда scrape`).

**Результат задачи 1 (21.09.2026)**

Создано: `listam/parsers/category_list.py`, `tests/fixtures/category-60-page1.html` (9.8 КБ,
обрезанный настоящий снимок), `tests/test_parser_category.py` — 10 тестов. Вся батарея:
123 passed, 7 skipped.

Прогон по двум живым снимкам (`data/pages/`): 96 карточек со страницы, обе страницы
разобраны, пагинатор отдаёт 2 и 3.

| Показатель | Значение |
| --- | --- |
| Всего карточек | 192 (уникальных 191 — один дубль между страницами) |
| Валюты | USD 183, AMD 8, EUR 1 |
| Продавцы | агентства 173, собственники 19 |
| Проверено по кадастру | 9, не проверено 177, значка нет 6 |
| Новостройки | 98 из 192 |
| Пустых полей | площадь 0, район 0, улица 36 |

Улица не читается у 19% карточек: в заголовке она бывает без предлога («14-я улица в Раздане»)
или её нет вовсе. Поле справочное, на матчинг не влияет (там район), поэтому оставлено как есть —
при необходимости добирается из карточки объявления на M4.

Решение по `seller_type`: сайт помечает значком только агентские объявления, частные ничем.
Значит `agency` по значку `span.ge3`, иначе `owner`. Проверить это стоит на живом прогоне
задачи 3: если доля собственников резко уедет вверх — значит значок иногда не отдаётся.

## Задача 2. Команда `scrape`

**Файлы:**
- Создать: `listam/crawler.py`
- Изменить: `listam/cli.py` (подкоманда), `README.md` (таблица команд)
- Создать: `tests/test_crawler.py`

**Интерфейс:** `run_scrape(config, *, max_pages=None, dry_run=False) -> Run`

- [x] **Шаг 1.** Тест на фейковом `Fetcher` (две страницы из фикстур): прогон пишет `listings`,
  точку в `price_history`, строку в `runs` с зафиксированным курсом; повторный прогон не плодит дубли.
- [x] **Шаг 2.** Тест `--dry-run`: база не меняется.
- [x] **Шаг 3.** Реализовать: курс в начале прогона → цикл по страницам (пауза из конфига,
  `max_pages` из конфига/флага) → апсерт → `price_history` только при изменении цены → запись `runs`.
- [x] **Шаг 4.** `python -m pytest -q` — зелено.
- [x] **Шаг 5.** Прогон на сохранённых страницах: `scrape.kind: files`, `python -m listam scrape` — в базе ~192 объявления.
- [x] **Шаг 6.** Коммит — вошёл в тот же первый коммит `9bb6958`: `cli.py`, `README.md` и
  `tests/test_cli.py` менялись в обеих задачах, разрезать их задним числом можно только
  выдумав промежуточные версии, которых в рабочем дереве никогда не было.

**Результат задачи 2 (21.09.2026)**

Создано: `listam/crawler.py` (`run_scrape`, `apply_rate`, `page_path`),
`tests/fixtures/category-60-page2.html` (3 карточки, одна повторяет карточку первой
страницы, пагинатор последней страницы), `tests/test_crawler.py` — 11 тестов.
Изменено: `listam/cli.py` (подкоманда `scrape` с `--max-pages` и `--dry-run`),
`tests/test_cli.py` (+3 теста), `README.md`. Вся батарея: 137 passed, 7 skipped.

Прогон на сохранённых страницах (`scrape.kind: files`, две страницы в `data/pages/`):

| Показатель | Значение |
| --- | --- |
| Карточек разобрано | 192 |
| Объявлений в базе | 191 (один дубль между страницами не записан повторно) |
| Точек в `price_history` | 191 |
| Строка в `runs` | курс 363.25 (rate.am, банков: 17), страниц 2, новых 191 |
| Валюты | USD 182, AMD 8, EUR 1 |
| Продавцы | агентства 172, собственники 19 |
| Медиана $/м² | 2284.16 |
| Повторный прогон | новых 0, обновлённых 0, дублей в `price_history` нет |
| `--dry-run` | 192 карточки разобраны, в базу и в хранилище не ушло ничего |

Что стоит знать про прогон:

Курс берётся один раз в начале и попадает в строку `runs` — старые цифры
воспроизводятся. Пересчёт валют делает прогон, не парсер: у парсера курса нет.
Евро курс не покрывает, поэтому у единственной евровой карточки `price_usd` и
`price_per_sqm` пустые — это видно в базе и честнее выдуманного курса.

Пауза между запросами живёт в адаптере `Fetcher` (`scrape.delay_seconds`), поэтому
цикл обхода её не дублирует и параллельных запросов не появляется по построению.

Страница, которая не отдалась, останавливает прогон и считается ошибкой: счётчик
`runs.errors` и текст в `runs.notes`, код возврата 1. На снимке из двух страниц это
видно как «страница 3 не получена» — пагинатор второй страницы честно зовёт дальше,
а третьей на диске нет. На живом прогоне задачи 3 такой строки быть не должно.

Курс на сохранённых страницах берётся тем же адаптером `rate_am`: снимок rate.am
лежит рядом с лентой в `data/pages/` под именем, которое даёт `FilesFetcher`
(`ru-armenian-dram-exchange-rates-banks-cash.html`). Отдельного «тестового» курса
в коде нет.

`config/dev.yaml` оставлен на `scrape.kind: files` — задача 3 начинается с того, что
его нужно переключить обратно на `playwright`.

## Задача 3. Двадцать страниц живьём

- [x] **Шаг 1.** `scrape.kind: playwright`, `python -m listam scrape --max-pages 20`.
- [x] **Шаг 2.** Статистика: собрано / уникальных / разбивка по районам / медиана $/м² по районам / доля собственников.
- [x] **Шаг 3.** Глазами проверить 5 случайных карточек против сайта. Расхождение — правка парсера и повтор на снимке, не на сайте.
- [x] **Шаг 4.** Коммит правок парсера, если были — парсер не правился, закоммичен только
  фикс вывода CLI (`95ca99a`).

**Результат задачи 3 (21.09.2026)**

Первый живой выход на сайт. Прогон: 20 страниц за 1 минуту 10 секунд, ошибок 0,
Cloudflare пройден с сохранённым профилем браузера, ни одна страница не потерялась.

| Показатель | Значение |
| --- | --- |
| Страниц | 20 |
| Карточек собрано | 1920 (ровно 96 x 20 — ни одной пропущенной страницы) |
| Уникальных id | 1920 (дублей между страницами нет) |
| Новых в прогоне | 1729 (191 уже лежали со снимков задачи 2) |
| Обновлённых | 7 |
| Курс в `runs` | 363.25 (rate.am, банков: 17) |
| Ошибок | 0 |
| Медиана $/м² по всей базе | 2161.02 |

Валюты: USD 1783, AMD 133, EUR 3, RUB 1. Рубль на снимках не встречался — курс его
не покрывает, поэтому `price_usd` пустой у 4 карточек (3 евро + 1 рубль). Это
по-прежнему честнее выдуманного курса; если рублёвых и евровых объявлений станет
заметно больше, курс добирается из того же `rate.am` отдельной задачей.

Прочее: проверено по кадастру 108, не проверено 1787, значка нет 25; новостройки 782;
без района 0, без площади 1, без улицы 349 (19% — та же доля, что на снимках).

Медиана $/м² по районам (топ по числу карточек):

| Район | Карточек | Медиана $/м² | Медиана $ |
| --- | --- | --- | --- |
| Кентрон | 428 | 3560.61 | 315 000 |
| Арабкир | 409 | 2573.53 | 195 000 |
| Малатия-Себастия | 215 | 1787.23 | 135 000 |
| Ачапняк | 173 | 1812.50 | 142 000 |
| Нор Норк | 137 | 1858.82 | 125 000 |
| Канакер-Зейтун | 107 | 1950.00 | 159 000 |
| Давташен | 87 | 2076.92 | 177 500 |
| Шенгавит | 83 | 1902.82 | 128 000 |
| Аван | 51 | 1837.84 | 127 000 |
| Эребуни | 30 | 1945.76 | 130 000 |

Картина совпадает с рыночной: Кентрон дороже всех, дальше Арабкир, спальные районы
около 1800–1950 $/м². Хвост из 48 городов и сёл (Гюмри, Ванадзор, Севан, Дилижан,
Эчмиадзин и далее по одной-двум карточкам) — лента category/60 не ограничена Ереваном.
На матчинг это не влияет: заявка приходит с районом.

**Гипотеза по `seller_type` проверена.** На снимках собственников было 10%, на живых
20 страницах — 20.9% (агентства 1518, собственники 402). Доля выросла вдвое, но не
«резко уехала»: два снимка задачи 2 — это первые страницы ленты, куда агентства
поднимают объявления чаще. Значок `span.ge3` отдаётся стабильно, правило
«есть значок → `agency`, нет → `owner`» остаётся.

**Сверка 5 случайных карточек с сайтом — расхождений ноль.**

| id | Проверено | Итог |
| --- | --- | --- |
| 24256037 | $155 000, 80 м², 1/9, 4 комн., агентство, не проверена, не новостройка | сходится |
| 22854849 | $125 000, 90 м², 1/9, 4 комн., собственник, проверено по кадастру | сходится |
| 20101369 | $218 000, 72 м², 6/6, 3 комн., агентство, Кентрон («в центре») | сходится |
| 24212590 | $292 000, 115 м², 3/16, 3 комн., собственник, новостройка | сходится |
| 21925663 | $41 000, 64 м², 2/4, 2 комн., собственник, новостройка, Мартуни | сходится |

Отдельно подтвердилось, что этаж берётся из `div.at`, а не из заголовка: у 20101369
в заголовке этажа нет вообще («каменное здание»), а в базе стоит верное 6/6.

**Найденный баг (исправлен, `95ca99a`).** Прогон сходил в сеть, записал 1920 объявлений
и упал с `UnicodeEncodeError` на печати итога: под Windows перенаправленный stdout
приходит в `cp1252`, и первая кириллическая буква роняет процесс уже после всей
полезной работы. `main()` теперь переводит stdout и stderr в UTF-8 с `errors="replace"`;
тест поднимает поток в `cp1252` и прогоняет через него `scrape`. Вся батарея:
138 passed, 7 skipped.

`config/dev.yaml` остаётся на `scrape.kind: playwright` — задача 4 начинается с
`max_pages: null`.

## Задача 4. Полный обход и выгрузка

- [x] **Шаг 1.** `max_pages: null`, полный обход (~215 страниц, ~20 600 объявлений, ~7–10 минут).
- [x] **Шаг 2.** `python -m listam export` → `.xlsx`: шапка заморожена, автофильтр, ссылка кликабельная,
  цена и площадь числами, сортировка по `first_seen`.
- [x] **Шаг 3.** Отдать файл, зафиксировать в README, что M0 закрыт.

**Результат задачи 4 (21.09.2026)**

Полный обход категории `60`: 215 страниц за 11 минут 36 секунд (09:25:39–09:37:16 UTC),
ошибок 0, ни одной потерянной страницы, профиль браузера проверку Cloudflare прошёл
без пересоздания. Изменений в коде не потребовалось: конфиг переведён на
`max_pages: null`, всё остальное отработало как на двадцати страницах задачи 3.

| Показатель | Значение |
| --- | --- |
| Страниц | 215 (214 полных по 96 карточек + последняя на 32) |
| Карточек собрано | 20 576 |
| Уникальных id в прогоне | 20 567 (9 карточек повторились между соседними страницами) |
| Новых | 18 649 |
| Обновлённых | 2 |
| Объявлений в базе | 20 569 |
| Точек в `price_history` | 20 570 (у одного объявления две — цена менялась) |
| Курс в строке `runs` | 363.25 (rate.am, банков: 17) |
| Ошибок | 0 |
| Медиана $/м² по базе | 2155.17 |

Валюты: USD 18 539, AMD 1973, EUR 54, RUB 3. `price_usd` пустой у 57 карточек —
это ровно евро и рубли: курс rate.am их не покрывает, выдуманного курса в базе нет.
Продавцы: агентства 16 493, собственники 4076 (19.8% — та же доля, что на двадцати
страницах задачи 3, значит правило по значку `span.ge3` держится на всей ленте).
Проверено по кадастру 777, не проверено 19 571, значка нет 221. Новостроек 8605.
Пустых полей: район 0, площадь 0, комнаты 0, этаж 0, улица 4161 (20.2% — та же доля,
что на снимках и на двадцати страницах).

Медиана $/м² по районам и городам (топ 15 по числу карточек):

| Район / город | Карточек | Медиана $/м² | Медиана $ |
| --- | --- | --- | --- |
| Кентрон | 4757 | 3500.00 | 298 000 |
| Арабкир | 3962 | 2553.02 | 205 000 |
| Ачапняк | 1674 | 1877.19 | 135 000 |
| Малатия-Себастия | 1638 | 1744.32 | 130 000 |
| Нор Норк | 1498 | 1800.00 | 121 129 |
| Канакер-Зейтун | 1275 | 2000.00 | 158 000 |
| Давташен | 966 | 2187.50 | 174 000 |
| Шенгавит | 943 | 1863.52 | 129 990 |
| Аван | 793 | 1923.08 | 140 000 |
| Эребуни | 407 | 1800.00 | 128 000 |
| Гюмри | 283 | 878.15 | 67 722 |
| Эчмиадзин | 241 | 1375.00 | 95 000 |
| Ванадзор | 224 | 811.91 | 56 000 |
| Норк-Мараш | 196 | 2075.23 | 171 500 |
| Абовян | 177 | 1489.49 | 92 500 |

Всего в базе 116 различимых районов и городов. На двадцати страницах их было 58:
хвост регионов виден только на полном обходе. Ереванские медианы почти не сдвинулись
относительно задачи 3 (Кентрон 3560 → 3500, Арабкир 2573 → 2553) — первые страницы
ленты оказались представительной выборкой по цене. Региональные города вдвое дешевле
ереванских спальных районов: Гюмри 878 $/м², Ванадзор 812 $/м².

**Заготовка под M1 уже видна в данных.** Два объявления из прогона задачи 3 на полном
обходе не встретились — они ушли с ленты за восемь минут между прогонами. Сейчас у них
`status=active` и старый `last_seen`; именно это и станет признаком `status=gone` на M1.

**Выгрузка.** `python -m listam export` → `out/listam-20260921-0937.xlsx`, 2.9 МБ,
20 569 строк и 20 колонок (после QA-аудита их 21, автофильтр `A1:U` — см. раздел
«M0 дозакрыт» ниже). Проверено на открытом файле: лист «Объявления», шапка
заморожена (`freeze_panes=A2`), автофильтр на `A1:T20570`, в колонке «Ссылка» слово
«открыть» с настоящей гиперссылкой на `list.am/ru/item/<id>`, цена и площадь лежат
числами с форматами `#,##0` и `0.0` (не текстом — сортировка и фильтр по ним работают),
строки отсортированы по `first_seen` по убыванию.

Батарея тестов после задачи: 138 passed, 7 skipped — код не менялся, менялся конфиг.

**M0 закрыт.** Критерий готовности выполнен: база на 20 569 объявлений, строка в `runs`
с зафиксированным курсом, `.xlsx` на руках.

## M0 дозакрыт после QA-аудита (21.09.2026)

Строгий QA-аудит нашёл 18 находок, из-за которых закрывать M0 было рано: ошибка
прогона не отменяла заливку общей базы, замок утекал, `--resume` притворялся успехом,
разряды в цене читались счётчиком разделителей, цены в EUR и RUB терялись целиком,
боевая база стояла на схеме 1 без колонки `anomaly`, а числа в этом плане не сходились
с кодом. Разбор и план починки — `docs/superpowers/plans/2026-09-21-m0-qa-fixes.md`
(пять фаз, на каждую находку — падающий тест до правки).

Что изменилось в числах этого плана:

| Что | Было в плане | Стало на деле |
| --- | --- | --- |
| Колонок в `.xlsx` | 19, автофильтр `A1:S` | **21**, автофильтр `A1:U20570` — добавились «Сомнительно» и «Цена в валюте» |
| Схема базы | 1 | **3** (миграции `002` и `003`) |
| Помечено аномалией | колонки не было | **24** объявления из 20 569 |
| Цены без пересчёта | терялись | **57** строк в EUR и RUB, у всех заполнена «Цена в валюте» |
| Батарея тестов | 138 passed, 7 skipped | **312 passed, 12 skipped** |
| Медианы | считались вместе с опечатками продавцов | пересчитаны **без помеченных** (таблица ниже) |

Медиана по базе без помеченных: **2155 $/м²** и **162 000 $** за квартиру (20 488 строк
с ценой за метр). Медианы по районам (топ-15 по числу карточек, помеченные исключены):

| Район / город | Карточек | Медиана $/м² | Медиана $ |
| --- | --- | --- | --- |
| Кентрон | 4733 | 3500.00 | 298 000 |
| Арабкир | 3954 | 2553.02 | 205 000 |
| Ачапняк | 1668 | 1876.10 | 135 000 |
| Малатия-Себастия | 1636 | 1744.32 | 130 000 |
| Нор Норк | 1496 | 1800.00 | 121 129 |
| Канакер-Зейтун | 1270 | 2000.00 | 158 000 |
| Давташен | 964 | 2191.97 | 174 500 |
| Шенгавит | 941 | 1863.52 | 129 990 |
| Аван | 789 | 1916.67 | 140 000 |
| Эребуни | 406 | 1800.00 | 128 500 |
| Гюмри | 278 | 878.15 | 67 722 |
| Эчмиадзин | 241 | 1375.00 | 95 000 |
| Ванадзор | 219 | 812.50 | 57 000 |
| Норк-Мараш | 196 | 2075.23 | 171 500 |
| Абовян | 175 | 1487.80 | 93 000 |

Районов и городов с непомеченными карточками — 115. Ереванские медианы от исключения
опечаток почти не сдвинулись (Кентрон 3500.00, Арабкир 2553.02 — те же), и это
ожидаемо: 24 строки на 20 569 медиану не двигают. Смысл пометки не в том, чтобы
поправить среднее, а в том, чтобы «3-комн., 1 кв.м.» и «$ 87,000,000 за 82 м²» не
приходили брокеру как варианты под заявку.

Боевая база доведена до схемы 3 командой `python -m listam recheck` (разовая операция,
копия базы снята перед ней), выгрузка пересобрана: `out/listam-20260921-1205.xlsx`.

## Критерий готовности M0

База на 20 000 объявлений, строка в `runs` с курсом, `.xlsx` на руках. После этого — M1
(инкрементальный прогон до известного ID, `status=gone`), схема под него уже есть.

---

## Стартовый промпт для новой сессии (задача 4)

Открыть новую сессию **в этой же папке** (`C:\Users\Admin\Downloads\list`) и скопировать целиком:

```markdown
Проект: инструмент брокера недвижимости, мониторинг list.am под заявки покупателей.
Работаем в папке проекта, ветка master.

Прочитай перед началом, в этом порядке:
1. docs/superpowers/plans/2026-09-21-m0-parser-crawl.md — план этапа M0, он же твоё задание.
2. «list.am → заявки покупателей MVP-спека и роадмап.md» — спека: разведка, ограничения,
   схема данных, раздел про переносимость. Раздел про переносимость обязателен к исполнению.
3. README.md — как устроен проект и чем он запускается.

Делаешь ТОЛЬКО задачу 4 из плана — полный обход и выгрузку. Задачи 1–3 сделаны и приняты:
парсер, команда scrape и живой прогон на 20 страниц (1920 объявлений, 0 ошибок, сверка
5 карточек с сайтом без расхождений). Конфиг уже на scrape.kind: playwright.

Правила, которые нельзя нарушать:
- Пагинация только путевая (/ru/category/60/2). Query-параметры фильтров запрещены robots.txt.
- Пауза между запросами берётся из конфига, параллельных запросов нет. Полный обход — это
  ~215 страниц и 7–10 минут, торопить его нельзя.
- Ни один путь, ключ, идентификатор и имя адаптера не зашит в код: внешнее — за портом,
  выбор — в конфиге, секреты — только в .env. Новые реализации подключаются в listam/wiring.py.
- Все отметки времени в UTC. Пути относительные от корня проекта.
- Схема базы меняется только новой версионированной миграцией в listam/migrations/.
- Правки парсера отлаживаются на сохранённых страницах, не повторным походом на сайт.

Запуск тестов — интерпретатором из виртуального окружения проекта:
.venv/Scripts/python.exe -m pytest -q
Системный python не годится: в нём нет openpyxl.

Когда закончишь: покажи мне статистику полного обхода, отдай .xlsx, отметь в README,
что M0 закрыт, и обнови раздел «Результат задачи 4» в файле плана — тем же форматом,
что у задач 1–3.
```
