-- Версия 3: сумма в валюте оригинала.
--
-- Курсов EUR и RUB у нас нет и выдумывать их нельзя, а до этой колонки такие
-- цены исчезали: price_usd и price_amd оставались пустыми, и в базе от цены
-- оставалась одна сырая строка. price_amount хранит число ровно как на сайте,
-- валюта его лежит рядом в listings.currency. Пересчитанные колонки
-- price_usd/price_amd остаются пересчитанными и для EUR/RUB пустыми.

ALTER TABLE listings ADD COLUMN price_amount REAL;
